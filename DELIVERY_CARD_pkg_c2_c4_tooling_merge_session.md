# تسليم — دمج ملفات PKG-C المتبقية (backup/restore + lint enforcement tooling) في النسخة الأساسية (2026-08-16، جلسة تابعة)

**السياق:** يكمل هذا التسليم `DELIVERY_CARD_pkg_a_b_c_d_merge_session.md` عند
النقطة التي توقف عندها بالضبط: *"لم يُدمَج بعد: `docker-compose.selfhosted.yml`،
سكربتات `backup_db.sh`/`test_backup_restore.sh`، سكربتات k6، `ci.yml`،
`audit_tenancy_isolation.py`."* هذه الجلسة تدمج ما هو **ملفي أنا فعلاً** من
تلك القائمة، وتترك ما تبيّن أنه يحتاج قراراً غير تقني لصاحب القرار (انظر §3).

---

## 1. ما دُمج فعلياً

| ملف | الحالة |
|---|---|
| `scripts/audit_tenancy_isolation.py` | **جديد، مُدمَج** |
| `scripts/test_backup_restore.sh` | **جديد، مُدمَج** |
| `scripts/generate_lint_baseline.sh` | **جديد، مُدمَج — أُعيدت كتابته بالكامل (انظر §2)** |
| `scripts/check_lint_against_baseline.py` | **جديد، مُدمَج — أُعيدت كتابته بالكامل (انظر §2)** |
| `scripts/load/k6_sales_invoice_create.js` | **جديد، مُدمَج** |
| `scripts/load/k6_sales_invoice_post.js` | **جديد، مُدمَج** |
| `scripts/load/k6_trial_balance_report.js` | **جديد، مُدمَج** |
| `scripts/backup_db.sh` | صلاحية تنفيذ ناقصة (`-rw-------`) — **صُحِّحت (`chmod +x`)**، المحتوى نفسه لم يتغيّر |
| `docker-compose.selfhosted.yml`, `.env.selfhosted.example`, `scripts/run_migrations.sh`, `scripts/seed_demo_company.py` | **فُحِصت (`diff`) — متطابقة بايت-لبايت مع نسخي، لا حاجة لدمج** |
| `.github/workflows/ci.yml` | **لم يُلمَس — عمداً، انظر §3** |

## 2. اكتشاف حرج قبل الدمج — لم أدمج نسختي الأصلية كما هي

نسخة `check_lint_against_baseline.py`/`generate_lint_baseline.sh` من جلسة
PKG-C المعزولة استخدمت صيغة baseline **مختلفة** (تجميع حسب (ملف، قاعدة، عدد))
عن الصيغة **الحقيقية المُعتمَدة فعلياً** في هذا المستودع
(`apps/core-api/known_failures.txt`، `apps/ai-platform/known_failures.txt`
— سطر ruff خام كامل، كما أنشأتها `TASK-CI-03` فعلياً بتاريخ 2026-08-13، ولم
تكن مرئية لي في جلسة PKG-C المعزولة). دمج نسختي الأصلية كانت لتنافس صيغة
موجودة فعلاً وتُهدر عملاً حقيقياً موثَّقاً (تصحيح رقم "278" المتوارَث في
الخطة إلى الرقم الحقيقي 78، مثلاً).

**أعدت كتابة كلا السكربتين بالكامل** ليطابقا صيغة المشروع الفعلية (مقارنة
سطر-بسطر كاملة، لا تجميع)، ووثّقت صراحة في السكربت الجديد القيد المعروف لهذه
الصيغة (انزياح رقم السطر من تعديل غير مرتبط يُظهر مخالفة قديمة كأنها
"جديدة") — **بدليل حقيقي لا افتراضي:** ضبطت هذا يحدث فعلاً في
`apps/core-api/known_failures.txt` الحالي (سطرا `E741` في
`test_period_closing.py` انزاحا بمقدار سطر واحد بسبب تعديل لاحق غير متعلق).

### اختبار السكربتين الجديدين — فعلياً، ضد الملفات الحقيقية

| سيناريو | التنفيذ | النتيجة |
|---|---|---|
| `apps/core-api` الحالي مقابل `known_failures.txt` الحقيقي (بلا تعديل) | تشغيل مباشر | ❌ **انحراف حقيقي مكتشَف**: 7 مخالفات فعلية غير مسجَّلة في الأساس — 5 من ملفات جديدة انضمت لاحقاً (دمج PKG-A: `purchasing_20260813_...`, `create_purchase_invoice_from_ai_draft_use_case.py`, إلخ)، و2 انزياح سطر حقيقي في `test_period_closing.py`. **لم أُعدِّل `known_failures.txt` نفسه** — ليس ملفي، والقرار بإعادة توليده لصاحب `TASK-CI-03/04` |
| `apps/ai-platform` الحالي مقابل `known_failures.txt` الحقيقي | تشغيل مباشر | ✅ لا انحراف — الأساس لا يزال دقيقاً |
| أساس مُولَّد حديثاً (`generate_lint_baseline.sh` فعلياً) مقابل نفسه | تشغيل مباشر | ✅ نظيف كما هو متوقَّع |
| مخالفة جديدة مُقحَمة يدوياً (`import os, sys` غير مستخدَم) في ملف حقيقي | تشغيل مباشر، ثم تراجع | ❌ اكتُشِفت بدقة (4 مخالفات: E401/I001/F401×2) على الملف الصحيح بالضبط، ثم ✅ بعد التراجع |

## 3. لماذا لم يُلمَس `ci.yml` — قرار مبني على دليل، لا تردد

`ALQAIM_V2_MASTER_EXECUTION_PLAN.md` يقفل `TASK-CI-05` (فشل PR على مخالفات
**جديدة فقط** — وهو بالضبط ما يبنيه `check_lint_against_baseline.py` أعلاه)
خلف `GATE-03` صراحة (`§12`: *"`TASK-CI-05` — إلزامي تدريجياً... SERIAL،
**بعد GATE-03 فقط**"*). و`DELIVERY_CARD_pkg_a_b_c_d_merge_session.md` نفسه
يؤكد **`GATE-03` لا يزال مقفولاً فعلياً** (فجوة `known_suppliers`/
`known_products` في `/ai/documents/analyze`).

**دليل إضافي مباشر من هذه الجلسة يؤكد القرار:** السويت الحالي لـ`core-api`
فيه فعلياً **11 اختبار فاشل حقيقي الآن** (قوالب IDOR غير مكتملة من PKG-C1،
غير أمنية — انظر التقرير الأصلي). لو أُزيل `continue-on-error` من خطوة
`pytest` الآن، **سينكسر CI فوراً على كل PR** لسبب لا علاقة له بأي تغيير
جديد يقدّمه أي مطوّر — بالضبط السيناريو الذي `GATE-03`/تسلسل `TASK-CI`
مصمَّم لمنعه.

**القرار:** `ci.yml` بقي بلا تعديل (لا يزال `continue-on-error: true` على
كل شيء، كما كان). `check_lint_against_baseline.py` و`test_backup_restore.sh`
مُدمَجان كأدوات **جاهزة للاستخدام يدوياً أو من قبل مالك `TASK-CI-04/05`**،
وليسا مُفعَّلين تلقائياً في أي workflow. هذا ليس تقاعساً — هو الالتزام
الحرفي بترتيب المهام الموثَّق في الخطة الرئيسية نفسها.

## 4. PKG-C2 — تحقُّق فعلي كامل هذه المرة، بالـSchema الحقيقية

الجلسة المعزولة السابقة لـPKG-C2 وثّقت نجاحاً فعلياً لكن على **schema
تمثيلية** (3 جداول بسيطة أنشأتها بنفسي)، لأن `platform_core/` والـmigrations
الحقيقية لم تكونا مرئيتين في تلك الحزمة المعزولة. **هذه الفجوة مُغلَقة الآن
بالكامل:**

1. `alembic upgrade head` حقيقي على Postgres 16 محلي — **27 migration**،
   وصل لنفس الرأس الوحيد الموثَّق في تقرير الدمج (`pos_20260816_0001`).
2. `scripts/seed_demo_company.py` **الحقيقي غير المعدَّل** شغَّل فعلياً
   (شركة + فرع + مستودع + تصنيف + وحدة قياس + منتج، عبر الـUse Cases
   الحقيقية، لا SQL خام).
3. زُرعت بيانات تمثيلية (500 فاتورة مبيعات، 300 قيد يومية، 800 حركة مخزون)
   **داخل الجداول الحقيقية** (`sales_invoices`, `journal_entries`,
   `stock_movements`) محترمة كل القيود الحقيقية (NOT NULL، Foreign Keys
   لشركة/فرع/مستودع/شريك حقيقيين).
4. `scripts/test_backup_restore.sh` **غير المعدَّل** شغَّل الدورة الكاملة:
   نسخ احتياطي حقيقي (208K) → **`DROP DATABASE` فعلي** → استعادة → مقارنة
   MD5. **النتيجة: تطابق كامل للجداول الثلاثة.**
5. تحقُّق إضافي بعد الاستعادة (لم يكن في التقرير الأصلي): كل الجداول الـ62
   نجت (لا فقط الثلاثة المُقاسة)، `alembic_version` سليم (`pos_20260816_0001`
   لا يزال الرأس)، وصفر صفوف يتيمة (Foreign Key integrity) بين
   `sales_invoices` وكل من `companies`/`branches`/`warehouses`.

**هذا يُغلق التوصية الصريحة في تقرير PKG-C2 الأصلي** ("النقطة التالية هي
استبدال بحت: التأشير لقاعدة تطوير حقيقية... لا تغيير آخر متوقَّع") — تم
فعلياً، لا نظرياً.

## 5. `audit_tenancy_isolation.py` — تحقُّق ضد الشجرة الكاملة الحقيقية

شُغِّل ضد `apps/core-api/modules` الحقيقية بكل الـ18 وحدة (لا عيّنة
`REFERENCE_ONLY` المستخدَمة في الجلسة المعزولة). **النتيجة مطابقة تماماً**
للعيّنة السابقة: 0 HIGH RISK، 11 NEEDS-MANUAL-VERIFICATION، 7 بلا
id-endpoints — يؤكد أن العيّنة الأصلية كانت تمثيلية فعلاً للشجرة الحقيقية،
لا مجرد تخمين محظوظ.

## 6. التحقق النهائي — صفر تراجع

```
apps/core-api: pytest tests/integration/ modules/partners/tests/ -q
    → 147 passed, 11 failed, 13 skipped, 4 errors
      (مطابق حرفياً لما وثّقه DELIVERY_CARD_pkg_a_b_c_d_merge_session.md —
       صفر تراجع فعلي من هذا الدمج)

apps/ai-platform: pytest tests/ -q
    → 21 passed, 0 failed (Redis حقيقي — ثبَّتُّه وشغَّلته في هذه الجلسة)
```

⚠️ ملاحظة عملية من هذا التشغيل: أول محاولة أظهرت `150 passed, 7 skipped,
7 errors` (بدل `147/13/4`) بسبب تلوّث بيئي ذاتي — متغيرات
`ALQAIM_DATABASE_URL`/`TEST_DATABASE_URL` من اختبار PKG-C2 اليدوي (§4)
بقيت مُصدَّرة في نفس الجلسة الطرفية. `tests/integration/conftest.py`
يستخدم SQLite في الذاكرة حصراً (`sqlite+aiosqlite:///:memory:`)، بمعزل تام
عن هذين المتغيرين — فتأثيرهما كان محصوراً بملف اختبار واحد
(`test_walkin_partial_unique_index.py`) له إعداد مختلف. `unset` لهما أعاد
الرقم الصحيح فوراً. يُذكَر كتذكير عملي: عزل متغيرات البيئة بين خطوات
الاختبار اليدوي والتحقق الآلي، لا كخلل في الكود المُدمَج.

## 7. المتبقي (لم يُلمَس في هذه الجلسة، بالتصميم)

- **9 من 11 ملف IDOR** — بيانات payload/seed فقط (موثَّقة في التقرير
  الأصلي)، ليست "ملفاتي" — تخص من أكمل التفعيل الحقيقي في الجلسة المعزولة
  السابقة.
- **`docker-compose.selfhosted.yml`** — لا حاجة فعلية للدمج (متطابق بالفعل،
  §1).
- **`ci.yml`** — عمداً، §3.
- **PKG-D بالكامل** — خارج نطاق "ملفاتي فقط" المطلوب هذه الجلسة تحديداً.
- تحديث `STATUS_20_TASKS.md`/`PROJECT_STATUS_SUMMARY.md` — لم يُطلَب في
  نطاق هذه الجلسة.
