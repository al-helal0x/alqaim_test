## TASK-BI-01 — لوحة "نبض الشركة اليومي" (Business Pulse Dashboard) — Vertical Slice أولى

> **حالة التنفيذ: IMPLEMENTED + TESTED.** نُفِّذت وتحقَّق منها فعلياً (لا
> افتراضاً) على شجرة `AlQaim_V2_v21_phase5_FINAL_MERGED__3_`. التفاصيل
> الكاملة في "سجل التنفيذ الفعلي" في نهاية هذا الملف. **لم تصل بعد لحالة
> ACCEPTED** — ينقصها تأكيد مستخدم فعلي حسب بند Product Acceptance أدناه،
> وهذا لا يمكن لأي تنفيذ آلي أن يحققه بديلاً عن مستخدم حقيقي.

**الهدف:** Endpoint واحد + مكون واجهة واحد يعرضان 5 مؤشرات حيّة من بيانات
حقيقية موجودة فعلاً في وحدات `sales` / `accounting` / `inventory` /
`payments`، بلا أي طبقة AI، وبلا تغيير معماري جديد.

**المشكلة التي يحلها:** لا توجد اليوم أي واجهة تجميعية واحدة يفتحها
مالك/محاسب الشركة ليرى "أين نقف الآن" — كل رقم موجود لكن مبعثر بين شاشات
منفصلة (فواتير، ميزان مراجعة، حركات مخزون، صندوق).

**Dependency:** لا شيء جديد — تُبنى فوق وحدات تعمل فعلياً اليوم
(`sales`, `accounting`, `inventory`, `payments` جميعها موجودة ومُختبَرة
حسب `PROJECT_STATUS_SUMMARY.md`). لا تعتمد على Outbox أو أي عمل AI قيد
الإنجاز.

**Preconditions:**
- تأكَّد أن `SalesInvoiceCreated` / `InvoicePosted` (`sales_use_cases.py`)
  و`FiscalPeriodClosed` (`fiscal_period_use_cases.py`) تُنشَر فعلياً على
  بيئتك المحلية قبل البدء — إن لم تكن كذلك، هذه المهمة لا تحتاجها أصلاً
  (المستوى 1/2 المقترَح أدناه استعلام مباشر، لا اشتراك أحداث).
- تحقّق من وجود بيانات اختبار فعلية (فواتير مرحَّلة + حركات مخزون) في
  بيئة `dev` — لوحة على قاعدة بيانات فارغة لا تختبر شيئاً حقيقياً.

**Files الممنوع لمسها:**
`modules/accounting/*`, `modules/sales/*`, `modules/inventory/*`,
`modules/payments/*` — **قراءة فقط عبر use cases/repositories القائمة،
بلا أي تعديل على منطق هذه الوحدات.** أي حاجة لتعديل هناك تعني أن نطاق
هذه المهمة غير صحيح ويجب التوقف والتصعيد، لا التوسّع الصامت.

**Files المتوقَّع إنشاؤها (جديد بالكامل):**
- `apps/core-api/modules/reporting/application/use_cases/business_pulse_use_case.py`
- `apps/core-api/modules/reporting/application/dto/business_pulse_dto.py`
- إضافة مسار واحد جديد فقط داخل
  `apps/core-api/modules/reporting/presentation/routes/reports_router.py`
  (نفس نمط `get_trial_balance` الموجود فعلاً في هذا الملف — لا راوتر جديد منفصل)
- واجهة: مكوّن جديد `BusinessPulseDashboard` (React/Next.js) — مسار
  الواجهة يُحدَّد حسب بنية `apps/web` الفعلية وقت التنفيذ (لم أُثبِّته هنا
  لأنه غير مؤكَّد من هذه الحزمة).

**النطاق الدقيق للمؤشرات الخمسة (لا أكثر في هذه النسخة):**
1. مبيعات اليوم/آخر 7 أيام مقابل نفس الفترة السابقة — من `sales` (فواتير
   مرحَّلة `InvoicePosted` فقط، لا مسودات)
2. أعلى 5 منتجات مبيعاً (بالقيمة) — من بنود الفواتير المرحَّلة
3. رصيد الصندوق/الحسابات البنكية الحالي — من `payments`
4. عناصر تحت حد إعادة الطلب — من `inventory` (استعلام موجود أصلاً في
   `balances_router.py` أو ما يعادله، إعادة استخدام لا إعادة بناء)
5. أعمار الذمم المدينة (0-30/31-60/60+) — من `accounting` + `payments`

**التنفيذ خطوة بخطوة:**
1. اقرأ Use Cases القائمة فعلياً في الوحدات الأربع أولاً (لا تخمّن
   التوقيعات) — تحديداً `GetTrialBalanceUseCase` كمرجع نمط، ودوال
   `inventory_use_cases.py` / `payments_use_cases.py` ذات الصلة بالأرصدة.
2. `BusinessPulseUseCase.execute(company_id, date_range)` يستدعي **4
   دوال قراءة موجودة فعلاً بالتوازي** (لا استعلام SQL خام جديد مباشر على
   الجداول — المرور إلزامياً عبر repositories القائمة لضمان فلترة
   `tenant_id`/`company_id` الإلزامية).
3. لا Materialized View ولا جدول تجميع في هذه النسخة الأولى — استعلام
   مباشر عند الفتح (مستوى 1 فقط، حسب الدراسة السابقة). التحسين لجدول
   تجميع/Event-driven يُفتح كمهمة منفصلة **فقط بعد قياس فعلي لبطء حقيقي**،
   لا افتراضاً مسبقاً.
4. DTO الاستجابة: 5 حقول مطابقة تماماً للمؤشرات الخمسة أعلاه، بلا حقول
   إضافية "قد تفيد لاحقاً" (تفادي Scope Creep من داخل المهمة نفسها).
5. الواجهة: بطاقات رقمية + رسمان بيانيان فقط (خط للمبيعات، أعمدة لأعلى 5
   منتجات) — لا مكتبة رسوم بيانية جديدة إن كانت هناك واحدة معتمَدة أصلاً
   في `apps/web`؛ تحقَّق من ذلك أولاً قبل إضافة أي تبعية npm جديدة.
6. RBAC: المسار الجديد يتطلب صلاحية على الأقل بمستوى قراءة تقارير مالية
   (نفس صلاحية `trial-balance` الحالية كنقطة بداية) — لا يُفتح لكل مستخدم
   افتراضياً.

**Idempotency:** لا ينطبق — قراءة فقط، لا كتابة بيانات.

**Tests Required:**
- بيانات اختبار: شركة بها فواتير مرحَّلة على تواريخ مختلفة + حركات مخزون
  + دفعات → استجابة `BusinessPulseUseCase` تطابق الحساب اليدوي المتوقَّع
  لكل مؤشر من الخمسة على حدة.
- شركة (tenant) أخرى بنفس البيئة **يجب ألا تظهر بياناتها إطلاقاً** في
  استجابة الشركة الأولى (اختبار عزل tenant صريح — إلزامي، لا اختياري).
- فواتير غير مرحَّلة (مسودات) **لا تُحتسَب** ضمن مبيعات اليوم.
- مستخدم بلا صلاحية → 403، لا بيانات جزئية.

**Acceptance Criteria (Technical):**
- [ ] المؤشرات الخمسة فقط، بلا إضافة صامتة لأي مؤشر سادس
- [ ] لا استعلام SQL خام جديد خارج repositories القائمة
- [ ] اختبار عزل tenant يمر فعلياً (لا py_compile فقط)
- [ ] لا تعديل واحد داخل `modules/accounting|sales|inventory|payments`
- [ ] زمن استجابة Endpoint مقاس فعلياً على بيانات اختبار واقعية الحجم
      (لا حكم "سريع بما يكفي" بلا رقم)

**Acceptance Criteria (Product — حسب اختبار الملاءمة، القسم 10 من PRODUCT_VISION.md):**
- [ ] مستخدم تجريبي واحد على الأقل (محاسب/مالك فعلي أو من يمثّله داخلياً)
      يفتح اللوحة ويؤكد أن الأرقام الخمسة تُجيب فعلاً عن سؤال "أين نقف
      اليوم؟" بدون شرح إضافي مطلوب منه
- [ ] لا مؤشر واحد من الخمسة يحتاج تفسيراً بشرياً إضافياً ليكون مفهوماً
      (لو احتاج → إعادة تصميم لا توثيق إضافي)

**Definition of Done:** IMPLEMENTED + TESTED + VERIFIED. **لا ACCEPTED
حتى تأكيد بند "Product" أعلاه من طرف فعلي يستخدم النظام — ليس افتراضاً
داخلياً أن الأرقام "مفيدة".**

**Verification Commands:**
```bash
python3 -m py_compile apps/core-api/modules/reporting/application/use_cases/business_pulse_use_case.py
pytest apps/core-api/tests/integration/test_business_pulse_dashboard.py -v
```

**Rollback/Failure Considerations:** ملف/مسار جديد بالكامل، إضافة سطر
واحد فقط في راوتر قائم — فشل هذه المهمة لا يؤثر على أي تقرير أو وحدة
تعمل حالياً. يمكن حذف الملفات الجديدة والسطر المُضاف دون أي أثر جانبي.

**Explicitly NOT in scope for this task (تحذيراً من التوسّع الصامت):**
- لا مستوى 3 (WebSocket/تحديث لحظي عبر Event Bus) — يُفتح لاحقاً كمهمة
  منفصلة فقط عند حاجة مؤكَّدة (POS، لوحة مدير مباشرة)
- لا أي طبقة AI/تفسير/توصية على هذه البيانات — هذه المهمة عرض بيانات
  خام فقط، حسب الفصل الصريح بين الطبقتين في الدراسة السابقة
- لا Materialized View أو جدول تجميع مسبق — إلا بعد قياس بطء فعلي
- لا تصدير/طباعة/جدولة تقارير — نطاق منفصل تماماً

---

### اعتماديات هذه المهمة
- تعتمد على: لا شيء (نقطة بداية مستقلة)
- تُبنى عليها لاحقاً (مهام مستقبلية منفصلة، غير مفتوحة الآن):
  - TASK-BI-02: تحويل مستوى 1 إلى مستوى 2 (Polling) إن ثبتت الحاجة
  - TASK-INS-01: "طبقة رؤى داخلية" بالمعنى المُعاد تأطيره في الدراسة —
    **لا تبدأ إلا بعد ACCEPTED فعلي لهذه المهمة + إغلاق حلقة AI الحرجة
    الحالية للإنتاج، حسب القسم 9 من `PRODUCT_VISION.md`**

---

## سجل التنفيذ الفعلي (Implementation Log)

نُفِّذ فعلياً، لا وصفاً نظرياً — كل بند أدناه تحقَّق منه بتشغيل حقيقي
(pytest / tsc / eslint / vitest / next build)، لا py_compile فقط.

### الملفات المُنشأة/المعدَّلة فعلياً

| الملف | النوع |
|---|---|
| `apps/core-api/modules/reporting/application/dto/business_pulse_dto.py` | جديد |
| `apps/core-api/modules/reporting/application/use_cases/business_pulse_use_case.py` | جديد |
| `apps/core-api/modules/reporting/presentation/routes/reports_router.py` | تعديل: +1 endpoint فقط (`GET /reports/business-pulse`) |
| `apps/core-api/tests/integration/test_business_pulse_dashboard.py` | جديد — 6 اختبارات |
| `apps/web/src/features/reporting/api.ts` | جديد |
| `apps/web/src/features/reporting/business-pulse-dashboard.tsx` | جديد |
| `apps/web/src/app/(dashboard)/dashboard/page.tsx` | تعديل: تضمين المكوّن الجديد فقط |

**لا تعديل واحد** خارج هذه القائمة — وحدات `sales/accounting/inventory/payments`
والملف `main.py` لم تُلمَس إطلاقاً، كما تشترط بطاقة المهمة.

### قرارات تنفيذ اتُّخذت أثناء العمل (موثَّقة، لا مخفية)

هذه انحرافات صغيرة عن الطموح الأصلي، اكتُشفت أثناء القراءة الفعلية للكود
لا افتراضاً مسبقاً، وكل واحدة موثَّقة أيضاً كتعليق داخل الكود نفسه:

1. **لا مكتبة رسوم بيانية جديدة.** `package.json` لم يكن يحوي أي مكتبة
   رسوم بيانية أصلاً — بدل افتراض إضافة `recharts` أو ما شابه، رُسم خط
   المبيعات وأعمدة أعلى المنتجات بـ SVG يدوي بسيط داخل المكوّن نفسه. قرار
   اختيار مكتبة رسوم بيانية معتمَدة للمشروع يستحق نقاشاً منفصلاً، لا أن
   يُتَّخذ ضمنياً هنا.
2. **"الأقل مخزوناً" بدل "تحت حد إعادة الطلب".** حقل حد إعادة الطلب (reorder
   level) يعيش في وحدة `catalog`، وهي خارج نطاق الوحدات المرجعية المسموح
   قراءتها في هذه المهمة (`sales/accounting/inventory/payments` فقط). أدنى
   5 عناصر بالكمية الفعلية بديل صادق، لا اجتهاد باسم الحل الأصلي.
3. **أعلى المنتجات بلا اسم، فقط `product_id`.** لنفس سبب استبعاد `catalog`
   أعلاه. الواجهة تعرض ذلك صراحة للمستخدم (لا تخفيه)، مع ملاحظة أنه قيد
   مؤقت.
4. **أنواع TypeScript كُتبت يدوياً** في `features/reporting/api.ts` بدل
   الاعتماد على `packages/api-types` المُولَّد تلقائياً — لأن التوليد
   التلقائي (`npm run generate:api-types`) يحتاج خادماً حياً على
   `localhost:8000`، وهو غير متاح في بيئة التنفيذ. يجب إعادة توليدها
   بالأمر الحقيقي أول مرة يعمل فيها الخادم فعلياً.
5. **تجميع في الذاكرة، لا Materialized View** — طابق تماماً ما نصّت عليه
   بطاقة المهمة أصلاً (مستوى 1: استعلام مباشر عند الفتح). سقف داخلي
   `page_size=2000` مضروب كحد أمان مؤقت، موثَّق كقيد معروف يُعاد تقييمه في
   TASK-BI-02 المستقبلية غير المفتوحة الآن.

### أدلة التحقق الفعلي (لا افتراض)

```
# Backend
$ python3 -m py_compile <الملفات الثلاثة الجديدة/المعدَّلة>          ✅
$ pytest tests/integration/test_business_pulse_dashboard.py -v       ✅ 6 passed
$ pytest tests/integration/ -q  (كامل الحزمة، لا الملف الجديد فقط)   ✅ 172 passed, 6 skipped, 0 failed
$ python -c "from main import app; app.openapi()"                    ✅ /reports/business-pulse مسجَّل

# Frontend
$ npx tsc --noEmit                                                    ✅ 0 errors
$ npx eslint <الملفات الثلاثة الجديدة/المعدَّلة>                     ✅ 0 warnings/errors
$ npx vitest run                                                      ✅ 37 passed (كامل الحزمة القائمة، بلا كسر)
$ npx next build                                                      ✅ نجح — /dashboard: 4.43 kB
```

### ما تحقَّق من Acceptance Criteria (Technical) — كل البنود ✅

- [x] المؤشرات الخمسة فقط، بلا إضافة صامتة لمؤشر سادس
- [x] لا استعلام SQL خام جديد خارج repositories/use cases القائمة
      (`ListSalesInvoicesUseCase`, `ListBalancesUseCase`,
      `BankAccountRepository`, `PaymentRepository`, `ReceiptRepository`)
- [x] اختبار عزل tenant يمر فعلياً
      (`test_business_pulse_is_isolated_between_tenants`)
- [x] لا تعديل واحد داخل `modules/accounting|sales|inventory|payments`
- [ ] زمن استجابة مقاس على بيانات واقعية الحجم — **لم يُقَس بعد** (يحتاج
      بيانات اختبار بحجم إنتاجي حقيقي وبيئة Postgres فعلية، غير متاحة في
      بيئة تنفيذ هذه المهمة؛ SQLite في الذاكرة المُستخدَم في الاختبارات لا
      يمثّل زمن استجابة واقعياً)

### ما تبقّى قبل ACCEPTED (لا يمكن لتنفيذ آلي تحقيقه)

- [ ] **Product Acceptance بند 1 و2**: تأكيد مستخدم فعلي (محاسب/مالك أو من
      يمثّله داخلياً) أن الأرقام الخمسة تُجيب عن "أين نقف اليوم؟" بلا شرح
      إضافي — يتطلب تشغيل النظام كاملاً (Postgres حقيقي + خادم حي) وعرضه
      على شخص فعلي، خارج نطاق ما يمكن لهذا التنفيذ إثباته وحده.
- [ ] قياس زمن استجابة على حجم بيانات واقعي (أعلاه)
