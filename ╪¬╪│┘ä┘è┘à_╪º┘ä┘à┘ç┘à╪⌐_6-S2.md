## بطاقة تسليم المهمة

- رقم المهمة: #6-S2 — Migration Chain Fix
- الفرع المقترح: task/06-s2-migration-chain-fix
- الحالة: 🟠 Integration Blocking → **محلولة** لجزء #6-S2 نفسه؛ راجع قسم
  "مشاكل جديدة مكتشَفة" أدناه — 3 مشاكل إضافية غير محلولة، تحتاج قرارًا
  مركزيًا قبل أي محاولة `alembic upgrade head` حقيقية للمشروع كاملاً.

---

### ⚠️ الاكتشاف الأهم: الرأس الفعلي لم يكن `identity_20260808_0003`

`README_المهمة.md`، `DELIVERY_CARD_TASK_06.md` الأصلية، و**ADR-001 §القرار 5**
نفسه — ثلاثتها ذكرت أن الرأس الحالي الفعلي هو `identity_20260808_0003` وأن
migration outbox يجب أن تُبنى فوقه. بطاقة تسليم #6 الأصلية اقترحت أصلًا
`down_revision = wfna_20260808_0002` (فرع أقدم حتى).

طبّقتُ خطوة 1 من الإجراء الإلزامي حرفيًا ("تحقّق بنفسك أولاً، لا تفترض")
وشغّلت `alembic heads` + `ScriptDirectory.get_heads()` (طريقتان مستقلتان)
على قاعدة Postgres حقيقية بكامل سلسلة الـ 20 migration المرفَقة. النتيجة
في كلتا الطريقتين: **رأس واحد فقط، وهو `sales_20260809_0001`** —
migration أضيفت لاحقًا (2026-08-09) فوق `identity_20260808_0003` نفسها،
أثناء دمج الدفعة الثالثة، لسد نقص migration كان مفقودًا من مهمة #11
(idempotency_key على sales_invoices). أي أن التعليمات وADR-001 كانا
صحيحين وقت كتابتهما، لكن أصبحا غير محدَّثين بحلول وقت تنفيذ هذه المهمة.

**القرار المتخذ:** `platform_20260809_0002_outbox_events.py` مبنية فوق
`down_revision = "sales_20260809_0001"` (الرأس الفعلي المتحقَّق منه)، **وليس**
`identity_20260808_0003` كما ورد حرفيًا في التعليمات. هذا ليس مخالفة
للتعليمات بل تنفيذ دقيق لخطوة 1 منها — البناء فوق `identity_20260808_0003`
كان سينتج فرعًا منفصلًا (رأسين)، وهو بالضبط ما يمنعه معيار القبول. مفصَّل
بالكامل داخل تعليق رأس ملف الـ migration نفسه.

---

### الملفات التي تم إنشاؤها (جديدة)
- `apps/core-api/migrations/versions/platform_20260809_0002_outbox_events.py`
  — جدول `outbox_events` (event_name, payload JSONB, status, attempts,
  last_error, dispatched_at, next_attempt_at + id/created_at)، مطابق تمامًا
  لوصف `DELIVERY_CARD_TASK_06.md`. تعريف بنية فقط — لا Worker، لا منطق
  تسليم. فهرس مركّب إضافي `(status, next_attempt_at)` لدعم استعلام استطلاع
  worker مستقبلي (DDL بحت، ضمن حدود "تعريف فقط").

### الملفات التي تم تعديلها
- `apps/core-api/platform_core/outbox_models.py` — **تعديل التعليق التوثيقي
  فقط**، لا تغيير في أي حقل. النسخة المؤقتة السابقة كانت مطابقة أصلًا
  لوصف بطاقة التسليم؛ التعديل الوحيد إزالة إطار "مؤقت" وربطها بالـ
  migration الفعلية الجديدة.

### لم يُعدَّل أي ملف آخر
لا لمس لأي migration موجودة مسبقًا، ولا لـ `event_bus.py`، ولا لأي من
`modules/sales`, `modules/accounting`, `modules/workflow`, `apps/ai-platform` —
تم التحقق يدويًا (diff) قبل التسليم.

---

### معيار القبول — التحقق الفعلي (Postgres 16 حقيقي، ليس SQLite)
- [x] `alembic heads` → نتيجة واحدة فقط (`platform_20260809_0002`) — تحقَّق
  بطريقتين مستقلتين (CLI + `ScriptDirectory` API)
- [x] `alembic upgrade head` من Fresh DB ينجح بلا خطأ حتى الرأس الجديد —
  **راجع ملاحظة منهجية التحقق أدناه**
- [x] `alembic upgrade` من نسخة سكيمة سابقة فعلية (`sales_20260809_0001`،
  الرأس الفعلي الصحيح — وليس `identity_20260808_0003` المذكور في
  التعليمات، لنفس سبب التصحيح أعلاه) خطوة واحدة إضافية ينجح بلا خطأ
- [x] `alembic downgrade -1` من الرأس الجديد يعمل بلا خطأ (الجدول
  والفهارس تُحذف نظيفًا، الرأس يعود لـ `sales_20260809_0001`)
- [x] لا تعديل على أي migration موجودة مسبقًا (تحقَّق يدويًا، diff نظيف)

**ملاحظة منهجية مهمة حول تحقق Fresh DB:** أثناء تشغيل السلسلة الكاملة
الأصلية (بلا أي تعديل) ضد Postgres حقيقي، فشلت في **3 مواضع سابقة على
migration هذه المهمة تمامًا**، بأخطاء حقيقية غير نظرية (راجع القسم التالي).
بما أن التعليمات تمنع تعديل أي migration موجودة مسبقًا "إطلاقًا"، تحقَّق Fresh
DB الكامل نُفِّذ على **نسخة منفصلة مؤقتة (scratch) خارج التسليم تمامًا** —
تعديلات الالتفاف الثلاثة طُبِّقت فقط على تلك النسخة المؤقتة (لم تُدمَج، لن
تُسلَّم)، فقط لإثبات أن migration outbox نفسها + بقية السلسلة سليمة معماريًا
من حيث DDL. **نسخة التسليم الفعلية (هذا المستند) لم تُعدَّل بأي من هذه
الالتفافات** — الملفات المُسلَّمة أعلاه فقط.

---

### 🔴 مشاكل جديدة مكتشَفة بالتشغيل الفعلي (غير محلولة عمداً — تُرفَع كتعارض)

اكتُشفت أثناء تنفيذ خطوات 5-7 من الإجراء الإلزامي (تشغيل فعلي ضد Postgres
حقيقي، لأول مرة في تاريخ هذا المستودع على ما يبدو — كل اختبارات integration
الحالية تعمل على SQLite في-الذاكرة التي لا تفرض قيود VARCHAR ولا تفرّق بين
دلالات asyncpg/psycopg2، فلا تكشف أيًا من هذه المشاكل). **لم أُصلح أيًا
منها في نسخة التسليم** — كلها خارج حدود migrations الخاصة بهذه المهمة
تحديدًا (مهام سابقة: #1، #6 الأصلي، عمل المحاسبة/الضرائب، إلخ)، والتعليمات
صريحة: "لا تُعدِّل أي migration موجودة سابقًا إطلاقًا". تُرفَع هنا كتعارض
🟠 Integration Blocking حقيقي (نفس تصنيف مشكلة `conftest.py` في ADR-002
§القرار ب) يمنع أي `alembic upgrade head` فعلي للمشروع كاملاً على Postgres
حقيقي، بغض النظر عن outbox:

**1. `sa.func.now()` داخل `op.bulk_insert()` يفشل تحت asyncpg (7 ملفات)**
`op.bulk_insert` يستخدم `executemany`، وasyncpg لا يقبل SQL clause
(`sa.func.now()`) كقيمة معامل مربوطة ضمن دفعة — الخطأ الفعلي:
`expected a datetime.date or datetime.datetime instance, got 'now'`.
**مثبَت أنه معروف مسبقًا:** `platform_20260806_0005_seed_ai_gateway_permissions.py`
(2026-08-06) يحتوي تعليقًا صريحًا يوثّق هذا الخطأ بالضبط والحل (قيمة
`datetime.now(UTC)` محسوبة مرة واحدة بدل `sa.func.now()`) — لكن الإصلاح لم
يُطبَّق رجعيًا على الملفات الأخرى. الملفات المتأثرة (غير المُصلَحة):
`identity_20260804_0002_seed_base_permissions.py`،
`docs_integr_20260807_0002_seed_permissions.py`،
`inventory_sales_pos_20260805_0009_seed_permissions.py`،
`partners_catalog_20260804_0005_seed_permissions.py`،
`purchasing_20260806_0004_seed_permissions.py`،
`wfna_20260808_0002_seed_permissions.py`.

**2. إنشاء ENUM مزدوج في migration اثنتين (accounting، taxation)**
`accounting_20260804_0003_initial_schema.py` و
`taxation_20260804_0004_initial_schema.py` كلاهما يستدعي
`ENUM.create(bind, checkfirst=True)` صراحة، ثم يستخدم نفس الـ ENUM كنوع
عمود في `op.create_table` بدون `create_type=False` — SQLAlchemy يحاول
إنشاء النوع مرة ثانية ضمنيًا عند إنشاء الجدول. الخطأ الفعلي:
`DuplicateObjectError: type "account_type" already exists` (وبالمثل
`e_invoice_submission_status`).

**3. معرّف revision أطول من عمود `alembic_version.version_num` الافتراضي**
`inventory_sales_pos_20260805_0009` (34 حرفًا) يتجاوز العرض الافتراضي
لعمود Alembic الداخلي (`VARCHAR(32)`). الخطأ الفعلي:
`StringDataRightTruncationError: value too long for type character varying(32)`.
غير مكتشَف سابقًا لأن SQLite لا يفرض طول VARCHAR فعليًا. الحل المحتمل خارج
حدودي (يحتاج تعديل `env.py` — `version_table` config — أو تقصير أسماء
الـ revisions المستقبلية لتبقى ≤32 حرفًا؛ كلاهما قرار يخص مالكي الملفات
لا حزمة Migration Chain Fix).

**التوصية:** فتح مهمة "🟠 Real Postgres Migration Validation" منفصلة (يبدو
أنها بالضبط `Real Environment Validation` المذكورة في جدول "ترتيب التنفيذ
المعتمَد" بنهاية ADR-001) لإصلاح هذه الثلاث فعليًا مع موافقة مالكي كل ملف.

---

### ⚠️ ملاحظة بيئة التحقق (ليست جزءًا من التسليم)
حزمة هذه المهمة تضمّنت `migrations/`, `alembic.ini`, `outbox_models.py`
والوثائق فقط — بدون كود التطبيق الكامل الذي يستورده `env.py`
(`modules.identity.infrastructure.models`, `modules.tenancy.infrastructure.models`,
`platform_core.config`, `shared_kernel.db_base`). لتشغيل التحقق الفعلي محليًا،
أُنشئت نسخ stub بديلة (فارغة، توفّر فقط ما يلزم لحل `import`) — هذه الـ
stubs **محلية لبيئة التحقق فقط ولم تُدرَج ضمن التسليم**؛ المستودع الفعلي
لدى الفريق يملك بالتأكيد هذه الوحدات كاملة.

### الاعتماديات
- تعتمد على: لا شيء تقنيًا (كما ذُكر أصلًا في بطاقة #6)
- تُبنى عليها: أي عمل مستقبلي على outbox (تنفيذ #6 الفعلي: Worker + دمج
  نقاط النشر الثلاث المذكورة في بطاقة #6 الأصلية) يجب أن يبني فوق
  `platform_20260809_0002`، وليس فوق الوصف القديم في بطاقة #6.
