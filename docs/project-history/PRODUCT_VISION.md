# PRODUCT_VISION.md — القائم (AlQaim)

> **الحالة:** ✅ معتمَد (Product Lead) — 2026-08-12، بموجب `ADR-003-vertical-slice-strategy.md §4-5`
> **الدور:** مرجع أعلى من أي بطاقة مهمة. بطاقة المهمة تسأل "هل أنجز
> المطوّر ما طُلب؟". هذه الوثيقة تسأل "هل ما أنجزه يخدم المنتج الذي نبنيه؟".
> تتغيّر نادراً وبقرار منتج صريح فقط — ليست وثيقة تقنية، ولا تُعدَّل كجزء
> من تسليم مهمة عادية.

---

## 1. ما هو AlQaim؟

**نظام ERP ومحاسبة تجاري مغلق المصدر للسوق المحلي**، يغطي دورة عمل
تجارية كاملة: المبيعات، المشتريات، المخزون، العملاء والموردون، المحاسبة
والقيود والفترات المحاسبية، الضرائب، التقارير، والصلاحيات — مبني على
Core محاسبي/مخزني موثوق (Modular Monolith)، مع طبقة ذكاء اصطناعي عميقة
مدمجة كـ Subsystem مستقل (`AI Platform`) وليس كوظيفة هامشية.

**الوصف بجملة واحدة:**
> ERP تجاري موثوق، والـAI يُستخدَم فيه فقط حين يقدّم قيمة عملية واضحة
> للمستخدم — لا لأن الذكاء الاصطناعي موجود.

---

## 2. من هو المستخدم؟

- **الشركات الصغيرة والمتوسطة** في السوق المحلي التي تحتاج نظام ERP
  متكامل بدل مجموعة أدوات منفصلة (فواتير + إكسل + برنامج مخزون منفصل).
- **أدوار داخل كل شركة عميلة:** محاسب (يراجع ويعتمد)، مسؤول مبيعات/شراء
  (يُنشئ المستندات)، مسؤول مخزون، أمين صندوق نقطة بيع، مدير/مالك (يوافق
  ويراقب التقارير).
- **ليس** مستخدماً نهائياً يتفاعل مباشرة مع "مساعد ذكاء اصطناعي عام" —
  الـAI يخدم المستخدمين أعلاه ضمن عمليات محددة، لا كواجهة مستقلة قائمة
  بذاتها في هذه المرحلة.

---

## 3. ما المشكلة التي يحلها؟

1. تشتت أدوات إدارة الأعمال الصغيرة/المتوسطة بين إكسل وبرامج فواتير
   منفصلة وأنظمة مخزون منفصلة، بلا اتساق محاسبي حقيقي بينها.
2. الإدخال اليدوي للفواتير الورقية/الممسوحة ضوئياً — وقت وأخطاء بشرية
   متكررة (بند تحل له طبقة الـAI تحديداً، راجع القسم 6).
3. غياب رقابة موثوقة على الفترات المحاسبية والصلاحيات في الأدوات
   الشعبية الرخيصة أو المبنية يدوياً (إكسل).
4. **تحويل مستندات غير مهيكلة إلى بيانات أعمال قابلة للاستخدام.** هذه
   ليست مشكلة OCR بحد ذاتها — القيمة ليست في "قراءة" الصورة، بل في:

   ```
   صورة / PDF / فاتورة ورقية
              ↓
         بيانات منظمة
              ↓
         مسودة عملية داخل ERP قابلة للمراجعة والاعتماد
   ```

   هذا هو جوهر تميّز AI في القائم تحديداً (راجع الحد الفاصل الرسمي بين
   AI وERP في القسم 6).

---

## 4. ما هي الوظائف الأساسية؟

هذه **ثوابت المشروع** — لا تتغيّر بتغيّر الأولويات التقنية أو الدفعات:

- **نظام محاسبي متكامل:** قيود، فترات محاسبية، ضرائب، تقارير مالية.
- **مخزون:** منتجات، مستودعات، حركات، تحويلات، تسويات — محكوم بـ
  Business Rules حقيقية، وليس بالـAI.
- **مبيعات ومشتريات:** عروض أسعار، أوامر بيع/شراء، فواتير، إشعارات
  دائنة، ترحيل محاسبي حقيقي (قيد مدين/دائن متوازن، ليس تسجيلاً سطحياً).
- **مدفوعات:** سندات قبض/صرف، حسابات بنكية.
- **نقطة بيع (POS):** جلسات، سلة بيع، دفع — **Offline-first كقرار منتج**،
  ليس فقط ميزة POS تقنية. هذا يعني أن النظام يجب أن يبقى قابلاً للعمل
  دون اتصال، ويُعيد المزامنة بسلامة عند عودة الاتصال. **لا يُطلَب بناء
  ذلك بالكامل الآن** — لكن أي مهمة مستقبلية تلمس POS يجب أن تتعامل مع
  هذا كهدف منتج ثابت لا بند تقني اختياري، وتحتاج معالجة صريحة لاحقاً
  لأسئلة: Sync، Conflict Resolution، Idempotency، Local Queue، Event
  Ordering (تفاصيل التنفيذ في الوثائق التقنية عند فتح تلك المهمة، لا هنا).
- **صلاحيات وإدارة عمليات:** RBAC كامل، وتصميم **Tenant-aware** مع عزل
  صارم لبيانات كل شركة منذ الأساس — هذا وصف لتصميم النظام حالياً، وليس
  ادعاءً بتشغيل SaaS متعدد المستأجرين على نطاق واسع اليوم (راجع القسم 8
  للفرق بين الاثنين).

**قاعدة صارمة غير قابلة للتفاوض:**
> **الذكاء الاصطناعي لا يصبح هو النظام المحاسبي.** AI يقترح/يحلل/يكتشف/
> يفسّر. **ERP Accounting Engine هو صاحب القرار والتنفيذ دائماً.**

---

## 5. ما الذي يميّز AlQaim؟

- **ERP هو الأساس، والـAI عنصر التميّز** — ليس العكس:

  ```
                   ALQAIM
                      │
         ┌────────────┴────────────┐
         │                         │
         ▼                         ▼
    ERP PLATFORM             AI PLATFORM
         │                         │
   Accounting                  Document AI
   Inventory                   Matching
   Sales                       Analysis
   Purchases                   Recommendations (مستقبلاً)
   Reports                     Assistant (مستقبلاً)
  ```

- **AI Platform مستقلة كمنتج فرعي**، متصلة بالـERP عبر عقد واضح فقط
  (بغض النظر عن الآلية التقنية الدقيقة خلف هذا العقد، والتي قد تتطور مع
  الوقت — التفاصيل الحالية موثَّقة في الوثائق التقنية المعمارية) — هذا
  الاستقلال يسمح بنشر Local أو Cloud أو Hybrid دون إعادة كتابة النظام:

  ```
  نسخة Local                     نسخة Cloud
  Customer Server                AlQaim Cloud
  ├── AlQaim                     ├── ERP
  └── AlQaim AI                  ├── Database
                                  ├── Storage
                                  └── AI Platform
  ```

- **قابل للتوسّع دون إعادة كتابة النظام:** `AlQaim Core` يحتفظ بكل
  Business Rules؛ الواجهات (Mobile/Web/Desktop/Integrations/Reporting/
  خدمات مستقبلية) تُبنى حوله دون المساس بجوهره.

---

## 6. ما دور AI؟

**الرؤية الحالية المعتمَدة لمسار المستندات (الأولوية الفعلية الآن):**

```
فاتورة صورة
 ↓
OCR
 ↓
Document Understanding
 ↓
Extraction
 ↓
Matching (Supplier/Product)
 ↓
Validation
 ↓
Invoice Draft
 ↓
Human Review (محاسب)
 ↓
Accounting / ERP (بعد الاعتماد فقط)
```

**الحلقة الحرجة الواجب إثباتها أولاً قبل أي توسّع** (راجع القسم 9):

> **صورة فاتورة → AI → Purchasing → مراجعة محاسب → عملية ERP حقيقية**

### الحد الفاصل الرسمي بين AI وERP: `Invoice Draft`

`Invoice Draft` (وحدث `InvoiceDraftReady` الذي يحمله) هو **الحد الفاصل
الرسمي** بين مسؤولية AI Platform ومسؤولية AlQaim ERP — وليس مجرد نقطة
تقنية عابرة:

```
┌──────────────────────────┐
│       AI PLATFORM        │
│                          │
│ OCR                      │
│ Understanding            │
│ Extraction               │
│ Matching                 │
│ Validation               │
└────────────┬─────────────┘
             │
             ▼
      Invoice Draft
             │
═════════════╪══════════════   ← الحد الفاصل الرسمي
             │
             ▼
┌──────────────────────────┐
│        ALQAIM ERP        │
│                          │
│ Purchasing               │
│ Inventory                │
│ Accounting               │
│ Business Rules           │
└──────────────────────────┘
```

هذا الحد يمنع خلط مسؤوليات AI مع مسؤوليات ERP لاحقاً: AI لا يقرر ولا
ينفّذ عملية تجارية أبداً — ينتج مسودة فقط وتنتهي مسؤوليته عندها. **مبدأ
حاكم دائم:**

> **AI يقترح، ERP يقرر.**

### "مراجعة المحاسب" ليست مجرد شاشة عرض

عندما نقول `AI → Invoice Draft → Human Review`، المقصود ليس عرض بيانات
مستخرَجة للمحاسب فقط، بل تمكينه فعلياً من مراجعة **مصدر الثقة** في كل
قيمة (درجة الثقة، ما تم التعرف عليه وما لم يُتعرَّف عليه، ما يحتاج
مراجعة يدوية)، ثم اتخاذ قرار صريح: **اعتماد / تعديل / رفض**. هذا مبدأ
منتج يجب أن يبقى حاضراً في أي مهمة تصميم لشاشة المراجعة — **تفاصيل
التنفيذ الدقيقة (الحقول، الشاشات) مكانها Blueprint/AI Specification، لا
هذه الوثيقة.**

**رؤية مستقبلية (بعد إثبات الحلقة أعلاه فعلياً، لا قبلها):**

```
ERP Data → AI Analysis → Anomaly Detection → Forecasting →
Recommendations → AI Assistant
```

**القيد الحاكم دائماً (Blueprint §17):** لا ترحيل تلقائي لأي مخرج AI
دون موافقة بشرية صريحة.

---

## 7. ما الذي لا يدخل ضمن نطاق المشروع؟ (راجع القسم 9 للتفصيل الكامل)

- ليس مشروعاً لإثبات قدرات Microservices — Modular Monolith هو الاختيار
  المتعمَّد لـ core-api، ولن يتحول إلى عشرات الخدمات الصغيرة "لمجرد
  اتباع معمارية حديثة".
- ليس مشروعاً بحثياً في OCR أو LLM.
- ليس تطبيق مساعد ذكاء اصطناعي عام قائماً بذاته.

---

## 8. الرؤية المستقبلية

بعد إثبات الحلقة الحرجة في القسم 6 على بيئة حقيقية، وبدون تغيير جوهر
المنتج:

- **AI:** قراءة مستندات أوسع، مطابقة أدق، كشف حالات غير طبيعية، تنبؤ،
  مساعد ذكي — **كل واحدة تُفتح كمهمة منتج مستقلة بعد إثبات قيمتها**، لا
  دفعة واحدة.
- **Integrations:** أجهزة POS، تطبيق موبايل مستقل (إن ظهرت حاجة منتج
  فعلية — راجع قرار #16)، API خارجي، خدمات دفع وشحن، أنظمة أخرى.
- **Business Intelligence:** Dashboards، تقارير متقدمة، مؤشرات أداء.
- **Cloud:** الانتقال من "تصميم Tenant-aware" (القسم 4، موجود من الأساس)
  إلى **تشغيل Multi-tenant Cloud فعلي على نطاق واسع** (SaaS)، مع Cloud
  Sync وRemote Access وAI مركزية — هذه خطوة منتج مستقبلية منفصلة، وليست
  حالة قائمة اليوم.

---

## 9. Product Non-Goals — ما الذي لن يحاول "القائم" أن يكونه

هذا القسم يحمي المشروع من الانحراف (Scope Creep) عبر 20+ مهمة وأعضاء
متعددين. أي مهمة أو قرار تقني يتعارض مع أي بند هنا **يُرفَض تلقائياً**،
بغض النظر عن جاذبيته التقنية:

- **ليس** مشروعاً لإثبات قدرات Microservices.
- **ليس** مشروعاً لإثبات قدرات LLM أو استعراض تقني للذكاء الاصطناعي.
- **ليس** مشروعاً بحثياً في OCR.
- **ليس** هدفه استخدام AI في كل وظيفة ممكنة لمجرد وجود AI — كل ميزة AI
  جديدة يجب أن تجيب أولاً: *ما القيمة التجارية الواضحة لمستخدم حقيقي؟*
- **ليس** الهدف بناء أكبر Architecture ممكنة — معيار الجودة المعماري هو
  "هل يخدم بناء منتج يعمل فعلياً؟"، لا "هل يتبع أحدث نمط معماري؟".
- **ليس** مشروعاً يوسّع الذكاء الاصطناعي (AI Assistant/Forecasting/RAG/
  Recommendations/Anomaly Detection) قبل إثبات الحلقة الحرجة الأولى
  (القسم 6) تعمل فعلياً من طرف لطرف على بيئة حقيقية.

> **ملاحظة نطاق:** هذه الوثيقة رؤية منتج، لا وثيقة معمارية. أي قرار
> تقني حالي (Modular Monolith، آلية الاتصال بين Core وAI عبر
> API/Events، إلخ) موثَّق بتفصيله الكامل في الوثائق التقنية المعمارية
> (`AlQaim_V2_Blueprint.md`, `docs/architecture/`) لا هنا — إن تغيّرت
> الآلية التقنية مستقبلاً مع بقاء جوهر المنتج كما هو، **لا يجب أن
> تحتاج هذه الوثيقة أي تعديل**.

**الهدف الحاكم النهائي:**
> **بناء منتج ERP تجاري حقيقي يعمل. الـAI يُستخدَم فقط عندما يقدّم قيمة
> عملية واضحة للمستخدم.**

---

## 10. اختبار سريع لأي قرار جديد (Product Litmus Test)

من الآن، أي قرار منتج أو ميزة جديدة مقترحة — بغض النظر عن جاذبيتها
التقنية — تُختبَر أولاً بهذه الأسئلة الخمسة قبل دخولها الـ Roadmap:

1. **هل يخدم ERP التجاري؟**
2. **هل يقدّم قيمة حقيقية لمستخدم فعلي؟**
3. **هل يحافظ على مبدأ "AI يقترح، ERP يقرر"؟**
4. **هل نحتاج هذه الميزة الآن فعلاً، أم أنها Scope Creep؟**
5. **هل يمكن إثباتها عبر Vertical Slice حقيقي (وليس تصميماً نظرياً فقط)؟**

**إن فشل القرار في أي من هذه الأسئلة، لا يدخل الـ Roadmap لمجرد أنه
تقنياً مثير للاهتمام.**

### الأثر على طريقة إدارة كل مهمة

من الآن، دورة حياة أي مهمة تتبع هذا النمط بدل النمط التقني البحت
السابق:

```
النمط القديم:                    النمط الجديد:
Task                              Task
 ↓                                 ↓
Implementation                    Technical Acceptance
 ↓                                 +
Tests                             Product Acceptance
 ↓                                 ↓
Done                              Vertical Slice
                                   ↓
                                  Real User Value
                                   ↓
                                  Done
```

هذا يعني أن بطاقة كل مهمة من الآن تحتوي دائماً **قسمين منفصلين
إلزاميين**: Technical Acceptance Criteria (هل يعمل الكود؟) وProduct
Acceptance Criteria (هل يحقق القيمة التجارية المقصودة؟) — تطبيق مباشر
لهذه الوثيقة على مستوى بطاقة المهمة الواحدة.
