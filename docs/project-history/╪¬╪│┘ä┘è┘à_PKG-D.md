# تسليم PKG-D — Quality & Documentation Closure (2026-08-16)

**الحالة العامة:** PKG-D1 مُغلَقة بلا تنفيذ (غير ذي موضوع). PKG-D2 مُنفَّذة
كتابةً بالكامل (الشاشات الخمس + CI)، **غير مُتحقَّق منها بالتشغيل الفعلي بعد**.

راجع `STATUS_20_TASKS.md §21` للنسخة الرسمية المُدمَجة في السجل التاريخي؛
هذا الملف تفصيل تكميلي.

---

## PKG-D1 — `AlQaim_V2_closing_sprint_plan.md`

**لم يُنفَّذ، وهذا مقصود.** `00_TASK_PACKAGE.md` استندت في وصف مشكلتها
حرفياً إلى `PROJECT_STATUS_SUMMARY.md §6/§7` (كما ذكرت في سطر "المرجع"
الخاص بها) — لكن تلك الفقرة نفسها كانت **قديمة وغير محدَّثة**: تصف فجوة
أُغلِقت فعلياً من قبل في `STATUS_20_TASKS.md §16` (2026-08-13)، بتحديث
إحالات `README.md` الأربع لتشير إلى `ALQAIM_V2_MASTER_EXECUTION_PLAN.md`
الموجود فعلياً في جذر المستودع، لا بإعادة بناء الملف المفقود.

**تحقَّقت من هذا مباشرة على شجرة المشروع الفعلية بين يديّ الآن (لا
افتراضاً)**:
- `grep` على `README.md` الفعلي: لا توجد أي إحالة لـ`AlQaim_V2_closing_sprint_plan.md`.
- الفقرة المعنية في `README.md` تتضمن أصلاً ملاحظة تاريخية صريحة تشرح
  بالضبط هذا الانتقال.
- لا يوجد `AlQaim_V2_closing_sprint_plan.md` في جذر المشروع، ولا حاجة له.

**الإجراء المتخذ:** حدَّثتُ `PROJECT_STATUS_SUMMARY.md §6/§7` (مصدر
المشكلة الأصلي) ليعكس الواقع الحالي، بدل ترك فقرة قديمة تُنتج مهام وهمية
مستقبلاً لأعضاء آخرين. لم أنشئ `AlQaim_V2_closing_sprint_plan.md`.

---

## PKG-D2 — اختبارات Flutter

### الملفات المُدمَجة في `apps/pos-app/test/`
| الملف | يغطي |
|---|---|
| `login_screen_test.dart` | عرض الشاشة، قبول الإدخال، نجاح/فشل `AuthRepository.login()` |
| `open_session_screen_test.dart` | تحميل المستودعات، تفعيل/تعطيل زر الإجراء، حقل الرصيد الافتتاحي، حالة فارغة/خطأ |
| `close_session_sheet_test.dart` | فتح الورقة، رفض إدخال غير صالح محلياً، قبول إدخال صحيح |
| `sale_screen_test.dart` | سلة فارغة/بها بند، شارة "بانتظار المزامنة"، تفعيل/تعطيل زر الدفع |
| `payment_screen_test.dart` | ملخّص الفاتورة، تحديث الإجمالي عند الخصم، فشل الحفظ، **ومسار النجاح الكامل** |

كلها عبر `ProviderScope.overrides` بمزيّفات fake لـ
`AuthRepository`/`SessionStorage`/`WarehousesRemoteDataSource`/`PosRepository`/
`CartController` — **بلا أي تعديل على `apps/pos-app/lib/`**، وبلا شبكة أو
قاعدة drift حقيقية في أي اختبار.

**تحقُّق إضافي أُجري عند الدمج في هذه الشجرة تحديداً:** قارنت (`diff`) كل
ملف `lib/` الذي استندت إليه الاختبارات عند كتابتها بنسخته الفعلية هنا —
**9 ملفات، تطابق تام بايتاً لبايت**، فلا حاجة لأي تعديل على الاختبارات
عند الدمج.

### `.github/workflows/ci.yml`
job جديد `pos-app-baseline` (`dart analyze` + `flutter test`، تقرير فقط
`continue-on-error: true`) — إضافة صرفة بلا تعديل على `python-baseline`
(`TASK-CI-01`) الموجود، بلا تعارض ملفات مع `PKG-C4`.

### ⚠️ ما لم يتحقق فعلياً (فجوة صريحة، لا افتراض)
لا تتوفر بيئة Flutter SDK في أي مرحلة من هذا العمل. **لم يُشغَّل `dart
analyze` ولا `flutter test` فعلياً على أي من الملفات الخمسة، ولا على
job الـCI الجديد، حتى لحظة كتابة هذا السطر.** أول تشغيل حقيقي لـ
`pos-app-baseline` على GitHub Actions بعد دفع هذا التسليم هو **التحقق
الفعلي الأول** — ومن المتوقع منطقياً حاجة تعديلات طفيفة (خاصة سلوك
`find.widgetWithText` مع `InputDecoration.labelText`، وعدد تطابقات نص
مبلغ متكرر في `sale_screen_test.dart`).

### السلوك الموثَّق صراحة كقابل للمراجعة لاحقاً
اختبار "زر الدفع يبقى معطَّلاً بلا عميل" في `sale_screen_test.dart` يوثّق
السلوك **الحالي الفعلي** (لا عميل walk-in افتراضي في الكود بعد). إن
أضافت `PKG-B3` هذا المفهوم لاحقاً، هذا الاختبار تحديداً سيحتاج مراجعة —
موثَّق داخل الملف نفسه أيضاً.

---

## ملخّص التغييرات في هذه الحزمة
1. `apps/pos-app/test/{login,open_session,close_session_sheet,sale,payment}_*_test.dart` — جديد.
2. `.github/workflows/ci.yml` — إضافة `pos-app-baseline` (لا تعديل على الموجود).
3. `STATUS_20_TASKS.md` — قسم 21 جديد مُلحَق.
4. `PROJECT_STATUS_SUMMARY.md §6/§7` — تصحيح فقرتين قديمتين (لا حذف تاريخي، تحديث بعلامة "[محدَّث]").
5. **لم يُنشأ** `AlQaim_V2_closing_sprint_plan.md` — عمداً، راجع PKG-D1 أعلاه.
6. `apps/pos-app/lib/` — **لم يُمَس إطلاقاً** (ملتزم بحدود الملكية).
