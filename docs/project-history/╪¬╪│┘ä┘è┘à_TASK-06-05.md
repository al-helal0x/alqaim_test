# تسليم TASK-06-05 — إغلاق فجوة `test_outbox_pattern.py` (GATE-01)

**المصدر:** `ALQAIM_V2_PACKAGE_MERGE_PLAN_REVISED.md §3.4/§5.2` —
البند الوحيد الموصوف صراحةً بأنه "مهمة قصيرة ومحدودة... حجم العمل صغير"
بين كل الانحرافات المكتشَفة، ولا يمس أي مسار حرج (Walk-in Customer،
AI→Purchasing). اختير لهذا السبب تحديداً: **الأقل مخاطرة والأسهل** ضمن
الخطة الكاملة.

## لماذا هذا البند تحديداً (وليس غيره)

| البند المرشَّح في الخطة | المخاطرة | القرار |
|---|---|---|
| `TASK-06-05` (هذا البند) | 🟢 إضافة اختبارات فقط، صفر لمس لكود إنتاجي | ✅ نُفِّذ |
| قرار backoff formula (§3.2) | 🟡 قرار سياسة، ليس عملاً تقنياً — لا "تنفيذ" حقيقي ممكن هنا | مؤجَّل، يحتاج صاحب قرار |
| `redis_bridge.py` → `outbox.enqueue_event()` (§3.3) | 🔴 يمس مسار AI→Purchasing المستقبلي، قرار معماري مفتوح غير محسوم | مؤجَّل عمداً |

## ماذا وُجِد فعلياً عند الفحص (قبل كتابة أي كود)

**مفاجأة إيجابية:** إصلاح `MAX_ATTEMPTS=8 → status="failed"` (الانحراف
3.1 في الخطة) **كان موجوداً بالفعل** في `outbox_worker.py` ضمن هذه
الحزمة — موثَّق بتعليق `TASK-06-02` داخل الملف نفسه. **لم يكن هناك أي
كود إنتاجي يحتاج تعديلاً.** الفجوة الوحيدة المتبقية فعلياً كانت غياب
`tests/integration/test_outbox_pattern.py` نفسه — الملف لم يكن موجوداً.

## ما نُفِّذ

ملف واحد جديد: `apps/core-api/tests/integration/test_outbox_pattern.py`
— 6 اختبارات تغطي السيناريوهات الأربعة الإلزامية من
`ALQAIM_V2_MASTER_EXECUTION_PLAN.md §13` حرفياً:

| # | السيناريو (من الخطة الرئيسية) | الاختبار |
|---|---|---|
| 1 | Transaction succeeds → Event exists | `test_transaction_succeeds_event_exists` |
| 2 | Transaction fails → Event does not exist | `test_transaction_fails_event_does_not_exist` |
| 3 | Worker fails → Event remains retryable | `test_worker_failure_reschedules_row_as_retryable` + `test_worker_failure_exhausted_attempts_marks_row_failed` (يغطي حد `MAX_ATTEMPTS` تحديداً — الفجوة الأصلية) |
| 4 | Event delivered twice → Consumer remains safe (at-least-once) | `test_worker_never_redelivers_a_dispatched_row` + `test_duplicate_delivery_requires_idempotent_consumer` |

**قرار تصميم الاختبار:** `_dispatch_one(session, row)` اختِير كنقطة
الاختبار (لا `_poll_once()`/`_run_loop()`) لأنها الدالة الوحيدة التي تأخذ
`session` كوسيط صريح، وبالتالي قابلة للحقن الكامل ببيئة SQLite في
الذاكرة. `_poll_once()` يفتح `AsyncSessionLocal` الإنتاجي (Postgres)
داخلياً بلا حقن — خارج نطاق هذه المهمة الصغيرة تعديل ذلك (سيتطلب Dependency
Injection لجلسة الـ Worker نفسه، تغيير أكبر لم تطلبه الخطة هنا).

## التحقق الفعلي (لا افتراض)

```
pytest tests/integration/test_outbox_pattern.py -v   → 6 passed
pytest tests/integration/ -v                          → 106 passed, 4 skipped (لا صفر تراجع)
py_compile outbox.py outbox_worker.py
            test_outbox_pattern.py main.py            → نجح على الجميع
```

## حالة GATE-01 بعد هذا التسليم

| معيار GATE-01 (`ALQAIM_V2_MASTER_EXECUTION_PLAN.md §11`) | قبل | بعد |
|---|---|---|
| `py_compile` على outbox.py/outbox_worker.py/main.py | ✅ | ✅ (بلا تغيير) |
| `pytest tests/integration/test_outbox_pattern.py -v` | ❌ الملف غير موجود | ✅ **6 passed** |
| rollback حقيقي (فشل بعد enqueue وقبل commit) | ⚠️ غير مُختبَر بملف مخصَّص | ✅ مُختبَر مباشرة |
| retry (attempts+1، pending حتى max_attempts) | ❌ لا `max_attempts` فعلياً | ✅ الكود كان صحيحاً فعلاً؛ الآن **مُختبَر** أيضاً بحدّه الأقصى تحديداً |

**الحكم المحدَّث:** `GATE-01` بات **مغلَقاً فعلياً** بكل معايير الخطة
الرئيسية الحرفية. لا يبقى من الخطة الأصلية سوى قرارين مفتوحين لا علاقة
لهما بهذه المهمة: (أ) توحيد رقم backoff formula بين الكود والوثيقة، (ب)
قرار `redis_bridge.py`/`outbox.enqueue_event()` — كلاهما يحتاج صاحب قرار
صريح، وليسا "عملاً" يمكن تنفيذه من طرف واحد.

## لم يُلمَس (عمداً، خارج نطاق TASK-06-05 تحديداً)

- `platform_core/outbox.py`, `outbox_worker.py`, `event_bus.py`, `redis_bridge.py` — صفر تعديل
- Walk-in Customer، AI→Purchasing — لا اعتماد تقني بينهما وبين هذه المهمة

---

## ملحق — TASK-CLEANUP-01: حذف `main.py.orig` (بند منفصل، نُفِّذ لاحقاً بنفس الجلسة)

**المصدر:** موصوف كـ 🟢 "فوري بالتوازي" في `قرار_استعادة_العميل_النقدي.md §7`
— أبسط بند مفتوح في كل الوثائق، صفر مخاطرة.

**التحقق قبل الحذف (لا حذف بلا تأكد):**
- `diff main.py.orig main.py` أظهر أن الملف نسخة **ما قبل Task #6-S1**
  (بلا `wire_event_subscriptions`، بلا Application-scoped EventBus) —
  بقايا من أداة الدمج، وليس ملفاً بديلاً معتمداً.
- `grep -rl "main.py.orig"` عبر المشروع بالكامل (`.py`, `.md`, `.toml`,
  `.cfg`, `.ini`, `Dockerfile*`) لم يُرجِع أي مرجع فعلي إليه (السطر الوحيد
  الذي يذكره كان تسليم هذه المهمة نفسه، كملاحظة "لم يُلمَس" قبل الحذف).

**الإجراء:** `rm apps/core-api/main.py.orig`.

**التحقق بعد الحذف:**
```
py_compile main.py                → نجح
pytest tests/integration/ -q      → 106 passed, 4 skipped (بلا تغيير عن ما قبل الحذف)
```

---

## ملحق ٢ — TASK-CLEANUP-02: تحذير أعلى `DELIVERY_CARD_TASK_06.md` (بند منفصل)

**المصدر:** موصوف صراحة في `ALQAIM_V2_PACKAGE_MERGE_PLAN_REVISED.md §3
(جدول B) و§5 (جدول الملفات)`: *"يصف تصميماً قديماً متروكاً يناقض
README_المهمة.md... يُوصى بحذفه من أي حزمة مستقبلية لتفادي تضليل لاحق"*.

**القرار المتبع هنا:** لا حذف — الملف موجود أصلاً داخل مجلد مؤرشَف
(`_PENDING_TASK06_NOT_MERGED/`، خارج الشجرة الحية، لا يستورده أي كود ولا
يُشغَّل). بدلاً من الحذف (الذي يُفقِد قيمة تعليمية حقيقية: يوثّق مشاكل
Decimal/JSON وترتيب commit/publish اكتُشفت أثناء البناء الأول)، **أُضيف
تحذير صريح أعلى الملف** يوضّح أن التصميم الموصوف فيه (`event_bus.py`
بتوقيع `publish(session,...)`) **مرفوض رسمياً** (قرار `MERGE-06-03`)
ويوجّه القارئ لمصدر الحقيقة الفعلي (`outbox.py`/`outbox_worker.py` في
الشجرة الحية). نفس المنطق المتبع في الملف الرئيسي للخطة نفسها (احتفظ
بالنص الأصلي كاملاً كملحق تاريخي بدل حذفه).

**الملف المعدَّل:** `_PENDING_TASK06_NOT_MERGED/DELIVERY_CARD_TASK_06.md`
(توثيقي بحت — لا كود، لا تأثير على أي اختبار أو تشغيل).

---

## ملحق ٣ — TASK-CI-01: `ci.yml` أساسي (report-only)

**المصدر:** `ALQAIM_V2_MASTER_EXECUTION_PLAN.md §8 Phase 4 / §12` —
مصنَّفة صراحة `SAFE PARALLEL`، بلا أي Dependency، ولا تعدّل منطقاً تجارياً.
اختيرت كـ"جزء بسيط آخر" لأنها الوحيدة المتبقية بلا حاجة قرار من أحد
(بخلاف backoff formula/`redis_bridge.py`) وبلا حاجة لمس أي كود تطبيقي.

**ما كان ناقصاً فعلياً (تحقَّقت منه قبل الكتابة):** لا يوجد `.github/`
في المشروع إطلاقاً — `infra/ci/README.md` يشير لـ"السكربتات الفعلية في
`.github/workflows/`" لكن المجلد نفسه لم يكن موجوداً.

**الملف الجديد:** `.github/workflows/ci.yml`

**النطاق:** مصفوفة (`matrix`) على تطبيقَي Python الوحيدين في المستودع —
`core-api` و`ai-platform` (كلاهما `pyproject.toml` مع `[dev]` extras
جاهزة أصلاً: pytest, ruff). `pos-app` (Flutter) وweb/desktop (Node)
خارج نطاق `TASK-CI-01` كما وردت — CI منفصل لهما لاحقاً إن احتاج المشروع.

**"تقرير فقط، لا فشل PR" (حرفياً من الخطة):** `continue-on-error: true`
صريح على خطوتَي `ruff check` و`pytest` — الـjob لا يفشل أبداً بغض النظر
عن نتيجة الأدوات، فلا يحجب أي PR. **لا تفعيل لـ`TASK-CI-05`** (الإلزام
الفعلي على مخالفات جديدة) — تلك مقفولة خلف `GATE-03` صراحة في الخطة، لم
تُلمَس هنا.

**⚠️ فخ اكتُشف أثناء التحقق (لا افتراضاً):** `on:` بلا اقتباس في YAML
يُفسَّر بمعيار YAML 1.1 كـBoolean (`true`) لا كمفتاح نصي — تحقّقت بتشغيل
`yaml.safe_load()` فعلياً محلياً، وأكّد أن `on:` غير المقتبَس يتحوّل فعلاً
لمفتاح `true` في الشجرة المُحلَّلة. GitHub Actions نفسه يتسامح مع هذا
تاريخياً، لكن **صُحِّح إلى `"on":`** (مقتبَس صراحة) لإزالة الغموض كلياً —
ليس أسلوباً تجميلياً، بل فخ فعلي مُثبَت.

**التحقق الفعلي (محاكاة خطوات الـCI محلياً لكلا التطبيقين):**
```
apps/core-api:      pip install -e ".[dev]" → ruff check . (78 مخالفة، تقرير فقط)
                     → pytest -v → 106 passed, 4 skipped
apps/ai-platform:    pip install -e ".[dev]" → ruff check . (3 مخالفات، تقرير فقط)
                     → pytest -v → 18 passed, 1 skipped
python3 -c "yaml.safe_load(...)"  → بنية الملف صحيحة، "on" يُقرَأ كمفتاح نصي صحيح
```

**لم يُنفَّذ ضمن هذا الملحق (خارج نطاق `TASK-CI-01` تحديداً):**
`TASK-CI-02` (توثيق فشل الاختبارات الحالية رسمياً كـBaseline)،
`TASK-CI-03` (توثيق الـ78 مخالفة/3 مخالفات كـ`known_failures.txt` رسمياً)
— كلاهما "بعد `CI-01`" حسب ترتيب الخطة، مهام منفصلة لاحقة.

---

## ملحق ٤ — TASK-CI-02 + TASK-CI-03: Test Baseline + `known_failures.txt`

**المصدر:** `ALQAIM_V2_MASTER_EXECUTION_PLAN.md §12` — كلاهما `SAFE
PARALLEL بعد CI-01`. نُفِّذا معاً هنا (كلاهما توثيق بحت لنتيجة تشغيل
مباشر، بلا أي كود تطبيقي مُعدَّل).

**TASK-CI-02** → ملف جديد `TEST_BASELINE.md` (جذر المشروع). **النتيجة
الفعلية المفاجئة:** لا يوجد أي اختبار فاشل موروث في أي من التطبيقين
(core-api: 106 passed/4 skipped؛ ai-platform: 18 passed/1 skipped؛ صفر
failed في كليهما) — الـ4+1 اختبارات المتخطّاة موثَّقة بأسمائها الكاملة
وسبب تخطيها (تحتاج Redis/`ai-platform` حقيقيَّين، غير متاحين في CI
البسيط الحالي). الأثر العملي: عند تفعيل `TASK-CI-05` مستقبلاً، **لا حاجة
لفصل inherited failures عن جديدة على مستوى pytest تحديداً** — الفصل
اللازم فعلياً هو على مستوى `ruff` فقط.

**TASK-CI-03** → ملفا `apps/core-api/known_failures.txt` (78 سطراً) و
`apps/ai-platform/known_failures.txt` (3 أسطر)، كل منهما بترويسة توثيقية
كاملة (منهج التوليد + تصنيف حسب القاعدة).

**⚠️ اكتشاف رقمي أثناء التنفيذ (لا افتراضاً):** الخطة/`ADR-002` تذكران
"278 مخالفة ruff موروثة" — **العدد الفعلي المُتحقَّق منه بتشغيل مباشر الآن
هو 78 (core-api) + 3 (ai-platform) = 81، وليس 278.** رقم 278 على الأرجح
من مرحلة أقدم قبل تنظيفات لاحقة (مهمة #17، حذف `apps/mobile`، إلخ) لم
تُحدَّث في الوثائق. وُثِّق هذا التصحيح صراحة داخل الترويسة نفسها بدل نسخ
الرقم القديم دون تحقق.

**التحقق:** ملفا توثيق فقط — لا تعديل كود، لا تأثير على أي اختبار.
أعيد تشغيل `pytest` على التطبيقين قبل الكتابة للتأكد من الأرقام الحقيقية
لحظة التوثيق (وليس نسخ أرقام سابقة من ملحق CI-01 أعلاه بلا إعادة تحقق).
