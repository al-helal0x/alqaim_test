# قرار مطلوب — `TASK-AI-02b`: حركة مخزون لفواتير الشراء عبر AI

**الحالة:** ⏳ بانتظار قرار (لم يُحسَم هنا عمداً) | **نوع الوثيقة:** تفكيك
قرار، لا قرار

> هذه الوثيقة **لا تقرر** — تُفكِّك القرارين المعلَّقين (المذكورين في
> `STATUS_20_TASKS.md §19.2` و`تسليم_TASK-AI-02_verification_and_gap.md`)
> إلى خيارات محدَّدة مبنية على فحص فعلي للكود القائم، ليصبح بإمكان صاحب
> القرار الاختيار مباشرة بدل البحث من الصفر. **صفر كود جديد في هذا
> التسليم** — فرض إجابة من طرف واحد هنا يخالف نفس المبدأ المتَّبع حرفياً
> في كل تسليم سابق لهذا المشروع.

## لماذا هذه بالذات هي العقبة الحرجة الحالية
`PROJECT_STATUS_SUMMARY.md §7` وَ`STATUS_20_TASKS.md §19` يصنّفانها 🔴
صراحة: فاتورة شراء عبر AI تُرحَّل محاسبياً بنجاح تام (مُختبَر فعلياً)، لكن
**لا حركة مخزون تحدث إطلاقاً** — لا خطأ ظاهر، لا استثناء، فقط غياب صامت
لأثر تجاري حقيقي (بالضبط نمط 🟣 Product Behavior Regression الموثَّق في
`GOVERNANCE-product-behavior-regression.md`، وإن لم يكن Refactor بل فجوة
تكامل جديدة). لا يمكن إغلاق `GATE-03` (E2E الكامل) وهذه الفجوة مفتوحة.

---

## القرار الأول — متى تُضاف حركة المخزون؟

### الخيارات المطروحة

| الخيار | الوصف | الحجة له | الحجة ضده |
|---|---|---|---|
| **أ) عند الترحيل** (نفس لحظة `PostPurchaseInvoiceUseCase`) | تُستدعى `increase_stock` داخل نفس معاملة الترحيل الحالية | يطابق دلالة "فاتورة AI = لا يوجد أمر شراء منفصل، فالترحيل هو أقرب لحظة لحدث \"استلام فعلي\"" | **يكسر التناظر مع المسار العادي**: في كل مسار آخر بالنظام، المخزون يزيد عند "الاستلام"، لا عند "الترحيل المحاسبي" — هذان مفهومان منفصلان عمداً في التصميم الحالي (راجع `ReceivePurchaseOrderUseCase` مقابل `PostPurchaseInvoiceUseCase`، لا تداخل بينهما إطلاقاً في أي مسار قائم) |
| **ب) عند إنشاء الفاتورة من المسودة** (`CreatePurchaseInvoiceFromAiDraftUseCase`) | الأقرب زمنياً لحظة اعتماد مسودة AI بشرياً | — | **الفاتورة تبقى `draft` في هذه اللحظة** — زيادة المخزون قبل الترحيل المحاسبي تعني مخزوناً "موجوداً" لصنف له فاتورة غير مُلزِمة محاسبياً بعد؛ يفتح احتمال عدم اتساق (فاتورة تُلغى لاحقاً وقد نُسي التراجع عن حركة المخزون) |
| **ج) خطوة "استلام" جديدة صريحة لفواتير AI** (تُحاكي `ReceivePurchaseOrderUseCase` لكن بلا أمر شراء) | **يحافظ على نفس المفهوم القائم فعلياً في كل مكان آخر بالنظام** — "الاستلام" فعل منفصل عمداً عن "الترحيل"، بقرار بشري صريح | يضيف خطوة/Endpoint جديد بدل إعادة استخدام الموجود؛ يحتاج تصميم حالة وسيطة جديدة للفاتورة (شيء بين draft وposted، أو حقل `received_at` منفصل) | تعقيد تنفيذي أعلى من (أ) قليلاً |

### الحقيقة الحاسمة من فحص الكود القائم (لا افتراض)
`ReceivePurchaseOrderUseCase.execute` يستقبل `warehouse_id` **كمعامل من
المستدعي مباشرة (الـrouter/HTTP request) في كل مرة** — **لا يُخزَّن على
أمر الشراء نفسه إطلاقاً**. هذا يعني أن مفهوم "الاستلام" في هذا النظام هو
عمداً **فعل بشري لحظي منفصل يحدد فيه المستلم المستودع وقتها**، وليس بيانات
مشتقة تلقائياً من الفاتورة/الأمر. الخيار (ج) هو الوحيد المتّسق فعلياً مع
هذا التصميم القائم؛ (أ) و(ب) كلاهما يخالفانه ضمنياً.

---

## القرار الثاني — من أي `warehouse_id`؟

### الحقيقة الحاسمة (مؤكَّدة من الكود، لا افتراض)
- `extracted_payload` القادم من `ai-platform` (`ai_dto.py`) **لا يحتوي أي
  حقل مستودع إطلاقاً** — الـOCR/فهم المستندات لا يستخرج هذه المعلومة من
  الفاتورة الورقية أصلاً (منطقي: الفاتورة الورقية من المورد لا تذكر مستودع
  الشركة المستقبِلة).
- الـEndpoint الحالي لـ`TASK-AI-01` (`build_purchase_invoice_from_ai_draft.py`)
  يطلب `branch_id` فقط، لا `warehouse_id` — والفرق بينهما مهم: قد يحتوي
  الفرع الواحد أكثر من مستودع.

### الخيارات المطروحة

| الخيار | الوصف | ملاحظة |
|---|---|---|
| **أ) حقل جديد إلزامي في الـEndpoint** (`warehouse_id` صريح عند تحويل المسودة أو عند "الاستلام" — يعتمد على قرار #1) | يطابق تماماً نمط `ReceivePurchaseOrderUseCase` القائم (معامل صريح من المستدعي، لا استنتاج) | أبسط وأكثر اتساقاً مع الكود القائم — **لا تغيير مفاهيمي جديد مطلوب** |
| **ب) استنتاج تلقائي** (مستودع افتراضي للفرع/الشركة) | أقل احتكاكاً لمستخدم الواجهة | يحتاج جدول/إعداد "مستودع افتراضي" **غير موجود في أي مكان بالنظام حالياً** — بناء بنية جديدة كاملة لحل مشكلة واحدة، تعقيد غير متناسب |
| **ج) اشتقاق من مسودة AI نفسها مستقبلاً** (تحسين OCR لاستخراج اسم المستودع من نص حر، إن وُجد) | — | يعتمد على قدرة `ai-platform` الفعلية، خارج نطاق `core-api` بالكامل، ليس قراراً يمكن حسمه من طرف هذه الوحدة |

**الأقرب اتساقاً مع الكود القائم (نفس منطق القرار الأول تماماً):** الخيار
(أ) — معامل صريح، مطابق حرفياً لنمط `ReceivePurchaseOrderUseCase` الموجود
فعلياً، بلا أي بنية جديدة.

---

## توصية غير مُلزِمة (للتسريع فقط — القرار يبقى لصاحبه)
بناءً على الاتساق الكامل مع الكود القائم فعلياً (لا اجتهاد جديد، إعادة
استخدام نفس المفهوم الموجود حرفياً في `ReceivePurchaseOrderUseCase`):
**القرار الأول → (ج)**، **القرار الثاني → (أ)**. هذا يعني عملياً: إضافة
خطوة "استلام" صريحة لفاتورة AI (مشابهة لـ`ReceivePurchaseOrderUseCase`
لكن بلا `purchase_order_id`)، تستقبل `warehouse_id` كمعامل صريح من طالب
الاستلام — **صفر مفاهيم جديدة، فقط تمديد نمط قائم فعلياً**.

هذه توصية مبنية على الاتساق المعماري فقط، وليست بديلاً عن قرار المنتج
الفعلي (هل يُقبَل عملياً أن يبقى الاستلام خطوة يدوية منفصلة حتى لفواتير
AI؟) — **يبقى بحاجة موافقة صريحة من صاحب القرار** قبل أي تنفيذ.

---

## بعد الموافقة — حجم العمل المتوقَّع (تقدير فقط، غير مُلزِم)
إن اعتُمدت التوصية أعلاه، العمل التالي مماثل تماماً لحجم أي جزء من
`TASK-AI-01` (استخدام مكوّن موجود، لا بناء بنية جديدة):
1. Endpoint جديد صغير (`POST /purchase-invoices/{id}/ai-receive` أو مشابه) يستقبل `warehouse_id`
2. استدعاء `IInventoryPort.increase_stock` مباشرة لكل بند (نفس نداء `ReceivePurchaseOrderUseCase` حرفياً، بلا تعديل على `IInventoryPort` نفسه)
3. اختبار تكامل واحد يثبت أن الفجوة المُثبَتة في `test_ai_invoice_posting_and_inventory_gap.py` أُغلقت فعلياً (تحديث الاختبار #2 هناك من "يثبت الفجوة" إلى "يثبت الإصلاح")

## ما لم يُنفَّذ في هذا التسليم (بالتصميم)
صفر كود، صفر migration، صفر تعديل على أي ملف إنتاجي أو اختبار قائم — هذا
تسليم تفكيك قرار فقط، تماماً كما يقتضيه وجود قرارين معلَّقين غير محسومين.
