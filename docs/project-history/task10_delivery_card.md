## بطاقة تسليم المهمة

- رقم المهمة: #10
- المسار: 📊 محاسبة
- الفرع: task/10-fiscal-period-closing-entry
- مبني على commit: (نسخة `AlQaim_V2_integration_round2.zip` المرفقة)

### الملفات/المجلدات التي تم إنشاؤها (جديدة)
- apps/core-api/tests/integration/test_period_closing.py — 6 اختبارات تكامل تغطي: قيد الإقفال المتوازن، منع الترحيل بعد الإقفال، منع الإقفال المزدوج، فترة بلا نشاط، غياب حساب الأرباح المرحّلة، وتوافق تقرير الميزانية مع الرصيد الفعلي بعد الإقفال.

### الملفات/المجلدات التي تم تعديلها
- apps/core-api/modules/accounting/application/use_cases/fiscal_period_use_cases.py — `ClosePeriodUseCase` أصبح يبني ويرحّل **قيد إقفال فعلي** (وليس فقط `is_closed=True`): يصفّر أرصدة حسابات الإيرادات/المصروفات ضمن مدى الفترة وينقل صافي الربح/الخسارة إلى حساب "الأرباح المرحّلة" (كود 3200)، مع تحقق توازن صارم (`assert_balanced`) قبل أي كتابة. أُضيف `FiscalPeriodAlreadyClosedError` (منع إقفال مزدوج) و`RetainedEarningsAccountMissingError`. `event_bus` أصبح مُحقَناً (Dependency Injection) بدل استيراد الـ Singleton العام مباشرة — نفس نمط `CreatePaymentUseCase` — لتفادي تلوّث حالة الاختبارات بين الملفات.
- apps/core-api/modules/accounting/presentation/routes/fiscal_periods_router.py — حقن `SqlNumberingService` و`event_bus` الحقيقي عند بناء `ClosePeriodUseCase`، ومعالجة `FiscalPeriodAlreadyClosedError`/`RetainedEarningsAccountMissingError` بردّ 422.
- apps/core-api/modules/reporting/application/use_cases/financial_statements_use_case.py — تحديث التوثيق فقط ليعكس أن "الأرباح المرحّلة" أصبحت تُقرَأ فعلياً من رصيد الحساب 3200 بعد قيود الإقفال (لا تغيير في منطق الحساب: كان صحيحاً رياضياً مسبقاً بمجرد وجود قيود الإقفال الفعلية، لأن قيود الإقفال تُصفّر حسابات الإيرادات/المصروفات ضمن المدى الزمني نفسه تلقائياً).
- apps/core-api/tests/integration/test_accounting_posting.py — تحديث استدعاء `ClosePeriodUseCase` ليمرر `numbering_service` و`EventBus()` محلي معزول (كسر توافقي بسبب تغيير توقيع الدالة أعلاه).

### Migrations (إن وجدت)
- لا يوجد (الجداول والحسابات المطلوبة — `journal_entries`/`journal_entry_lines`/حساب 3200 — موجودة أصلاً من migration المحاسبة السابقة `accounting_20260804_0003_initial_schema.py` وشجرة الحسابات الافتراضية).

### الاختبارات المضافة
- apps/core-api/tests/integration/test_period_closing.py — انظر أعلاه (6 اختبارات).

### التوثيق المحدَّث
- Docstrings داخل fiscal_period_use_cases.py و financial_statements_use_case.py توضّح آلية الإقفال الجديدة وأثرها. لا تعديل على contracts.md (لا عقد جديد بين وحدات — الإقفال داخلي بالكامل ضمن accounting/reporting).

### اعتماديات هذه المهمة
- تعتمد على: لا شيء
- تُبنى عليها: لا شيء مباشرة ضمن جدول الـ20 مهمة (مهمة مستقلة الحدود)

### التحقق المحلي قبل التسليم
- [x] تشغيل الاختبارات محلياً وناجحة (13/13 لاختبارات المحاسبة/التقارير ذات الصلة، و73/79 على كامل مجموعة core-api — الفشل الوحيد المتبقي (6 اختبارات في test_sales_and_pos.py) هو خلل بنيوي **موجود مسبقاً في الكود الأصلي غير المعدَّل** (تلوّث `event_bus` Singleton عند تشغيل كل الاختبارات معاً بسبب استيراد main.py من ملف اختبار آخر) وتم التحقق منه بتشغيل نفس المجموعة على النسخة الأصلية قبل أي تعديل فأعطت نفس الفشل بالضبط — خارج نطاق هذه المهمة).
- [x] لا مخالفات ruff جديدة في الملفات المعدَّلة (كل ما تبقّى من تحذيرات ruff هو نمط متكرر موجود أصلاً في كل الراوترات `Depends(...)` وسطر `date.today()` الأصلي غير المُلمَس — من اختصاص مهمة #4).
- [x] لا تعديل خارج المسارات المصرَّح بها في القسم 4.2 (وحدة accounting + تقرير reporting داخل apps/core-api فقط، إضافةً لتعديل طفيف على ملفَي اختبار قائمَين مباشرة تأثرا بتغيير التوقيع).
- [x] كل بند في عمود "المخرج" الخاص بالمهمة محقَّق فعلياً: (1) قيد إقفال فعلي يُرحِّل صافي الربح إلى الأرباح المرحّلة ✅ (2) منع أي قيد جديد على فترة مُقفلة ✅ (كان موجوداً، تم التحقق باختبار إضافي) (3) استبدال الاعتماد على سطر "صافي الربح المؤقت" بقراءة فعلية من الأرباح المرحّلة في تقرير الميزانية ✅ (4) اختبار تكامل ✅.

---

### ملاحظة فنية مهمة (يُفضَّل نقلها لمهمة #18 — تحديث contracts.md، أو لمن يراجع مهمة #4)
اكتُشفت أثناء هذه المهمة مشكلة بنيوية عامة: `platform_core.event_bus.event_bus` Singleton عملية واحدة، ويُشترَك فيه بمجرد استيراد `main.py` (الذي يسجّل مشتركين حقيقيين يتصلون بـ Postgres عند أول استيراد). أي اختبار تكامل يستدعي `event_bus.publish(...)` مباشرة (بدل حقن `EventBus` محلي) سيفشل بفشل اتصال Postgres إن تشغّل بعد أي اختبار آخر يستورد main.py ضمن نفس عملية pytest. تم حلّ هذا محلياً في هذه المهمة عبر حقن `EventBus` في `ClosePeriodUseCase` (نفس نمط `CreatePaymentUseCase` الموجود مسبقاً)، لكن ملفات اختبار أخرى (`test_audit.py` مثلاً) قد لا تزال عرضة لنفس الخطر إن تغيّر ترتيب التشغيل مستقبلاً — يستحق تذكرة متابعة مستقلة، وليس ضمن حدود مهمة #10.
