# تسليم مهمة #17 — حذف المجلدات الفارغة + تحديث README الجذري

## بطاقة تسليم المهمة
- رقم المهمة: #17
- المسار: 🧹 تنظيف
- الاعتماديات: لا شيء

### الملفات/المجلدات التي تم حذفها
تحقَّقتُ من كل مسار عبر فحص محتويات الحزمة المرفَقة (`مهمة_17_تنظيف_وREADME.zip`)
سطرًا سطرًا: لا يوجد داخل أي منها أي ملف فعلي (`find . -type f` لا يُرجع شيئًا
سوى ملفات الـ README الخاصة بالحزمة نفسها). الستة مسارات كلها بأسماء أقواس
حرفية `{...}` غير قابلة للاستخدام كمسار حقيقي أصلًا:

- `apps/core-api/tests/{unit,integration,e2e}`
- `apps/core-api/modules/identity/application/{use_cases,dto,ports}`
- `apps/core-api/modules/identity/domain/{entities,value_objects,rules}`
- `apps/core-api/modules/tenancy/application/{use_cases,dto,ports}`
- `apps/core-api/modules/tenancy/domain/{entities,value_objects,rules}`
- `packages/i18n/{ar,en}`

**⚠️ تنبيه تنفيذي مهم (كما حذّرت حزمة المهمة نفسها):** الحذف أعلاه تم التحقق
منه على **نسخة هذه الحزمة فقط**، وليس على `main` الفعلي. قبل تنفيذ:
```bash
git rm -r "apps/core-api/tests/{unit,integration,e2e}" \
          "apps/core-api/modules/identity/application/{use_cases,dto,ports}" \
          "apps/core-api/modules/identity/domain/{entities,value_objects,rules}" \
          "apps/core-api/modules/tenancy/application/{use_cases,dto,ports}" \
          "apps/core-api/modules/tenancy/domain/{entities,value_objects,rules}" \
          "packages/i18n/{ar,en}"
git commit -m "chore: حذف مجلدات الأقواس الحرفية الفارغة — مهمة #17"
```
أعد التشغيل أولًا: `find . -iname "*{*}*" -type d` على آخر سحب من `main`،
لأن مهمتَي #13 و#15 (قيد التسليم بالتوازي) قد أضافتا ملفات ضمن
`apps/core-api/modules/identity/` و`.../tenancy/` **الحقيقية** (بدون أقواس) —
لا علاقة لها بهذه المسارات الوهمية، لكن التحقق الحرفي قبل الحذف الفعلي يبقى
ضروريًا كما نصّت حزمة المهمة.

**لم أحذف `apps/mobile`** — خارج حدود هذه المهمة (خاص بمهمة #16 المنجزة
بشكل منفصل، وقد نُفِّذ حذفها بالفعل هناك).

### الملفات التي تم تعديلها
- `README.md` (الجذر) — إعادة كتابة كاملة لِـ:
  1. استبدال إطار "المرحلة 0 — التأسيس" (لم يعد يعكس الواقع؛ المشروع الآن في
     جولة الإغلاق بدورة تجارية كاملة تعمل فعليًا) بإشارة صريحة لحالة المشروع
     الفعلية ولوثيقة `AlQaim_V2_closing_sprint_plan.md` كأول مرجع يُقرأ.
  2. تصحيح شجرة `apps/` بحذف `apps/mobile` (محذوف فعليًا بقرار #16) وإضافة
     توضيح أن `packages/ui-kit` غير قابل للاستخدام من Flutter (مطابقة لقرار
     مهمة #13 الموثَّق في `apps/pos-app/STATUS.md` و`sync_manager.dart`).
  3. **تصحيح "خريطة الفريق":** الجدول الأصلي يتوقف عند العضو 8 ولا يذكر مالكًا
     لـ `apps/pos-app` رغم وجوده في الشجرة. أضفت **العضو 9 — Mobile/POS Client
     Lead** استنادًا لِـ `apps/pos-app/STATUS.md` (يُسمّي نفسه صراحة "العضو 9")
     و`apps/pos-app/lib/core/sync/sync_manager.dart` (يشير لـ"العضو 7" بصيغة
     تفصل بينه وبين عضو pos-app). بقية الأرقام (1، 4، 7، 8) تحققت مطابقتها مع
     إشارات فعلية عبر حزم التسليم الأخرى (`sales_use_cases` ملك العضو 4 حسب
     `pos_sync_dto.dart`؛ إشعار ترحيل ui-kit من العضو 7 حسب `تسليم_المهمة_15.md`
     و`sync_manager.dart`) فأُبقيت كما هي.
  4. إزالة سطر "لا يوجد هنا منطق أعمال حقيقي بعد" (لم يعد صحيحًا).

### Migrations
لا يوجد

### الاختبارات المضافة
لا يوجد (مهمة توثيق/تنظيف بحتة)

### التوثيق المحدَّث
`README.md` (الجذر)

### اعتماديات هذه المهمة
- تعتمد على: لا شيء
- تُبنى عليها: لا شيء مباشرة، لكنها تحقق أحد بنود "تعريف الانتهاء" العام
  (القسم 3 من `AlQaim_V2_closing_sprint_plan.md`): "لا مجلدات مكرَّرة أو
  فارغة متبقية" و"`README.md` يعكس الحالة الفعلية للمشروع"

### التحقق المحلي قبل التسليم
- [x] لا تعديل خارج المسارات المصرَّح بها (المجلدات الفارغة الستة + `README.md`)
- [x] كل مجلد مُدرَج للحذف تحقَّقتُ يدويًا أنه خالٍ من أي ملف داخل الحزمة المرفَقة
- [x] لم أمس `apps/mobile` (خارج الحدود، مهمة #16 منفصلة)
- [ ] **تحقق لازم من طرف من يُطبِّق الحذف فعليًا على `main`:** إعادة تشغيل
      `find . -iname "*{*}*" -type d` على آخر سحب حقيقي قبل تنفيذ `git rm`، لأن
      هذا خارج قدرتي هنا (لا وصول لمستودع `main` الفعلي، فقط لحزمة الملفات
      المرفَقة في هذه المحادثة)
