# تسليم مهمة #11 — أمان معاملة إنشاء→ترحيل فاتورة البيع

## ملخص التغييرات

### 1) idempotency على مستوى الطلب (الإنشاء)
- **`infrastructure/models/sales_models.py`**: عمود جديد `idempotency_key`
  (نص، اختياري) على `SalesInvoice` + `UniqueConstraint(company_id, idempotency_key)`.
  NULL لا يتعارض مع NULL آخر (سلوك UNIQUE القياسي)، فالفواتير القديمة/بلا
  مفتاح غير متأثرة إطلاقاً.
- **`application/dto/sales_dto.py`**: حقل `idempotency_key` اختياري في
  `SalesInvoiceCreateRequest` وفي `SalesInvoiceResponse`.
- **`application/use_cases/sales_use_cases.py` → `CreateSalesInvoiceUseCase`**:
  - قبل أي عمل: لو أُرسل مفتاح وسبق استخدامه لنفس الشركة، تُعاد نفس الفاتورة
    المُنشأة أول مرة فوراً (بلا تحقق/ترقيم/إنشاء جديد).
  - **حماية من السباق الحقيقي**: لو وصل طلبان بنفس المفتاح شبه-متزامنين
    ونجح كلاهما تجاوز الفحص أعلاه، فقيد UNIQUE في القاعدة يمنع الإدراج
    المزدوج فعلياً؛ عند `IntegrityError` أثناء الـ commit، يتراجع الكود
    (`rollback`) ويعيد نفس الفاتورة الفائزة بالسباق بدل كسر الطلب الخاسر.
- **`presentation/routes/invoices_router.py`**: يقبل أيضاً ترويسة
  `Idempotency-Key` (لها الأولوية على حقل الـ body بنفس الاسم إن وُجدت
  الاثنان معاً) — نمط شائع (Stripe-style) لا يفرض تعديل شكل الـ body.

### 2) "نجح الإنشاء وفشل الترحيل" — إعادة محاولة بدل ازدواج
- **`PostSalesInvoiceUseCase.execute`**:
  - **إصلاح فجوة كانت موجودة**: فشل `accounting_port.record_document_posting`
    (المثال الموثَّق أصلاً أعلى الملف: فترة مالية مقفلة) كان يترك حجوزات
    المخزون (من المرحلة 1) معلَّقة بلا تحرير حتى تنتهي صلاحيتها تلقائياً
    (15 دقيقة). الآن تُحرَّر فوراً (`release_reservation`) قبل رفع الاستثناء
    من جديد، والفاتورة نفسها تبقى `draft` دون أي تغيير أو تكرار — أي إعادة
    استدعاء `/post` بنفس `invoice_id` (فوراً، بلا انتظار) تعمل بأمان من
    جديد على نفس الفاتورة الأصلية.
  - **idempotent عند إعادة إرسال طلب الترحيل نفسه**: لو كانت الفاتورة
    `posted` فعلاً (نجح الترحيل السابق لكن ردّه ضاع على العميل — timeout
    مثلاً)، تُعاد نفس الفاتورة فوراً بلا أي إعادة تنفيذ لأي أثر جانبي (لا حجز،
    لا محاسبة، لا خصم) — كان هذا سابقاً يرمي `InvoiceNotDraftError` (422)
    وهو تعامل خاطئ مع طلب retry شرعي.
- **`presentation/routes/invoices_router.py`**: أُزيلت وسيطة رابعة
  (`WorkflowPortAdapter`) والتعامل مع `InvoiceApprovalPendingError`/
  `InvoiceApprovalRejectedError` من مسار `/post` لأنها **غير موجودة فعلياً**
  في `sales_use_cases.py` المُسلَّم (راجع القسم 3 أدناه) وكانت تكسر استيراد
  الملف بالكامل.

### 3) ملاحظة مهمة: تعارض توثيقي موجود مسبقاً (خارج نطاق #11)
لاحظتُ أن `presentation/routes/invoices_router.py` و`infrastructure/event_handlers.py`
و`sales/README.md` تفترض جميعها وجود تكامل موافقة المدير (مهمة #12:
`WorkflowPortAdapter`، `InvoiceApprovalPendingError`/`RejectedError`،
`StartManagerApprovalForSalesInvoiceUseCase`، معامل `workflow_port` في
`PostSalesInvoiceUseCase`) — لكن هذه الرموز **غير موجودة** في
`sales_use_cases.py` كما سُلِّم لهذه الحزمة (وهو، بحسب `README_المهمة.md`
لهذه الحزمة، النسخة الأساسية *قبل* تعديلات #6/#12 عمداً). هذا كان يجعل
`invoices_router.py` يفشل عند الاستيراد فعلياً (ImportError).
لم أُنفِّذ تكامل #12 بنفسي (تخمين تفاصيل `IWorkflowPort` الفعلية خارج نطاق
حزمتي وخطر) — بدلاً من ذلك أعدتُ `invoices_router.py` ليطابق النسخة
الأساسية المُسلَّمة فعلياً (استيراد قابل للعمل)، وتركت تعليقاً واضحاً في
كلا الملفين يشير إلى أن إعادة الربط تحدث عند حسم تعارض #6×#12 على
`sales_use_cases.py` — تماماً كما حذّر `README_المهمة.md` لهذه الحزمة.
**يُرجى إبلاغ مسؤول الدمج بهذه الملاحظة صراحة.**

## الاختبار
`apps/core-api/tests/integration/test_sales_invoice_idempotent_posting.py`
— 5 اختبارات pytest-asyncio، شغّلتُها فعلياً محلياً (SQLite في-الذاكرة +
AsyncMock للـ Ports الخارجية) وتنجح كلها:
1. نفس `idempotency_key` مرتين → فاتورة واحدة فقط.
2. بلا `idempotency_key` → لا تكرار تلقائي (سلوك سابق محفوظ).
3. **السيناريو الأساسي للمهمة**: إنشاء ناجح، ثم فشل الترحيل المحاسبي
   (`RuntimeError` محاكاة "فترة مالية مقفلة") → تحرير الحجز فوراً + الفاتورة
   تبقى `draft` + فاتورة واحدة فقط في القاعدة → إعادة محاولة الترحيل بعدها
   تنجح على *نفس* الفاتورة (لا ازدواج).
4. فشل الحجز (نقص مخزون) لا يزال يعمل كما كان (لم يتأثر بالإصلاح).
5. إعادة إرسال `/post` لفاتورة مُرحَّلة فعلاً → نفس النتيجة بلا أي أثر جانبي
   إضافي (تحقق بـ `assert_not_awaited` على كل الـ Ports).

**ملاحظة تشغيل**: نفّذتُ الاختبار هنا باستخدام حزم Stub محلية مؤقتة (خارج
هذا التسليم) لبقية الوحدات (accounting/inventory/catalog/partners/tenancy/
platform_core/shared_kernel) لأنها غير موجودة في حزمة العمل المُسلَّمة لي —
عند تشغيله داخل الشجرة الكاملة الحقيقية سيُستبدَل هذا تلقائياً بالوحدات
الفعلية بلا أي تعديل على ملف الاختبار نفسه.

## ما لم يتغيّر عمداً
- الخطر النادر الموثَّق أصلاً في تعليق أعلى `sales_use_cases.py` (فشل
  `deduct_stock` بعد نجاح الترحيل المحاسبي، بعد استهلاك بعض الحجوزات) —
  يبقى كما هو (يحتاج Saga/Outbox حقيقي، خارج حجم يومين لهذه المهمة، وموثَّق
  أصلاً كـ TODO معروف).
- لا Alembic migration مُرفَقة لعمود `idempotency_key` الجديد (لا مجلد
  migrations ضمن حدود هذه الحزمة) — يلزم إضافة revision عند الدمج.
