# docs/modules

ملف تفصيلي لكل Module عند تصميمه (Domain Rules، ERD جزئي) — إضافة إلى
`README.md` المختصر الموجود داخل كل `apps/core-api/modules/{name}/`.

سمِّ الملف: `{module_name}.md` (مثال: `accounting.md`, `inventory.md`).
