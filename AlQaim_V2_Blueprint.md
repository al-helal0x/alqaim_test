# 🏛️ القائم V2 — الوثيقة المرجعية الرسمية لإعادة بناء المنتج
## AlQaim ERP — Product & Architecture Blueprint (Reference Document)

> هذه الوثيقة هي **المرجع الرسمي** الذي يعتمد عليه كل عضو في الفريق طوال دورة حياة المشروع.
> لا يوجد هنا كود. الهدف: **تصميم منتج تجاري كامل** قبل كتابة أي سطر برمجي.

**نسخة الوثيقة:** 2.0 — **تاريخ:** أغسطس 2026 — **الحالة:** معتمد للبدء بمرحلة التصميم التفصيلي

> **ملخص التحديث من النسخة 1.0:** بناءً على مراجعة خبير، رُفعت **منصة الذكاء الاصطناعي** (قراءة وفهم الفواتير بالذكاء الاصطناعي تحديداً) من "ميزة مستقبلية" إلى **منصة أساسية Core** مصمَّمة منذ اليوم الأول، مع قسم كامل جديد (7) لها، وتحديثات متوازية في الأولويات (5)، قاعدة البيانات (8)، الـ APIs (9)، هيكل المشروع (12)، وتقسيم الفريق (13) — مع إضافة عضو ثامن أساسي مسؤول عنها.

---

## جدول المحتويات

1. تحليل المشروع الحالي (V1)
2. تحليل الأنظمة المنافسة
3. تحليل احتياجات السوق
4. القائمة الشاملة للوحدات (Modules)
5. ترتيب الأولويات (MVP → Future)
6. تصميم النظام (Architecture)
7. **منصة الذكاء الاصطناعي (AI Platform)** ⭐ جديد
8. تصميم قاعدة البيانات
9. تصميم الـ APIs
10. Naming Convention
11. Coding Standards
12. هيكل المشروع (Folder Structure)
13. تقسيم الفريق (8+ أعضاء)
14. مصفوفة الملفات/الجداول/الـ APIs لكل عضو
15. خطة الدمج على GitHub
16. خطة التنفيذ خطوة بخطوة
17. المخاطر المحتملة وكيفية تجنبها
18. خطة التطوير المستقبلية

---

## 1. تحليل المشروع الحالي (V1)

> بناءً على فحص فعلي للحزمة المرفوعة (`alqaim_project-main.zip`).

### 1.1 البنية الفعلية الموجودة

المشروع مقسّم إلى 4 مجلدات رئيسية:

| المجلد | الحالة الفعلية | الملاحظة |
|---|---|---|
| `alqaim_core` | ✅ منطق فعلي يعمل | محرك محاسبة + مخزون مبني على SQLAlchemy + SQLite، فيه فعلاً Models وServices تعمل ومُختبرة |
| `alqaim_desktop` | 🚧 هيكل جزئي | واجهات PySide6 (Windows/Dialogs/ViewModels) تستورد `alqaim_core` **مباشرة داخل نفس العملية** (in-process import)، وليس عبر API |
| `alqaim_gateway` | ⏳ أسماء مجلدات فقط | `api/ cloud/ licensing/ networking/ security/ sync/` كلها تحتوي فقط على `__init__.py` فارغ — لا يوجد أي منطق فعلي |
| `alqaim_mobile` | ⏳ أسماء مجلدات فقط | نفس الحال، هيكل بدون أي تنفيذ |

### 1.2 نقاط القوة (تستحق الاحتفاظ بها كـ"مفهوم")

- **فصل مبدئي واضح بين المحرك والواجهة**: فكرة "Core مستقل يُستهلك من واجهات متعددة" فكرة صحيحة ويجب البناء عليها — لكن يجب تطبيقها بشكل صحيح (عبر API وليس عبر import مباشر).
- **محرك محاسبة مزدوج القيد حقيقي**: `JournalEntryService` يتحقق فعلاً من توازن القيد (مدين = دائن) قبل الترحيل — هذا مبدأ محاسبي صحيح ويجب أن يبقى جوهر أي محرك محاسبي جديد.
- **شجرة حسابات هرمية (Parent/Child)** مع حساب `full_code` و`full_path` — مفهوم سليم لـ Chart of Accounts.
- **ربط تلقائي بين الفواتير والمخزون والمحاسبة** (`InvoiceAccountingService`, `InventoryAccountingService`) — فكرة الـ "auto journal generation" من المستندات التشغيلية هي نفس فلسفة Odoo/QuickBooks، ويجب أن تستمر كحجر أساس.
- وجود اختبارات فعلية (`run_all_tests.py`, `test_partner_system.py`...) — ثقافة اختبار موجودة ولو بدائية.
- طموح معماري مبكر جيد: التفكير في `gateway` منفصل للـ licensing/sync/cloud يدل على وعي بأن هناك حاجة مستقبلية لخدمات مركزية — لكنه لم يُنفَّذ.

### 1.3 نقاط الضعف الجوهرية (يجب التخلص منها)

| المشكلة | التفصيل | الأثر |
|---|---|---|
| **لا يوجد Multi-Tenancy إطلاقاً** | لا يوجد `company_id` في أي جدول (`Account`, `Product`, `Invoice`...). قاعدة بيانات واحدة = شركة واحدة فقط | يستحيل تشغيله كـ SaaS لآلاف الشركات بدون إعادة بناء كاملة |
| **لا يوجد Multi-Branch / Multi-Warehouse** | جدول `Product` فيه `current_stock` كحقل واحد مباشر على المنتج، لا يوجد مفهوم "مخزون لكل فرع/مستودع" | لا يمكن إدارة أكثر من موقع |
| **Primary Keys أعداد صحيحة تسلسلية (Auto-increment Integer)** | لا UUID ولا ULID | يصعّب المزامنة Offline/Cloud مستقبلاً، ويكشف حجم البيانات (IDOR)، ويصعّب الدمج بين قواعد بيانات متعددة عند الحاجة لـ sharding |
| **الواجهة تستدعي المحرك مباشرة (Direct Import) وليس عبر API** | `alqaim_desktop` يعمل `sys.path.append('../alqaim_core')` ويستورد الكلاسات مباشرة | هذا يعني: لا يمكن أبداً بناء ويب أو موبايل أو Cloud لاحقاً بدون إعادة كتابة الطبقة بالكامل؛ أي منطق أعمال "مطبوخ" داخل الواجهة أحياناً |
| **لا يوجد نظام صلاحيات (RBAC) ولا مستخدمين متعددين** | لا يوجد جدول `users` ولا `roles` ولا `permissions` في `alqaim_core` | لا يمكن اعتماده لشركة فيها أكثر من موظف واحد |
| **لا يوجد Soft Delete ولا Audit Trail حقيقي** | `BaseModel` فيه فقط `created_at/updated_at`، لا `deleted_at`، لا `created_by/updated_by`، لا Audit Log منفصل | لا يمكن تتبّع من غيّر ماذا، ولا استرجاع البيانات المحذوفة، وهذا مطلب أساسي لأي نظام محاسبي (تدقيق) |
| **لا يوجد Multi-Currency** | كل المبالغ `Float` بدون عملة ولا سعر صرف | Float نفسه خطأ محاسبي (يجب Decimal)، وغياب العملة يمنع التوسع الدولي |
| **استخدام `Float` للمبالغ المالية** | فروقات تقريب (rounding errors) خطيرة في نظام محاسبي | مشكلة صحة بيانات جوهرية، ليست تفصيلاً |
| **`alqaim_gateway` و`alqaim_mobile` مجرد أسماء مجلدات فارغة** | لا يوجد أي منطق فعلي لتنفيذ الترخيص أو المزامنة أو السحابة | كل ما ورد في التوثيق عن "الجاهزية السحابية" طموح غير منفَّذ، ويجب تصميمه من الصفر |
| **SQLite كقاعدة بيانات وحيدة** | مناسبة فقط لنسخة تجريبية/Desktop-only | لا تصلح لـ Cloud متعدد المستخدمين المتزامنين بكثافة |
| **لا فصل بين Domain/Application/Infrastructure** | الـ Services تتعامل مباشرة مع SQLAlchemy Session، والـ ViewModels تتعامل مباشرة مع الـ Services دون طبقة Use-Cases واضحة | يصعّب الاختبار المستقل والاستبدال المستقبلي (مثلاً تغيير قاعدة البيانات) |
| **الترقيم اليدوي للمستندات (`entry_number`, `invoice_number`) يُمرَّر من الخارج** | لا يوجد Sequence/Numbering Service مركزي بقواعد قابلة للتخصيص لكل شركة | خطر تكرار الأرقام عند تعدد الشركات/الفروع |
| **لا توجد طبقة Validation موحدة (لا Pydantic Schemas ولا DTOs)** | التحقق مبعثر داخل الـ Models (`@validates`) | صعوبة توحيد رسائل الخطأ وتعدد اللغات |

### 1.4 ما يجب الاحتفاظ به كمفهوم (وليس كتنفيذ)

- مبدأ "محرك مستقل + عملاء متعددون".
- مبدأ القيد المزدوج المتوازن وربطه التلقائي بالمستندات.
- شجرة الحسابات الهرمية.
- ثقافة الاختبارات من اليوم الأول.
- فكرة وجود Gateway مركزي للترخيص/المزامنة/السحابة — لكن يُعاد تصميمها كخدمة API حقيقية من البداية.

### 1.5 القرار

**لا تُستخدم أي قاعدة كود من V1 كأساس.** V1 يُعامل كوثيقة "منطق أعمال مرجعية" فقط (كيف يجب أن يتوازن القيد، كيف تُربط الفاتورة بالمخزون والمحاسبة). البناء الفعلي يبدأ من الصفر وفق التصميم الوارد في هذه الوثيقة.

## 2. تحليل الأنظمة المنافسة

> الهدف: استخلاص الأفكار الفائزة والشكاوى المتكررة، دون نسخ أي نظام.

| النظام | أفضل ما فيه (نستفيد منه) | نقاط الضعف / شكاوى شائعة | الفرصة لنا |
|---|---|---|---|
| **Odoo** | معمارية Modular حقيقية (كل تطبيق Module منفصل يعتمد على `base`)، ORM قوي، ربط تلقائي سلس بين المبيعات↔المخزون↔المحاسبة، Studio لتخصيص الحقول بدون كود | يصبح بطيئاً وثقيلاً مع كثرة الـ Modules، التسعير المعقّد بين Community/Enterprise، صعوبة الترقية بين الإصدارات (Migration مؤلم)، منحنى تعلّم إداري صعب للمستخدم البسيط | نتبنى فكرة "Module يعتمد على Module" لكن بحدود صارمة (Dependency Graph موثّق)، ونجعل الترقية بين الإصدارات جزءاً من التصميم من اليوم الأول (Migration-first) |
| **ERPNext (Frappe)** | مفتوح المصدر بالكامل وسهل التخصيص، محرك Workflow مرن (حالات مخصصة لكل مستند)، تقارير ديناميكية (Report Builder) قوية | واجهة مستخدم أقل نضجاً بصرياً، الأداء يضعف مع البيانات الضخمة بدون tuning، التوثيق العربي ضعيف | نبني UI عصرية من البداية (Design System)، ونضمّن Workflow Engine عام قابل لإعادة الاستخدام عبر كل الوحدات |
| **Dolibarr** | خفيف وسريع التثبيت، مناسب للشركات الصغيرة، Modules تُفعَّل عند الحاجة فقط (Pay-as-you-grow UX) | تصميم واجهة قديم نسبياً، بعض الوحدات سطحية وتحتاج إضافات خارجية لتكتمل | نتبنى فلسفة "فعّل ما تحتاج فقط" مع Onboarding يقترح الوحدات المناسبة لنوع النشاط |
| **Akaunting** | تركيز واضح على المحاسبة البسيطة أولاً بدل ERP ثقيل، نظام Plugins/Marketplace نظيف | محدود جداً خارج المحاسبة الأساسية، المخزون والمشتريات المتقدمة تحتاج Plugins طرف ثالث | نصمم Marketplace/Plugin System منذ البداية كجزء من العمارة الأساسية وليس إضافة لاحقة |
| **Zoho Books** | تجربة مستخدم ناعمة، تكامل واسع مع بقية منظومة Zoho (CRM, Inventory, Payroll)، أتمتة قواعد سير العمل (Workflow Rules) بدون كود | يصبح مكلفاً مع الاشتراك في تطبيقات Zoho المتعددة، بعض التقارير الضريبية المحلية (خارج أسواقها الأساسية) محدودة | نُغلّف الضرائب والفوترة الإلكترونية كـ "Tax Engine" قابل للتوطين محلياً (السعودية/الخليج/مصر...) بدل حل عام |
| **QuickBooks** | الأشهر عالمياً للشركات الصغيرة، تسوية بنكية (Bank Reconciliation) ممتازة وربط مباشر بالبنوك | ضعيف في تعدد الفروع/المخزون المتقدم، محدود المرونة للشركات متوسطة/كبيرة الحجم | نجعل التسوية البنكية والربط مع البنوك/البوابات المالية أولوية MVP مبكرة وليست إضافة لاحقة |
| **Xero** | تصميم واجهة أنيق جداً، Marketplace تكاملات ضخم، تقارير مالية واضحة للمستخدم غير المحاسبي | لا يدعم POS داخلياً بقوة، بعض الأسواق تفتقر لدعم ضريبي محلي كامل | نصمم Dashboard مالي "يفهمه غير المحاسبي" من اليوم الأول (لغة مبسطة + رسوم بيانية) |
| **SAP Business One** | عمق وظيفي هائل للشركات المتوسطة/الكبيرة، تحكم دقيق في التكاليف والمخازن ومراكز التكلفة | تعقيد شديد وتكلفة تنفيذ عالية، يحتاج Partner متخصص للتركيب والتخصيص | نأخذ عمق "مراكز التكلفة" و"تقييم المخزون المتقدم" لكن نغلّفه بواجهة أبسط وتفعيل تدريجي |
| **Microsoft Dynamics 365 Business Central** | تكامل عميق مع Office/Excel، محرك Extensions/AppSource قوي ومرن | يتطلب Microsoft Stack غالباً، تكلفة تراخيص مرتفعة | نتبنى فكرة "Extension Marketplace" لكن مبنية على معايير مفتوحة (REST/Webhooks) بدل قفل على منصة واحدة |
| **Oracle NetSuite** | الأقوى في Multi-Company/Multi-Subsidiary وتوحيد العملات والتقارير المجمّعة (Consolidation) عالمياً | مكلف جداً، غير مناسب للشركات الصغيرة، تخصيصه يحتاج فرق تقنية مخصصة | نبني Multi-Company/Multi-Currency Consolidation كـ Core Capability من اليوم الأول، لكن نجعله متاحاً حتى للشركة الصغيرة بدون تعقيد إضافي إن لم تحتَجه |

### أهم الشكاوى المتكررة عبر كل الأنظمة (Pain Points المشتركة)
1. صعوبة الترحيل من نظام لآخر (Import بيانات قديمة) — لذلك سنُصمم أدوات Import/Export قوية كوحدة أساسية وليست ثانوية.
2. ضعف الدعم المحلي للفوترة الإلكترونية والضرائب العربية (ZATCA، ضريبة القيمة المضافة الخليجية والمصرية...) — سنبني **Tax/E-Invoicing Engine** قابل للتوطين كوحدة مستقلة.
3. بطء الأداء مع كبر البيانات — سنعتمد Pagination/Indexing/Caching كمتطلب تصميم من البداية وليس تحسيناً لاحقاً.
4. صعوبة العمل بدون إنترنت (خاصة لنقاط البيع في مناطق ضعيفة التغطية) — سنُصمم **Offline-First لوحدة POS تحديداً** من البداية.
5. تعقيد الصلاحيات عند نمو الشركة (فروع/أقسام) — سنبني RBAC هرمي مرن (دور + استثناءات على مستوى الشاشة/الحقل).

## 3. تحليل احتياجات السوق

### 3.1 خصائص السوق العربي المستهدف (أولوية الإطلاق)
- **الفوترة الإلكترونية الإلزامية**: السعودية (ZATCA Phase 1 & 2 — QR + XML + ربط API)، مصر (منظومة الفاتورة الإلكترونية ETA)، دول الخليج الأخرى تتجه لنفس النمط. يجب أن يكون **E-Invoicing Engine** قابلاً للتوطين لكل دولة كـ Plugin منفصل، لا منطقاً مدمجاً بالكود الأساسي.
- **الاتجاه من اليمين لليسار (RTL)** كواجهة أساسية وليست "ترجمة إضافية" — التصميم (Layout, Icons, Reports, PDF) يجب أن يُبنى RTL-first ثم يُدعم LTR، وليس العكس.
- **تعدد اللهجات/المصطلحات المحاسبية** بين الدول (مثال: "الضريبة المضافة" مقابل "ضريبة القيمة المضافة") — نظام ترجمة (i18n) مرن يسمح بقواميس مخصصة لكل عميل/دولة.
- **اعتماد كبير على نقاط البيع (POS) في القطاع التجاري الصغير والمتوسط** خصوصاً التجزئة والمطاعم — يجب أن تكون POS تجربة مستقلة سريعة (Offline-first) وليست شاشة ثانوية داخل ERP ثقيل.
- **تفاوت جودة الإنترنت** في بعض المناطق — الحاجة لنسخة Self-Hosted (On-Premise) بجانب Cloud، وقابلية العمل دون اتصال لبعض الوحدات الحرجة (POS، إدخال المخزون).
- **الشركات العائلية/الصغيرة تفضل التسعير البسيط** (لكل مستخدم أو لكل فرع) — يجب أن يدعم النظام Licensing مرن (SaaS متعدد الخطط + عدد مستخدمين/فروع كوحدة تسعير).

### 3.2 الفجوة التي يمكن للقائم V2 استغلالها
1. لا يوجد حالياً نظام عربي "أصيل" (Arabic-native, ليس مترجَماً) يجمع بين عمق SAP B1 وبساطة Zoho Books وسعر Dolibarr.
2. غياب حل عربي جيد يدمج POS + Accounting + E-Invoicing + Multi-Branch بسلاسة حقيقية دون الحاجة لتكامل يدوي بين أنظمة منفصلة.
3. فرصة تقديم Self-Hosted حقيقي (وليس Cloud فقط) للقطاعات الحساسة (حكومي، عائلي كبير) التي ترفض تخزين بياناتها خارجياً.

### 3.3 استنتاج مباشر يؤثر على التصميم
- **Multi-Company / Multi-Branch / Multi-Currency / RBAC / Audit Trail / Soft Delete** ليست "مميزات مستقبلية" بل **متطلبات Core** يجب أن تكون في تصميم قاعدة البيانات وAPI منذ اليوم الأول، حتى لو لم تُستخدم فعلياً في MVP — لأن إضافتها لاحقاً تعني إعادة هيكلة كاملة (وهذا بالضبط خطأ V1).
- **E-Invoicing/Tax Engine** يُصمم كـ Plugin/Module منفصل قابل للتوطين، وليس Hardcoded.
- **POS** يُصمم Offline-first منذ البداية.
- **إدخال البيانات اليدوي (خاصة فواتير الموردين الورقية) يبقى أكبر نقطة ألم تشغيلية يومية** في الشركات الصغيرة/المتوسطة بالسوق العربي تحديداً، حيث ضعف الأتمتة أكبر مقارنة بالأسواق الناضجة — هذا يجعل **قراءة الفواتير بالذكاء الاصطناعي (OCR/Invoice Understanding) فرصة تفوّق تنافسي حقيقية وليست ميزة كمالية**، وتُفصَّل بالكامل كمنصة أساسية في القسم 7.

## 4. القائمة الشاملة للوحدات (Modules)

> هذه القائمة أوسع من القائمة المذكورة في الطلب الأصلي، وتضيف وحدات اكتُشفت أثناء التحليل ضرورية لأي ERP حقيقي (خاصة Multi-Tenancy، الترقيم، الضرائب، محرك الموافقات، والمزامنة).
>
> **تحديث بعد مراجعة الخبير:** أُضيفت مجموعة كاملة (ط) أدناه لـ **منصة الذكاء الاصطناعي**، ورُفعت من "ميزة مستقبلية بسيطة" إلى **منصة أساسية موازية للـ Modules التشغيلية**، بتفاصيل كاملة في القسم 7 المخصص لها.

### 4.1 الجدول الرئيسي (تصنيف حسب المجموعة)

#### أ) الأساسيات والبنية التحتية (Platform / Foundation)
| # | الوحدة | الوصف المختصر | يعتمد على |
|---|---|---|---|
| 1 | Identity & Authentication | تسجيل الدخول، JWT/Sessions، OAuth، 2FA | — (أساس للجميع) |
| 2 | Users | إدارة المستخدمين وملفاتهم الشخصية | Identity |
| 3 | Roles & Permissions (RBAC) | أدوار + صلاحيات دقيقة (شاشة/حقل/عملية) | Users |
| 4 | Companies (Tenants) | الكيان القانوني/الشركة، إعداداتها، شعارها، سجلها الضريبي | Identity |
| 5 | Branches | فروع الشركة، كل فرع له مستودعات ومستخدمين | Companies |
| 6 | Warehouses | المستودعات وموقعها وربطها بالفروع | Branches |
| 7 | Currencies & Exchange Rates | العملات، أسعار الصرف اليومية/التاريخية | Companies |
| 8 | Numbering Sequences | مولّد أرقام مستندات قابل للتخصيص لكل شركة/فرع | Companies |
| 9 | System Settings | إعدادات عامة/شركة/فرع (Cascading Settings) | Companies |
| 10 | Localization (i18n) | اللغات، الترجمات، تنسيقات التاريخ/الأرقام، RTL/LTR | — |
| 11 | Audit Log | سجل كل تغيير (من، متى، ماذا) عبر كل الوحدات | Users |
| 12 | Notifications | تنبيهات داخل النظام + بريد + SMS + Push | Users |
| 13 | File & Document Manager | تخزين مرفقات (فواتير ممسوحة، عقود، صور منتجات) | Companies |
| 14 | Global Search | بحث موحّد عبر كل الوحدات | كل الوحدات |
| 15 | Import / Export Engine | استيراد/تصدير Excel/CSV/JSON لكل كيان | كل الوحدات |
| 16 | Backup & Restore | نسخ احتياطي واسترجاع (Cloud/Self-Hosted) | Companies |
| 17 | Workflow & Approval Engine | تدفقات موافقة قابلة للتهيئة (مثال: فاتورة > 10000 تحتاج موافقة مدير) | Roles |
| 18 | Event Bus / Webhooks | نشر أحداث النظام لأنظمة خارجية | Infrastructure |
| 19 | Sync Engine (Offline) | مزامنة البيانات بين عميل Offline والخادم | Companies |
| 20 | Licensing & Subscription (Platform) | إدارة باقات الاشتراك للشركات (SaaS) | Companies |
| 21 | Admin/Platform Console | لوحة تحكم لمزود الخدمة لإدارة آلاف الشركات | Licensing |

#### ب) العلاقات التجارية (Parties)
| # | الوحدة | الوصف | يعتمد على |
|---|---|---|---|
| 22 | Customers | بيانات العملاء، حدود الائتمان، كشف الحساب | Companies |
| 23 | Suppliers | بيانات الموردين، شروط الدفع | Companies |
| 24 | Contacts (Partner الموحّد) | كيان موحّد (عميل/مورد/موظف قد يتشارك بيانات الاتصال) | — |
| 25 | Employees | بيانات الموظفين الأساسية (تُستخدم من HR أيضاً) | Companies, Branches |

#### ج) المخزون والمنتجات
| # | الوحدة | الوصف | يعتمد على |
|---|---|---|---|
| 26 | Products & Services | كتالوج المنتجات/الخدمات | Companies |
| 27 | Categories | تصنيف شجري للمنتجات | Products |
| 28 | Units of Measure (UoM) | وحدات القياس وتحويلاتها (كرتون↔قطعة) | Products |
| 29 | Price Lists | قوائم أسعار متعددة (جملة/تجزئة/عميل خاص) | Products |
| 30 | Inventory / Stock | أرصدة المخزون لكل مستودع، حركات الدخول/الخروج | Products, Warehouses |
| 31 | Stock Valuation | طرق تقييم المخزون (FIFO/Average/Standard Cost) | Inventory |
| 32 | Barcode & QR | توليد/قراءة الباركود لكل منتج | Products |
| 33 | Serial & Batch/Lot Tracking | تتبع الأرقام التسلسلية وتواريخ الصلاحية | Inventory |
| 34 | Stock Transfers | تحويل بضاعة بين مستودعات/فروع | Inventory |
| 35 | Stock Adjustments/Count | جرد وتسوية الفروقات | Inventory |

#### د) المشتريات والمبيعات
| # | الوحدة | الوصف | يعتمد على |
|---|---|---|---|
| 36 | Quotations (عروض الأسعار) | عرض سعر قابل للتحويل لطلب/فاتورة | Customers, Products |
| 37 | Sales Orders | أوامر البيع | Quotations |
| 38 | Purchase Requests/Orders | طلبات وأوامر الشراء | Suppliers, Products |
| 39 | Invoices (Sales & Purchase) | الفواتير | Orders, Accounting |
| 40 | Credit/Debit Notes (المرتجعات) | إشعارات دائن/مدين والمرتجعات | Invoices |
| 41 | POS (نقاط البيع) | واجهة بيع سريعة Offline-first | Inventory, Invoices |
| 42 | Payments & Receipts | سندات القبض والصرف وربطها بالفواتير | Invoices, Accounting |
| 43 | Bank & Cash Accounts | حسابات بنكية/صندوق وتسوية بنكية | Accounting |

#### هـ) المحاسبة والمالية
| # | الوحدة | الوصف | يعتمد على |
|---|---|---|---|
| 44 | Chart of Accounts | شجرة الحسابات | Companies |
| 45 | Journal Entries | القيود المحاسبية اليدوية والتلقائية | Chart of Accounts |
| 46 | Cost Centers | مراكز التكلفة وتوزيع المصاريف | Journal Entries |
| 47 | Fiscal Years & Periods | سنوات وفترات مالية + إقفال الفترة | Companies |
| 48 | Budgeting | الموازنات التقديرية ومقارنتها بالفعلي | Cost Centers |
| 49 | Fixed Assets | الأصول الثابتة والإهلاك | Chart of Accounts |
| 50 | Tax Engine | ضرائب متعددة قابلة للتوطين (VAT، ضريبة دخل...) | Companies |
| 51 | E-Invoicing | التكامل مع أنظمة الفوترة الإلكترونية الحكومية | Tax Engine, Invoices |
| 52 | Financial Reports | ميزان المراجعة، قائمة الدخل، الميزانية العمومية، التدفقات النقدية | Journal Entries |
| 53 | Multi-Currency Consolidation | تجميع تقارير عدة شركات/عملات في تقرير موحّد | Companies, Currencies |

#### و) الموارد البشرية
| # | الوحدة | الوصف | يعتمد على |
|---|---|---|---|
| 54 | Employees Profiles | ملفات موظفين تفصيلية (عقود، مستندات) | Employees |
| 55 | Attendance | الحضور والانصراف | Employees |
| 56 | Leave Management | الإجازات وأرصدتها | Employees |
| 57 | Payroll | الرواتب والاستقطاعات والتأمينات | Attendance, Accounting |
| 58 | Performance/Appraisal | تقييم الأداء | Employees |
| 59 | Recruitment | التوظيف ومتابعة المتقدمين | — |

#### ز) العلاقات مع العملاء والمشاريع
| # | الوحدة | الوصف | يعتمد على |
|---|---|---|---|
| 60 | CRM (Leads/Opportunities) | إدارة العملاء المحتملين وفرص البيع | Contacts |
| 61 | Helpdesk / Support Tickets | تذاكر دعم فني لعملاء الشركة نفسها | Contacts |
| 62 | Projects | المشاريع والمراحل | Companies |
| 63 | Tasks | المهام المرتبطة بالمشاريع أو مستقلة | Projects |
| 64 | Timesheets | تسجيل ساعات العمل على المشاريع/المهام | Tasks, Employees |
| 65 | Manufacturing (BOM & Work Orders) | قوائم المواد وأوامر التصنيع | Inventory, Products |
| 66 | Subscriptions & Recurring Billing | اشتراكات دورية وفوترة متكررة للعملاء | Invoices |
| 67 | Fleet Management | إدارة المركبات (مستقبلي) | Assets |

#### ح) التحليلات والتوسع
| # | الوحدة | الوصف | يعتمد على |
|---|---|---|---|
| 68 | Dashboards & Analytics (BI) | لوحات تحكم تفاعلية | كل الوحدات |
| 69 | Custom Reports Builder | بناء تقارير مخصصة بدون كود | Analytics |
| 70 | Plugin / Marketplace System | إضافات طرف ثالث قابلة للتركيب | Platform Core |
| 71 | Integration Hub (Payment Gateways, Shipping, Banks) | تكاملات جاهزة | Webhooks |
| 72 | AI Assistant / Smart Insights | اقتراحات ذكية (تنبؤ مخزون، كشف شذوذ) | Analytics |
| 73 | OCR for Invoices | استخراج بيانات الفواتير الورقية آلياً | File Manager |
| 74 | Admin/Settings UI Panel | شاشات إدارة عامة للنظام | كل الوحدات |

#### ط) منصة الذكاء الاصطناعي (AI Platform) — تُفصَّل بالكامل في القسم 7
| # | الوحدة | الوصف | يعتمد على |
|---|---|---|---|
| 75 | Document Preprocessing | تصحيح ميلان/ضوضاء/اكتشاف حدود المستند | File Manager |
| 76 | OCR Engine | استخراج نص خام من صور/PDF بالعربية والإنجليزية | Document Preprocessing |
| 77 | Invoice Understanding (Document AI) | تحويل النص الخام لحقول فاتورة مُهيكَلة | OCR Engine |
| 78 | Product/Supplier Matching Engine | مطابقة دلالية بالـ Embeddings + اقتراح كيانات جديدة | Catalog, Partners |
| 79 | AI Validation Engine | تحقق منطقي من صحة الأرقام المستخرجة | Invoice Understanding |
| 80 | Human Review & Learning Engine | شاشة مراجعة + تسجيل تصحيحات المستخدم لتحسين الدقة | Notifications |
| 81 | Accounting AI | اقتراح حسابات/كشف قيود شاذة | Accounting |
| 82 | AI Assistant | مساعد محادثة طبيعية عبر LLM Gateway | كل الوحدات (قراءة) |
| 83 | Recommendation Engine | اقتراحات إعادة طلب/تسعير/خفض مصاريف | Inventory, Sales |
| 84 | Predictive Analytics | توقع مبيعات/مخزون/تدفق نقدي | Reporting |
| 85 | LLM Gateway | نقطة وصول موحّدة لأي نموذج لغوي تستهلكه أي وحدة | — |

### 4.2 شرح تفصيلي للوحدات الجوهرية (Core Modules)

> يُشرح هنا بعمق الوحدات التي تشكّل عمود النظام (المطلوبة لـ MVP والمرحلة الثانية). باقي الوحدات (المرحلة الثالثة وما بعدها) تُفصَّل بنفس القالب عند بدء تصميمها التفصيلي، تفادياً لتضخّم هذه الوثيقة قبل أوانه.

**Companies (Tenants)**
- **الهدف:** تمثيل كل عميل مشترك في المنصة كشركة مستقلة البيانات بالكامل.
- **لماذا مهم:** هو "جذر" العزل بين كل الشركات (Multi-Tenancy) — كل جدول تشغيلي في النظام يحمل `company_id` يشير إليه.
- **طريقة العمل:** عند تسجيل شركة جديدة تُنشأ سجلاتها الافتراضية (شجرة حسابات افتراضية، عملة أساسية، فرع رئيسي واحد) تلقائياً عبر Onboarding Wizard.
- **البيانات:** الاسم القانوني، السجل التجاري، الرقم الضريبي، العملة الأساسية، الشعار، الباقة المشترك بها.
- **العلاقة ببقية النظام:** كل وحدة تقريباً تتبع لشركة واحدة عبر `company_id`.
- **يعتمد عليه مستقبلاً:** Consolidation بين عدة شركات لنفس المجموعة (Group of Companies).

**Branches / Warehouses**
- **الهدف:** تمكين الشركة من إدارة أكثر من موقع فعلي (فرع بيع، مستودع تخزين).
- **طريقة العمل:** الفرع وحدة تنظيمية/جغرافية (لها مستخدمون ونقاط بيع)، المستودع وحدة مخزنية (قد يتبع أكثر من فرع نظرياً، لكن الغالب مستودع لكل فرع).
- **العلاقة:** المخزون والفواتير والمستخدمون كلهم يُقيَّدون اختيارياً بفرع.

**RBAC (Roles & Permissions)**
- **الهدف:** التحكم الدقيق فيمن يرى/يعدّل/يحذف/يوافق على ماذا.
- **طريقة العمل:** دور (Role) = مجموعة صلاحيات معرّفة على مستوى (Module → Action: view/create/edit/delete/approve/export)، مع إمكانية استثناءات فردية لمستخدم معيّن، وربط اختياري بفرع محدد (مدير فرع لا يرى فرعاً آخر).
- **العلاقة:** بوابة أمان تُستدعى من كل API Endpoint.

**Chart of Accounts / Journal Entries (المحاسبة)**
- **الهدف:** جوهر أي عملية مالية في النظام — كل حدث تجاري (فاتورة، دفعة، صرف مخزون) يُترجم لقيد محاسبي متوازن.
- **طريقة العمل:** قيد = رأس (Header) + سطور (Lines)، كل سطر مدين أو دائن على حساب معيّن، ومجموع المدين = مجموع الدائن إلزامياً (Constraint على مستوى قاعدة البيانات + التطبيق).
- **الفرق عن V1:** الحساب هنا مرتبط بشركة (`company_id`)، والمبالغ `Decimal` وليست `Float`، والقيد يحمل مرجع للعملة وسعر الصرف وقت الترحيل.

**Inventory / Stock**
- **الهدف:** معرفة الكمية المتاحة والقيمة الفعلية لكل صنف في كل مستودع لحظياً.
- **طريقة العمل:** رصيد المنتج ليس حقلاً واحداً بل **مُشتق (Derived) من مجموع حركات المخزون** لكل (منتج × مستودع)، مع طريقة تقييم قابلة للاختيار لكل شركة (متوسط مرجّح افتراضياً).

**Sales/Purchase Invoices**
- **الهدف:** المستند التجاري الأساسي الذي يولّد أثراً محاسبياً ومخزنياً معاً.
- **طريقة العمل:** الفاتورة تمر بحالات (مسودة → مرحّلة → مدفوعة جزئياً/كلياً → ملغاة)، وكل انتقال حالة قد يُطلق أحداثاً (توليد قيد، تحديث مخزون، إشعار).

**POS**
- **الهدف:** نقطة بيع سريعة الاستجابة تعمل حتى بدون إنترنت، ثم تُزامن عند عودة الاتصال.
- **طريقة العمل:** طبقة تخزين محلي (SQLite/IndexedDB حسب المنصة) + طابور مزامنة (Sync Queue) يُعالج التعارضات (Conflict Resolution) عند إعادة الاتصال.

**Numbering Sequences / Workflow Engine**
- **الهدف:** توليد أرقام مستندات فريدة وقابلة للتخصيص (بادئة/سنة/تسلسل لكل فرع)، وتنظيم مسارات الموافقة على المستندات الحساسة.
- **طريقة العمل مستقبلية:** أي وحدة جديدة تُسجَّل في Workflow Engine بدل بناء منطق موافقة خاص بها.

## 5. ترتيب الأولويات (MVP → Future)

### فلسفة الترتيب
كل وحدة "أساسية" (Foundation) تدخل ضمن MVP حتى لو بشكل مبسّط، لأن غيابها يعني إعادة هيكلة لاحقاً (نفس خطأ V1 بالضبط). أما وحدات "التوسع الأفقي" (HR، CRM، Manufacturing...) فتُؤجَّل لأنها لا تُغيّر البنية الأساسية عند إضافتها لاحقاً.

> **تحديث بعد مراجعة الخبير:** بند "AI Assistant / AI Accounting / OCR" الذي كان مذكوراً سابقاً هنا تحت "قابلية التوسع" ضمن Future Modules فقط **أُلغي بصيغته المبسّطة تلك**. جزء منه (OCR + فهم الفاتورة الأساسي + المطابقة + المراجعة البشرية) انتقل الآن إلى **MVP نفسه** كميزة تنافسية جوهرية، والباقي (المساعد الذكي، Accounting AI، التنبؤات) موزَّع على المراحل 2/3/Future أدناه. التفصيل الكامل والمبررات في القسم 7 (منصة الذكاء الاصطناعي).

### 🟢 MVP (الإصدار الأول القابل للبيع)
**الهدف:** شركة واحدة أو عدة شركات صغيرة، فرع أو أكثر، تدير مبيعات/مشتريات/مخزون/محاسبة/POS بثقة كاملة، عبر API من اليوم الأول.

- Identity & Authentication, Users, Roles & Permissions (RBAC)
- Companies, Branches, Warehouses
- Currencies & Exchange Rates (حتى لو بعملة واحدة فعلياً، البنية موجودة)
- Numbering Sequences, System Settings, Localization (عربي/إنجليزي RTL)
- Audit Log (أساسي: من/متى/ماذا)
- Customers, Suppliers, Contacts
- Products, Categories, Units of Measure, Price Lists
- Inventory (أرصدة + حركات + تقييم متوسط مرجّح)
- Purchase Orders, Sales Orders/Quotations
- Sales & Purchase Invoices, Credit/Debit Notes
- Payments & Receipts, Bank & Cash Accounts (تسوية بسيطة)
- Chart of Accounts, Journal Entries, Fiscal Years/Periods
- Financial Reports الأساسية (ميزان مراجعة، قائمة دخل، ميزانية عمومية)
- Tax Engine (VAT أساسي) + E-Invoicing لسوق إطلاق واحد (مثلاً السعودية)
- POS (نسخة أولى، Offline لاحقاً في المرحلة الثانية)
- Notifications الأساسية (داخل النظام)
- Import/Export (Excel أساسي للمنتجات والعملاء)
- Admin/Platform Console (نسخة مبسّطة لإدارة الشركات المشتركة)
- **AI Platform (المرحلة الأولى فقط — القسم 7.13):** OCR + Invoice Understanding الأساسي + Product/Supplier Matching + AI Validation + Human Review — **بدون ترحيل تلقائي بلا موافقة بشرية**

### 🟡 المرحلة الثانية
- Cost Centers, Budgeting, Fixed Assets
- Multi-Currency Consolidation
- Workflow & Approval Engine (عام لكل الوحدات)
- Stock Transfers, Stock Adjustments/Count, Barcode & QR, Serial/Batch Tracking
- POS Offline-First الكامل + Sync Engine
- File & Document Manager, Global Search
- Employees, Attendance, Leave Management, Payroll (HR أساسي)
- CRM (Leads/Opportunities)
- Dashboards & Analytics (BI) نسخة أولى
- E-Invoicing لأسواق إضافية (مصر، الإمارات...)
- Webhooks / Event Bus (فتح النظام لتكاملات خارجية)
- **AI Platform (المرحلة الثانية — القسم 7.13):** تحسين المعالجة المسبقة لصور الهاتف، بصمة تخطيط المورد، تفعيل حلقة التعلّم الفعلية

### 🟠 المرحلة الثالثة
- Projects, Tasks, Timesheets
- Manufacturing (BOM & Work Orders)
- Helpdesk / Support Tickets
- Subscriptions & Recurring Billing
- Custom Reports Builder (بدون كود)
- Performance/Appraisal, Recruitment
- Plugin / Marketplace System (فتح المنصة لمطورين خارجيين)
- Integration Hub (بوابات دفع، شحن)
- **AI Platform (المرحلة الثالثة/الرابعة — القسم 7.13):** Accounting AI، Recommendation Engine، AI Assistant بمحادثة طبيعية

### 🔵 Future Modules
- **AI Platform (المرحلة الخامسة — القسم 7.13):** Predictive Analytics متقدمة، قراءة إيصالات/شيكات/كشوف حساب/عروض أسعار، التعرف على التوقيعات
- Fleet Management
- تطبيقات Native كاملة (iOS/Android/Desktop مخصصة لكل نظام تشغيل بدل PWA فقط)
- GraphQL API (بجانب REST) عند نضوج الحاجة من المطورين الخارجيين
- Marketplace علني لبيع الإضافات من طرف ثالث بعمولة

### لماذا هذا الترتيب تحديداً
- **Multi-Tenancy وRBAC والترقيم والعملات** ليست ميزات بل "عمود فقري" — لو أُجّلت، كل وحدة تُبنى بعدها ستحتاج تعديلاً رجعياً (Retrofit) مكلفاً جداً — هذا بالضبط ما حدث في V1.
- **POS Offline والـ Workflow Engine العام** أُجّلا للمرحلة الثانية لأنهما يحتاجان نضج الـ API الأساسي أولاً (لا معنى لمزامنة Offline لواجهة لم تستقر بعد).
- **HR وCRM والتصنيع** لاحقة لأنها "أفقية" — تُضاف كوحدات مستقلة دون التأثير على جوهر المحاسبة/المخزون.
- **الـ Marketplace العلني** يحتاج قاعدة مستخدمين وبيانات فعلية أولاً ليكون مجدياً. أما **OCR وفهم الفواتير الأساسي فأُدرِج في MVP نفسه** (وليس بعده) لأنه — بحسب مراجعة الخبير — نقطة تفوّق تنافسي مقصودة منذ الإطلاق الأول وليست ميزة "لطيفة الإضافة" لاحقاً؛ أما الطبقات الأعمق (Accounting AI، المساعد الذكي، التنبؤات) فتبقى مؤجَّلة لأنها تحتاج فعلياً بيانات تاريخية وحلقة تعلّم ناضجة (القسم 7.6) لا تتوفر إلا بعد تشغيل MVP فترة كافية.

## 6. تصميم النظام (Architecture)

### 6.1 القرار المعماري الأول: نمط النظام العام

| الخيار | المزايا | العيوب |
|---|---|---|
| **Microservices من اليوم الأول** | استقلالية كاملة، توسع أفقي لكل خدمة | تعقيد تشغيلي هائل مبكراً (Service Discovery, Distributed Transactions, Observability)، فريق صغير لا يستطيع إدارته، معاملات محاسبية موزّعة (Distributed Transactions) خطرة جداً في المحاسبة |
| **Monolith تقليدي (طبقات أفقية فقط)** | بساطة البداية | يتحول سريعاً لـ "Big Ball of Mud"، صعوبة عزل فريق لكل وحدة (نفس مشكلة V1) |
| **✅ Modular Monolith (الموصى به)** | خدمة واحدة تُشغَّل، لكن **مقسّمة داخلياً لوحدات مستقلة الحدود (Bounded Contexts)** بحيث كل Module له طبقاته الخاصة (Domain/Application/Infrastructure) ولا يصل لبيانات module آخر إلا عبر واجهة معلنة (Public Interface/Service)؛ قابلة للفصل لاحقاً كـ Microservice مستقلة إذا استدعت الحاجة (نمو فريق، حمل مرتفع على وحدة معيّنة مثل POS) | يتطلب انضباطاً في فرض حدود الوحدات (يُفرض عبر Linting/CI وليس فقط اتفاقاً شفهياً) |

**القرار:** Modular Monolith مبني بحيث "حدود الوحدة اليوم = حدود الخدمة المستقبلية عند الفصل". هذا يحل مشكلة V1 (حيث لا توجد حدود واضحة أصلاً) دون تحمّل تعقيد Microservices مبكراً.

### 6.2 القرار المعماري الثاني: مبدأ "API-first" الإلزامي

**كل عميل (Web / Desktop / Mobile / POS / تكاملات خارجية) يتحدث حصراً مع Core API عبر HTTP، ولا يوجد أي اتصال مباشر بقاعدة البيانات من أي واجهة.** هذا يُصحّح الخطأ الجوهري في V1 (حيث استوردت الواجهة المحرك مباشرة). النتيجة: نفس منطق الأعمال يعمل مطابقاً على كل منصة، لأنه لا يوجد إلا نسخة واحدة منه — داخل الـ API.

```
┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐
│    Web    │  │  Desktop  │  │  Mobile   │  │    POS    │   ← Presentation Clients (كلها Thin Clients)
│  (React)  │  │  (Tauri)  │  │ (Flutter) │  │ (Flutter) │
└─────┬─────┘  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘
      │              │              │              │
      └──────────────┴──────HTTPS───┴──────────────┘
                          │
                 ┌────────▼─────────┐
                 │   Core API        │  ← REST (+ Webhooks)، طبقة Application/Domain كاملة هنا
                 │ (Modular Monolith)│
                 └────────┬─────────┘
                          │
      ┌───────────────────┼───────────────────┐
┌─────▼─────┐      ┌──────▼──────┐     ┌──────▼──────┐
│ PostgreSQL│      │    Redis     │     │ Object Store │
│ (بيانات)  │      │(Cache/Queue) │     │ (مرفقات S3)  │
└───────────┘      └──────────────┘     └──────────────┘
```

### 6.3 اختيار التقنية (Technology Stack)

| الطبقة | الخيار الموصى به | البديل المدروس | سبب الاختيار |
|---|---|---|---|
| Backend Language/Framework | **Python + FastAPI** | Node.js/NestJS، Java/Spring Boot، Go | الفريق لديه خبرة Python فعلية من V1، FastAPI يدعم Async وPydantic للتحقق التلقائي من DTOs وتوليد توثيق OpenAPI تلقائياً (يخدم API-first مباشرة)، نضج مكتبات المحاسبة/الأرقام (`decimal`) |
| قاعدة البيانات | **PostgreSQL** | MySQL، SQL Server | دعم JSONB (لحقول مرنة دون تعديل Schema كل مرة)، Row Level Security (تفيد Multi-Tenancy)، Window Functions (تقارير مالية)، مفتوح المصدر بالكامل (يخدم Self-Hosted) |
| Cache/Queue | **Redis** | Memcached, RabbitMQ | استخدام مزدوج: Cache + Queues بسيطة (Celery/RQ) في مرحلة مبكرة قبل الحاجة لـ Broker متخصص |
| Object Storage | **S3-compatible (AWS S3 سحابياً / MinIO ذاتياً)** | تخزين على القرص المحلي مباشرة | نفس الواجهة البرمجية (S3 API) تعمل سحابياً وذاتياً بدون تغيير كود |
| Web Frontend | **React + Next.js** (أو Vue كبديل مكافئ) | Angular | نضج المكتبات، سهولة توفر مطورين بالسوق العربي |
| Desktop (Windows/Linux/macOS) | **تغليف تطبيق الويب عبر Tauri** بدل PySide6 منفصل | Electron، PySide6 (V1) | كود واحد لكل المنصات، حجم تطبيق أصغر من Electron، ويستهلك نفس Core API — إن احتاج المستخدم تجربة "Native حقيقية" لاحقاً (خصوصاً POS بأجهزة قديمة الموارد)، يُدرَس Flutter Desktop كبديل في المرحلة الثانية |
| Mobile (Android/iOS) | **Flutter** | React Native | كود واحد لمنصتين، أداء قريب من Native، مناسب لواجهة POS/مخزون بسيطة |
| PWA | نفس تطبيق الـ Web (Next.js) بإضافة Service Worker | تطبيق منفصل | لا تكلفة إضافية، يُستخدم مباشرة كنسخة مثبّتة من نفس الكود |
| Background Jobs | **Celery / RQ** (على Redis) | مؤجل حتى الحاجة الفعلية | مهام مثل توليد التقارير الثقيلة، إرسال الفواتير الإلكترونية، النسخ الاحتياطي |
| Containerization | **Docker + Docker Compose (Self-Hosted) / Kubernetes (Cloud)** | تشغيل مباشر بدون حاويات | يوحّد بيئة Cloud وSelf-Hosted، ويسهّل التوسع الأفقي سحابياً |

### 6.4 الطبقات داخل كل Module (Clean/Hexagonal Architecture — Feature First)

كل Module (مثال: `invoicing`, `inventory`, `accounting`) يحتوي داخلياً على نفس الطبقات الأربع، **معزولة عن باقي الوحدات**:

```
modules/
└── invoicing/
    ├── domain/            # الكيانات والقواعد الصرفة، بدون أي اعتماد على DB أو Framework
    │   ├── entities/       # Invoice, InvoiceLine (Business Objects نقية)
    │   ├── value_objects/  # Money, TaxRate
    │   └── rules/          # قواعد العمل (لا يمكن ترحيل فاتورة غير متوازنة...)
    ├── application/        # Use Cases / Services تنسّق العمل، تستدعي الـ Domain وتتحدث مع Ports
    │   ├── use_cases/      # CreateInvoiceUseCase, PostInvoiceUseCase
    │   ├── dto/            # Pydantic Schemas للطلب/الاستجابة
    │   └── ports/          # واجهات مجرّدة (IInvoiceRepository) — لا تنفيذ هنا
    ├── infrastructure/     # التنفيذ الفعلي: SQLAlchemy Repository، استدعاء Modules أخرى، Email...
    │   ├── repositories/
    │   ├── external/
    │   └── models/         # SQLAlchemy ORM Models (تُطابق DB Schema)
    └── presentation/       # طبقة الـ API فقط (FastAPI Routers) — لا منطق أعمال هنا إطلاقاً
        └── routes/
```

**قاعدة الاعتماد الصارمة (Dependency Rule):**
`presentation → application → domain` و`infrastructure → application/domain` (تنفيذ الـ Ports). **لا يُسمح أبداً** لـ `domain` أن يستورد من `infrastructure` أو `presentation`. يُفرض هذا عبر أداة تحليل استيراد (import-linter/dependency-cruiser) في CI، وليس اتفاقاً شفهياً فقط.

### 6.5 المجلدات المشتركة (Core / Shared / Common)

- **`platform_core/`**: بنية تحتية عابرة لكل الوحدات: قاعدة الاتصال بقاعدة البيانات، إعداد Logging، Middleware المصادقة/الصلاحيات، Base Repository، Event Bus، Cache Client، Storage Client. **لا يحتوي أي منطق أعمال خاص بوحدة معينة.**
- **`shared_kernel/`**: كيانات/قيم يُشترك فيها أكثر من Module حقيقةً بحكم النطاق (مثال: `Money`, `Currency`, `TenantContext`, `AuditFields`) — يُستخدم بحذر شديد لتفادي تحوّله "مكب" لكل شيء مشترك.
- **`common/` (Frontend)**: مكوّنات UI عامة (أزرار، جداول، نماذج) مستقلة عن أي منطق أعمال.

### 6.6 نظام الأحداث (Event-Driven بين الوحدات)

كل تغيير مهم (فاتورة رُحّلت، دفعة استُلمت، مخزون نفد) يُصدر **Domain Event** داخلياً. الوحدات الأخرى **تشترك** في الأحداث التي تهمها بدل أن تستدعي بعضها مباشرة (تقليل الترابط/Coupling):

- مثال: عند ترحيل فاتورة بيع → `InvoicePosted` Event → وحدة `inventory` تستهلكه لخصم الكمية، ووحدة `accounting` تستهلكه لتوليد القيد، ووحدة `notifications` تستهلكه لتنبيه المحاسب.
- **الآلية الفنية:** في المرحلة الأولى (Modular Monolith) الأحداث تُنشر داخل نفس العملية (In-process Event Bus)، مع **نمط Outbox** (تُكتب الأحداث في جدول `outbox_events` ضمن نفس معاملة قاعدة البيانات، ثم تُنشر لاحقاً) لضمان عدم فقدان الحدث حتى لو تعطّل النظام لحظة الحفظ. عند الانتقال مستقبلاً لـ Microservices، يُستبدل الناقل الداخلي بـ Message Broker خارجي (RabbitMQ/Kafka) دون تغيير منطق الوحدات نفسها.

### 6.7 Logging & Observability
- **Structured Logging (JSON)** مع `request_id` و`tenant_id (company_id)` و`user_id` في كل سطر لوق — يسمح بتتبع أي عملية عبر كل الطبقات.
- تجميع مركزي للّوقات (مثال: Grafana Loki أو ELK حسب البنية التحتية المتاحة وقت التنفيذ — قرار تشغيلي وليس معمارياً حرجاً الآن).
- Metrics أساسية (زمن استجابة API، معدل الأخطاء، عدد القيود المرحّلة/الدقيقة) عبر Prometheus-compatible endpoint.

### 6.8 Exception Handling
- **تصنيف موحّد للأخطاء:** `DomainError` (خطأ منطق أعمال، مثال: "القيد غير متوازن") → `ValidationError` (بيانات مدخلة خاطئة) → `AuthorizationError` → `NotFoundError` → `InfrastructureError` (فشل تقني: قاعدة بيانات، شبكة).
- **Exception Middleware واحد مركزي** في الـ API يحوّل كل نوع لكود HTTP وصيغة استجابة موحّدة (انظر قسم Naming Convention لصيغة كود الخطأ)، ولا يُسرَّب أي Stack Trace للعميل في الإنتاج.

### 6.9 Caching Strategy
- Cache للقراءات كثيرة التكرار قليلة التغيّر: صلاحيات المستخدم (تُبطَل عند تعديل الدور)، قوائم الأسعار، أسعار الصرف اليومية، إعدادات الشركة.
- إبطال الكاش (Invalidation) عبر نفس Event Bus (حدث `AccountSettingsChanged` يمسح مفتاح الكاش المرتبط).

### 6.10 الأمان (Security)
- JWT (Access Token قصير العمر + Refresh Token) لكل الجلسات عبر كل المنصات.
- كل طلب API يمر عبر Middleware يتحقق من: التوثيق (Authentication) → عزل الشركة (`company_id` من التوكن، لا يُقبل أبداً من الـ Body) → الصلاحية (RBAC) → صحة المدخلات (Schema Validation).
- تشفير كلمات المرور (bcrypt/argon2)، تشفير الحقول الحساسة في قاعدة البيانات (أرقام حسابات بنكية)، HTTPS إلزامي، Rate Limiting على نقاط تسجيل الدخول.
- **مبدأ Zero Trust بين الوحدات نفسها:** حتى الاستدعاء الداخلي بين Module وModule يمر عبر واجهة تتحقق من صلاحية السياق (Tenant Context) المُمرَّر، لا يُفترض "أننا داخل نفس العملية إذاً آمن".

### 6.11 Backup & Synchronization
- **Backup:** نسخ احتياطي آلي مجدول لكل قاعدة بيانات (أو لكل Schema في حال العزل بـ Schema-per-tenant)، اعتماد WAL Archiving في PostgreSQL لدعم Point-in-Time Recovery، ونسخ منفصل لمخزن الملفات (Object Storage).
- **Synchronization (لعملاء Offline مثل POS):** كل عملية محلية تُسجَّل في طابور محلي (`local_sync_queue`) بترتيب زمني، وعند عودة الاتصال تُرسَل دفعة (Batch) للخادم عبر Endpoint مخصص للمزامنة يتحقق من التعارضات (مثال: نفس المنتج بيع من نقطتي بيع مختلفتين وقت انقطاع الاتصال) بقاعدة "آخر تحديث فائز على مستوى الحقل + تنبيه للمحاسب عند تعارض الكميات السالبة".

### 6.12 استثناء معماري مقصود: منصة الذكاء الاصطناعي
بخلاف كل الوحدات التشغيلية أعلاه التي تعيش داخل `core-api` كـ Modules ضمن Modular Monolith واحد (القسم 6.1)، **منصة الذكاء الاصطناعي (AI Platform) هي الاستثناء الوحيد المتعمَّد**: تُبنى كخدمة منفصلة قابلة للنشر باستقلالية منذ اليوم الأول، بسبب اختلاف طبيعة حملها (OCR/LLM أثقل وأبطأ من CRUD عادي) ودورة تحديثها (تحديث نموذج لا يجب أن يوقف النظام المحاسبي). التفصيل الكامل — الموضع، آلية التواصل عبر Event Bus/Job Queue، وكيفية استبدال أي محرك (OCR/LLM) دون لمس بقية النظام — في **القسم 7 (منصة الذكاء الاصطناعي)** المخصص بالكامل لهذا الموضوع.

## 7. منصة الذكاء الاصطناعي (AI Platform)

> **قرار استراتيجي مُحدَّث بناءً على مراجعة خبير:** الذكاء الاصطناعي (خاصة قراءة وفهم الفواتير) لن يُعامَل كـ"ميزة مستقبلية" (Future Module) كما ورد سابقاً في القسم 5، بل كـ**منصة أساسية (Core Platform)** مصمَّمة منذ اليوم الأول بحيث تُبنى كل الوحدات التشغيلية (الفواتير، المستندات، المحاسبة، المخزون) بافتراض وجودها، حتى لو كانت درجة الاستخدام الفعلي تتوسع تدريجياً عبر المراحل. **يُعاد تموضع "القائم" رسمياً كـ "AI-First ERP" وليس "برنامج محاسبة مضاف إليه OCR".** هذا القرار يؤثر على قاعدة البيانات (القسم 8) والـ APIs (القسم 9) وهيكل المشروع (القسم 12) وتشكيل الفريق (القسم 13) — وقد عُدِّلت جميعها بما يعكس ذلك.

### 7.1 تحليل احتياجات الذكاء الاصطناعي (كل السيناريوهات، مرتّبة حسب الأولوية)

| السيناريو | القيمة للمستخدم | المرحلة |
|---|---|---|
| قراءة فاتورة مورد (صورة/PDF) واستخراج حقولها الأساسية | يلغي إدخال الفواتير يدوياً — أكبر نقطة ألم تشغيلية يومية | 🟢 MVP |
| مطابقة المنتجات المستخرجة مع الكتالوج القائم، واقتراح منتج جديد عند عدم الوجود | يمنع ازدواج/تشتت بيانات المنتجات | 🟢 MVP |
| التحقق الآلي من صحة الأرقام (تطابق الإجمالي، عدم وجود قيم سالبة) | يمنع أخطاء إدخال قبل وصولها للمحاسبة | 🟢 MVP |
| شاشة مراجعة بشرية (Human Review) لكل نتيجة قبل الاعتماد | الثقة — لا فاتورة تُرحَّل آلياً بدون عين بشرية في هذه المرحلة | 🟢 MVP |
| التعرف على المورد تلقائياً وربطه بسجل موجود | تسريع الإدخال المتكرر لنفس الموردين | 🟡 المرحلة 2 |
| تعلّم النظام من تعديلات المستخدم (Learning Loop) لتحسين الدقة مع الوقت | تراكم قيمة تنافسية حقيقية بمرور الوقت (Moat) | 🟡 المرحلة 2 |
| تصحيح جودة الصورة تلقائياً (ميلان، ضوضاء، اكتشاف حدود المستند) قبل الـ OCR | يرفع الدقة على صور الهاتف الواقعية (ليست ماسحة ضوئية مثالية) | 🟡 المرحلة 2 |
| اقتراح الحساب المحاسبي المناسب لكل بند فاتورة | يقلّل الاعتماد على خبرة محاسب متمرس لكل إدخال | 🟠 المرحلة 3 |
| اكتشاف القيود الناقصة/الشاذة والتنبيه عليها | أداة تدقيق داخلي مستمر | 🟠 المرحلة 3 |
| توصيات إعادة الطلب وتحسين الأسعار بناءً على أنماط البيع | قيمة تحليلية تفوق مجرد إدخال البيانات | 🟠 المرحلة 3 |
| قراءة الإيصالات، الشيكات، كشوف الحساب، عروض الأسعار، أوامر الشراء | يوسّع أتمتة الإدخال لكل مستندات دورة العمل، وليس الفواتير فقط | 🔵 Future |
| مساعد ذكي بمحادثة طبيعية (استعلام/تقارير/شرح أخطاء) | واجهة تفاعل بديلة أسرع من التنقل بين الشاشات | 🔵 Future |
| توقع المبيعات/المخزون/التدفق النقدي | تخطيط استباقي بدل تفاعلي فقط | 🔵 Future |
| التعرف على التوقيعات، تحليل مستندات عامة متعددة الأنواع | امتداد طبيعي بعد نضج المحرك الأساسي | 🔵 Future |

### 7.2 OCR & Document Intelligence

**الهدف:** تحويل أي مصدر بصري (صورة هاتف، PDF، ملف ماسحة ضوئية، عدة صفحات) إلى نص خام موثوق، بغض النظر عن جودة المصدر.

**خط المعالجة الفرعي (قبل OCR الفعلي):**
1. **Document Detection** — اكتشاف حدود المستند داخل الصورة (فصل الفاتورة عن الخلفية/الطاولة).
2. **Deskew & Denoise** — تصحيح الميلان وإزالة الضوضاء (كلاسيكيات معالجة صور OpenCV، لا تحتاج نموذج ذكاء اصطناعي ثقيل).
3. **Orientation Detection** — اكتشاف اتجاه الصفحة (مقلوبة/مستقيمة) وتصحيحه.
4. **Multi-page Assembly** — دمج عدة صفحات/صور لنفس المستند في وحدة معالجة واحدة.
5. **OCR الفعلي** — استخراج النص مع دعم العربية والإنجليزية والمستندات المختلطة (فاتورة عربية بأرقام/مصطلحات إنجليزية داخلها، شائعة جداً في السوق العربي).

### 7.3 Invoice Understanding (فهم وليس قراءة فقط)

طبقة منفصلة تعلو الـ OCR الخام، تحوّل "نصاً مبعثراً على الصفحة" إلى **بيانات مُهيكَلة** عبر نموذج فهم مستندات (Document Understanding — يدمج موقع النص على الصفحة/Layout مع معناه، وليس النص وحده): اسم المورد، رقم الفاتورة، التاريخ، العملة، الضريبة، الخصم، الإجمالي، وقائمة البنود (كل بند: الوصف، الكمية، الوحدة، السعر، الباركود إن وُجد)، والملاحظات.

### 7.4 Smart Entity Recognition & Matching Engines

- **Product Matching Engine:** يقارن اسم المنتج المستخرج مع كتالوج الشركة عبر تشابه دلالي (وليس تطابقاً حرفياً فقط) — "كوكاكولا" و"Coca Cola" و"Coca-Cola" و"كولا" تُعامَل كمرشحين لنفس المنتج عبر مقارنة تمثيلات نصية (Embeddings)، وليس Regex أو قواعد يدوية تنهار مع أول اسم جديد.
  - **آلية العمل:** يُنتَج تمثيل رقمي (Embedding) لاسم كل منتج مستخرج، يُقارَن بأقرب تمثيلات مخزّنة لمنتجات الشركة (Vector Similarity Search) → (أ) تطابق قوي جداً (>عتبة عالية): ربط تلقائي مباشر، (ب) تطابق متوسط: يُعرض أفضل 3 مرشحين للمستخدم ليختار، (ج) لا تطابق: يُقترَح إنشاء منتج جديد ببياناته المستخرجة معبّأة مسبقاً.
- **Supplier Recognition Engine:** نفس المبدأ على مستوى المورد، مع إضافة **بصمة تخطيط الفاتورة (Layout Fingerprint)** — نفس المورد غالباً يستخدم نفس قالب الفاتورة، فيتعرف عليه النظام بثقة أعلى في المرات القادمة حتى لو تغيّر الاسم المكتوب قليلاً.

### 7.5 AI Validation Engine

بعد الاستخراج، يفحص النظام: تطابق (مجموع البنود + الضريبة − الخصم = الإجمالي)، عدم وجود كميات صفرية أو أسعار سالبة، تطابق العملة مع عملة الشركة أو وجود سعر صرف صريح، عدم تكرار رقم فاتورة لنفس المورد. أي مخالفة تُعرَض كتنبيه واضح في شاشة المراجعة، **ولا تُرحَّل الفاتورة تلقائياً أبداً عند وجود أي تنبيه غير محلول.**

### 7.6 Human Review & Learning Engine (حلقة التعلّم — جوهر الميزة التنافسية)

- **Human Review:** شاشة تعرض صورة الفاتورة الأصلية جنباً إلى جنب مع الحقول المستخرجة، كل حقل قابل للتعديل المباشر، مع إبراز الحقول ذات الثقة المنخفضة (Confidence Score) ليركّز عليها المستخدم بدل مراجعة كل شيء يدوياً.
- **Learning Engine:** كل تعديل يُجريه المستخدم يُسجَّل كـ "زوج تصحيح" (القيمة المستخرجة آلياً ↔ القيمة الصحيحة النهائية) في جدول تغذية راجعة مخصص (القسم 8.9). هذه التصحيحات تُستخدم دورياً (ليس فورياً لكل تعديل) لـ: (أ) تحسين عتبات الثقة، (ب) إعادة تدريب/ضبط دقيق (Fine-tuning) لنماذج المطابقة الداخلية، (ج) بناء "بصمة" لكل مورد متكرر تقلّل الحاجة للمراجعة اليدوية مع الوقت لنفس المورد تحديداً.
- **مبدأ مهم:** التعلّم يبقى **مؤسسياً لكل شركة على حدة بشكل أساسي** (تصحيحات شركة لا تُسرَّب لشركة أخرى)، مع إمكانية اختيارية (Opt-in فقط) لمشاركة تحسينات عامة غير حساسة (مثل "بصمة تخطيط" لمورد كبير معروف يتعامل معه آلاف العملاء) على مستوى المنصة ككل — قرار خصوصية صريح يُعرض للشركة ويمكنها رفضه.

### 7.7 Accounting AI

بعد نضج الوحدة المحاسبية والبيانات التاريخية الكافية (المرحلة 3): اقتراح الحساب المحاسبي المناسب لكل بند فاتورة بناءً على تصنيف المنتج/المورد وسوابق الشركة نفسها، اقتراح توزيع مركز التكلفة، اكتشاف قيود ناقصة (مستندات مرحّلة بلا قيد مقابل) وعمليات شاذة إحصائياً (مبلغ يفوق النمط المعتاد لهذا المورد/الحساب بشكل ملحوظ) — تُعرض كـ**اقتراحات قابلة للرفض**، لا تُنفَّذ تلقائياً أبداً على حسابات مالية.

### 7.8 AI Assistant & Recommendation & Predictive Analytics (مرحلة لاحقة)

- **AI Assistant:** إجابة أسئلة بلغة طبيعية عن بيانات الشركة ("ما هو رصيد العميل X؟")، توليد تقرير بوصف نصي، شرح سبب رفض عملية معينة (خطأ صلاحية/توازن قيد) بلغة مفهومة لغير التقني.
- **Recommendation Engine:** اقتراح إعادة الطلب عند اقتراب نفاد المخزون، اقتراح تعديل أسعار بناءً على هامش الربح والمنافسة الداخلية بين المنتجات، اقتراح تقليل مصاريف متكررة غير ضرورية.
- **Predictive Analytics:** توقع المبيعات/الطلب/التدفق النقدي عبر نماذج سلاسل زمنية إحصائية أولاً (بسيطة، شفافة، قابلة للتفسير) قبل الانتقال لنماذج تعلّم عميق أثقل — الشفافية أهم من الدقة الهامشية في سياق مالي يحتاج المستخدم أن يفهم "لماذا" يقول النظام هذا.

### 7.9 هندسة الخدمات (AI Services)

خدمات مستقلة داخلياً خلف واجهات ثابتة (Ports)، بحيث يمكن استبدال أي منها دون لمس البقية:

| الخدمة | المسؤولية | الواجهة (Port) |
|---|---|---|
| Document Preprocessing Service | Deskew/Denoise/Detection | `IDocumentPreprocessor` |
| OCR Service | استخراج النص الخام | `IOCREngine.extract(image) -> RawText` |
| Invoice Parser Service | تحويل النص الخام لحقول مُهيكَلة | `IInvoiceParser.parse(raw_text, layout) -> InvoiceDraft` |
| Product Matcher | مطابقة دلالية للمنتجات | `IEntityMatcher.match(text, candidates) -> RankedMatches` |
| Supplier Matcher | مطابقة الموردين + بصمة التخطيط | `IEntityMatcher` (نفس الواجهة، تنفيذ مختلف) |
| AI Validation Service | فحوصات المنطق المالي | `IValidator.validate(invoice_draft) -> Warnings[]` |
| Learning Engine | تسجيل التصحيحات + إعادة الضبط الدوري | `ILearningStore.record_correction(...)` |
| LLM Gateway | نقطة وصول موحّدة لأي استدعاء نموذج لغوي (فهم/مساعد/اقتراحات) | `ILLMProvider.complete(prompt) -> Response` |
| Recommendation Engine | التوصيات والتنبؤات | `IRecommendationService` |

### 7.10 AI Architecture — الموضع داخل النظام (استثناء معماري مقصود)

**القرار:** خلافاً لبقية الوحدات التشغيلية (التي تعيش كـ Modules داخل Modular Monolith واحد كما في القسم 6.1)، **منصة الذكاء الاصطناعي تُبنى كخدمة منفصلة قابلة للنشر باستقلالية (`apps/ai-platform`) منذ اليوم الأول**، وليس كـ Module داخلي. السبب:

1. **طبيعة حمل مختلفة جذرياً:** استدعاءات OCR/LLM أثقل وأبطأ (ثوانٍ لا مللي ثانية) من عمليات CRUD العادية — دمجها بنفس عملية `core-api` يهدد زمن استجابة كل الشاشات الأخرى.
2. **دورة نشر/تحديث مختلفة:** تحديث نموذج أو محرك OCR لا يجب أن يتطلب إعادة نشر النظام المحاسبي بالكامل.
3. **تبعيات ثقيلة ومتخصصة** (مكتبات رؤية حاسوبية/تعلّم آلي) يُفضَّل ألا تُثقِل بيئة تشغيل `core-api` الأساسية.
4. **حدود الوحدات أصلاً مصمَّمة لتحتمل الفصل لخدمة مستقلة عند الحاجة (القسم 6.1)** — الذكاء الاصطناعي هو أول مرشّح طبيعي لتطبيق هذا المبدأ من البداية بدل الانتظار.

**آلية التواصل مع `core-api`:**
- المهام غير الفورية (معالجة فاتورة مرفوعة) تمر عبر **طابور مهام غير متزامن (Job Queue على Redis/Celery)**: `core-api` ينشر مهمة `ProcessInvoiceDocument{attachment_id}`، `ai-platform` يلتقطها، يعالجها عبر خط الأنابيب (القسم 7.11)، وينشر حدث `InvoiceDraftReady{...}` عائداً عبر نفس Event Bus الموصوف في القسم 6.6 — بقية النظام (وحدة الفواتير، الإشعارات) تشترك في هذا الحدث دون معرفة أي تفاصيل داخلية عن كيفية عمل الذكاء الاصطناعي.
- الاستدعاءات شبه الفورية (سؤال للمساعد الذكي، طلب توصية سريعة) تمر عبر REST داخلي مباشر بين الخدمتين خلف الشبكة الداخلية (ليس عبر الإنترنت العام).
- **قابلية الاستبدال:** كل من `IOCREngine` و`ILLMProvider` و`IEntityMatcher` عبارة عن واجهة مجرَّدة يُهيَّأ تنفيذها الفعلي عبر إعدادات الشركة/النشر (Config) — استبدال محرك OCR أو نموذج LLM لاحقاً يعني كتابة تنفيذ جديد للواجهة نفسها، دون أي تعديل في `core-api` أو في أي Module آخر يستهلك النتيجة.

### 7.11 AI Data Pipeline (دورة حياة البيانات الكاملة)

```
Image/PDF
   │  (رفع من هاتف/سكانر/PDF مباشر)
   ▼
Preprocessing         → تصحيح ميلان، إزالة ضوضاء، اكتشاف حدود المستند، تحديد الاتجاه
   ▼
OCR                    → استخراج نص خام + إحداثيات كل كلمة على الصفحة (Bounding Boxes)
   ▼
Document Understanding → دمج النص + التخطيط المكاني لتحديد الحقول (رأس الفاتورة/جدول البنود/التذييل)
   ▼
Entity Recognition     → تصنيف كل حقل مستخرج (مورد/تاريخ/عملة/بند...) بثقة رقمية لكل حقل
   ▼
Validation             → فحوصات المنطق المالي (القسم 7.5)، تنبيهات إن وُجدت
   ▼
Product Matching       → ربط كل بند بمنتج قائم أو اقتراح منتج جديد
   ▼
Supplier Matching      → ربط الفاتورة بمورد قائم أو اقتراح مورد جديد
   ▼
Human Review           → شاشة مراجعة، المستخدم يعتمد أو يعدّل
   ▼
Learning               → تسجيل أي تعديل كتغذية راجعة لتحسين النماذج لاحقاً
   ▼
Database               → الفاتورة تدخل النظام التشغيلي رسمياً (تتبع بقية دورة الحياة العادية للفاتورة من القسم 6)
```

### 7.12 اختيار التقنيات (مقارنة وتوصية)

| الفئة | الخيارات المدروسة | المزايا/العيوب | التوصية |
|---|---|---|---|
| **OCR** | Tesseract (مفتوح المصدر) / PaddleOCR (مفتوح المصدر، دعم عربي قوي) / خدمات سحابية مُدارة (Azure Document Intelligence, Google Document AI, AWS Textract) | المفتوح المصدر: مجاني وقابل للتشغيل الذاتي (يخدم Self-Hosted) لكن دقته أقل على المستندات المعقدة؛ السحابي: دقة أعلى بكثير خاصة على تخطيطات معقدة لكن يتطلب اتصالاً بالإنترنت وتكلفة لكل استدعاء ويطرح سؤال خصوصية إرسال صور فواتير لخدمة خارجية | **نموذج هجين عبر نفس الواجهة (`IOCREngine`):** PaddleOCR كافتراضي ذاتي التشغيل (يخدم Self-Hosted وخصوصية البيانات)، مع خيار تفعيل محرك سحابي مُدار للعملاء على خطة Cloud الذين يفضّلون الدقة الأعلى — قرار يُترك لإعداد كل شركة |
| **Document AI / Layout Analysis** | نماذج مفتوحة قائمة على LayoutLM (تدمج النص+الموقع المكاني) / خدمات Document AI السحابية الجاهزة | المفتوح: قابل للتشغيل الذاتي وأرخص على نطاق واسع لكن يحتاج ضبطاً دقيقاً (Fine-tuning) على نماذج فواتير عربية فعلية؛ السحابي: جاهز فوراً وأدق مبدئياً لكن أقل تحكماً وأعلى تكلفة تراكمية | البدء بخدمة سحابية جاهزة لتسريع MVP (القسم 7.1)، مع الاستثمار التدريجي في نموذج مفتوح مضبوط على بيانات عربية فعلية (بفضل حلقة التعلّم في القسم 7.6) لتقليل التكلفة والاعتماد الخارجي مع نمو قاعدة البيانات التدريبية |
| **رؤية حاسوبية أساسية (Preprocessing)** | OpenCV (كلاسيكي) مقابل نماذج تعلّم عميق للـ Deskew/Detection | OpenCV كافٍ تماماً لهذه المهام البسيطة نسبياً، وأخف بكثير وأسرع | **OpenCV** — لا حاجة لتعقيد نموذج تعلّم عميق لمهمة يحلّها الجبر الخطي البسيط |
| **LLMs** (للفهم/المساعد/الاقتراحات) | استدعاء واجهة برمجية لنموذج لغوي مُدار (سحابي) مقابل نموذج مفتوح الأوزان يُشغَّل ذاتياً (Self-Hosted) | السحابي: جودة أعلى فوراً بدون بنية تحتية GPU خاصة، لكن يعني إرسال بيانات مالية لطرف ثالث — مشكلة فعلية للعملاء Self-Hosted الحسّاسين (حكومي، عائلي كبير) المذكورين في القسم 3 | **نموذج هجين عبر `ILLMProvider`:** مزوّد سحابي افتراضي لعملاء Cloud (أسرع طريق لجودة عالية)، وخيار توصيل نموذج مفتوح الأوزان ذاتي التشغيل لعملاء Self-Hosted الذين يمنعهم سياسة داخلية من إرسال بيانات خارجاً — نفس الواجهة، تنفيذ مختلف حسب بيئة النشر |
| **Embedding Models** (للمطابقة الدلالية) | نموذج تمثيل نصي متعدد اللغات (يدعم عربي/إنجليزي معاً) — مفتوح المصدر مقابل سحابي | يجب دعم تعدد اللغات صراحة (عربي وإنجليزي بنفس الفضاء الدلالي) وإلا تفشل مطابقة "كوكاكولا" مع "Coca Cola" | نموذج تمثيل متعدد اللغات مفتوح المصدر يُشغَّل ذاتياً — تكلفة تشغيل منخفضة جداً مقارنة بـ LLM كامل، ولا حاجة لجودة توليد نص، فقط تمثيل تشابه |
| **Vector Database** | امتداد `pgvector` على نفس PostgreSQL القائم مقابل قاعدة بيانات متجهات مستقلة (Milvus/Qdrant/Pinecone) | قاعدة مستقلة أسرع عند نطاق ضخم جداً لكنها تضيف مكوناً تشغيلياً جديداً بالكامل (تعقيد Self-Hosted، نسخ احتياطي منفصل...) | **`pgvector` على نفس PostgreSQL الموجود أصلاً (القسم 6.3)** — يتفق مع فلسفة "تقنية مثبتة وبسيطة أولاً" المعتمدة في هذه الوثيقة، ويُعاد التقييم لقاعدة متجهات مستقلة فقط إذا أثبت القياس الفعلي حاجة أداء تتجاوز طاقة `pgvector` |
| **Search Engine** (بحث عام يستخدمه AI Assistant) | فهرسة نصية كاملة (Full-Text Search) في PostgreSQL نفسه مقابل محرك بحث مستقل (Elasticsearch) | نفس منطق `pgvector` أعلاه: تقليل عدد المكوّنات التشغيلية المستقلة قدر الإمكان في المراحل المبكرة | **PostgreSQL Full-Text Search** (المذكور أصلاً في القسم 8.5) يكفي MVP والمرحلة الثانية؛ Elasticsearch يُدرَس فقط عند حاجة بحث نصي حر معقّد جداً عبر كل الوحدات دفعة واحدة |

### 7.13 خطة تطوير منصة الذكاء الاصطناعي (مراحل)

- **المرحلة الأولى (ضمن MVP):** OCR + استخراج الحقول الأساسية لفاتورة المورد (نص واضح نسبياً)، مطابقة منتج/مورد بسيطة، تحقق منطقي أساسي، شاشة مراجعة بشرية إلزامية لكل نتيجة. **لا ترحيل تلقائي بدون موافقة بشرية في هذه المرحلة إطلاقاً.**
- **المرحلة الثانية:** تحسين جودة المعالجة المسبقة (صور هاتف واقعية)، بصمة تخطيط المورد المتكرر، تفعيل حلقة التعلّم الفعلية من تصحيحات المستخدمين، خفض تدريجي في نسبة الفواتير التي تحتاج مراجعة كاملة (اعتماداً على درجة الثقة).
- **المرحلة الثالثة:** Accounting AI (اقتراح حسابات/كشف شذوذ)، Recommendation Engine (إعادة الطلب/الأسعار).
- **المرحلة الرابعة:** AI Assistant بمحادثة طبيعية، تفعيل استدعاء LLM Gateway عبر الوحدات الأخرى (تلخيص تقرير، شرح خطأ).
- **المرحلة الخامسة (Future):** Predictive Analytics متقدمة (مبيعات/تدفق نقدي)، توسيع أنواع المستندات المقروءة (إيصالات، شيكات، كشوف حساب، عروض أسعار)، تحليل توقيعات.

> **ملاحظة تكامل مع بقية الوثيقة:** بند "AI Assistant / AI Accounting / OCR" الوارد سابقاً في قسم "قابلية التوسع" (Future Modules) في القسم 5 **أُلغي كبند مستقل** واستُبدل بالخطة التفصيلية أعلاه، مع نقل الأجزاء الأساسية منه (OCR + Invoice Understanding الأولي) إلى MVP فعلياً كما هو موضح في القسم 7.1.

## 8. تصميم قاعدة البيانات

### 8.1 استراتيجية Multi-Tenancy

| الخيار | المزايا | العيوب |
|---|---|---|
| Database-per-Tenant | عزل كامل، نسخ احتياطي مستقل لكل شركة | تكلفة تشغيلية هائلة مع آلاف الشركات (Migrations تُشغَّل آلاف المرات)، لا يصلح لـ SaaS بسعر منخفض |
| Schema-per-Tenant | عزل جيد، Backup/Restore لشركة واحدة ممكن | إدارة آلاف الـ Schemas تعقّد الـ Migrations أيضاً، لكن أقل من الخيار الأول |
| **✅ Shared Schema + `company_id` (row-level) — الموصى به لـ Cloud** | Migration واحدة تخدم الجميع، تكلفة تشغيلية منخفضة، تحليلات عبر الشركات (لمزوّد الخدمة) سهلة | يتطلب انضباطاً صارماً (كل استعلام يجب أن يُقيَّد بـ `company_id`) — نمنع الخطأ عبر PostgreSQL **Row Level Security (RLS)** كخط دفاع ثانٍ بعد الكود |
| Dedicated DB للعميل الكبير/Self-Hosted | يُقدَّم كخيار Enterprise وSelf-Hosted فقط | — |

**القرار النهائي (نموذج هجين):**
- **Cloud (SaaS العام):** Shared Schema + `company_id` + Row Level Security. هذا هو الافتراضي لأغلب الشركات الصغيرة/المتوسطة.
- **Cloud (خطة Enterprise):** يمكن نقل الشركة لـ Schema أو Database مستقلة عند الحاجة (نفس بنية الجداول، فقط عزل فيزيائي أعلى) دون تغيير الكود لأن كل استعلام أصلاً مبني على افتراض العزل بـ `company_id`.
- **Self-Hosted:** قاعدة بيانات واحدة مستقلة بالكامل (لأن العميل أصلاً شركة واحدة غالباً).

### 8.2 مبادئ التصميم الإلزامية لكل جدول تشغيلي

1. **UUID (v7 أو ULID) كمفتاح أساسي** بدل Integer التسلسلي — يمنع كشف حجم البيانات، ويسهّل التوليد من العميل نفسه (مهم لعمليات Offline في POS حيث يُنشأ السجل محلياً قبل المزامنة).
2. **`company_id` إلزامي** على كل جدول تشغيلي (غير الجداول العامة مثل جدول الدول/العملات المرجعي).
3. **Audit Fields موحّدة** على كل جدول: `created_at, created_by, updated_at, updated_by, deleted_at` (Soft Delete وليس حذف فعلي أبداً للمستندات المالية).
4. **الأموال دائماً `DECIMAL/NUMERIC`** (مثال `NUMERIC(18,4)`) — **ممنوع استخدام Float** لأي مبلغ مالي أو كمية مخزون (كان خطأً جوهرياً في V1).
5. **كل مبلغ مالي يُرافقه حقل `currency_code`** وسعر الصرف وقت التنفيذ (`exchange_rate_at_transaction`) — لا يُعاد حساب المبلغ التاريخي لاحقاً حتى لو تغيّر سعر الصرف.
6. **Optimistic Locking**: حقل `version` (Integer) على الجداول القابلة للتعديل المتزامن (فاتورة، مخزون) لمنع Race Conditions عند تعديلين متزامنين.
7. **الفهارس (Indexes):** فهرس مركّب `(company_id, ...)` كأول عمود في كل فهرس تقريباً، لأن كل استعلام يبدأ بتصفية الشركة.

### 8.3 نموذج الجداول الأساسية (مختصر تمثيلي — ليس شاملاً، يُفصَّل بالكامل عند تصميم كل Module)

**`companies`**
`id (UUID PK), legal_name, trade_name, tax_registration_no, base_currency_code, subscription_plan, is_active, created_at...`

**`branches`**
`id (UUID PK), company_id (FK), name, code, address, is_main, is_active...`

**`warehouses`**
`id (UUID PK), company_id (FK), branch_id (FK), name, code, is_active...`

**`users`** (عام عبر المنصة، مستقل عن `company_id` مباشرة)
`id (UUID PK), email (unique), password_hash, full_name, is_platform_admin, is_active...`

**`user_company_roles`** (جدول ربط: مستخدم قد يعمل بأكثر من شركة/دور)
`id, user_id (FK), company_id (FK), role_id (FK), branch_id (FK nullable), is_active`

**`roles` / `permissions` / `role_permissions`**
`roles(id, company_id nullable للأدوار النظامية, name)`
`permissions(id, module, action)` — مثال: `module='invoicing', action='approve'`
`role_permissions(role_id, permission_id)`

**`accounts` (شجرة الحسابات)**
`id (UUID PK), company_id (FK), code, name, name_en, account_type, account_nature, parent_id (self FK), is_system, is_active, version...`

**`journal_entries` / `journal_entry_lines`**
`journal_entries(id, company_id, branch_id, entry_number, entry_date, fiscal_period_id, source_type, source_id, status, currency_code, exchange_rate, created_by...)`
`journal_entry_lines(id, journal_entry_id (FK), account_id (FK), cost_center_id nullable, debit NUMERIC(18,4), credit NUMERIC(18,4), description)`
— قيد CHECK على مستوى قاعدة البيانات: `SUM(debit) = SUM(credit)` يُتحقق منه Trigger عند تغيير حالة القيد إلى `posted` (وليس عند كل سطر منفرد).

**`products` / `product_categories` / `units_of_measure`**
`products(id, company_id, code, name, type[product|service], category_id, base_uom_id, sale_price, purchase_price, tax_rate_id, track_inventory, is_active)`

**`stock_movements`** (مصدر الحقيقة الوحيد لأرصدة المخزون — Event Sourced جزئياً)
`id, company_id, warehouse_id, product_id, movement_type[in|out|transfer|adjustment], quantity, unit_cost, source_type, source_id, movement_date, created_by`
— **لا يوجد حقل `current_stock` ثابت على جدول `products`**؛ الرصيد يُحسب (Materialized View أو جدول ملخّص `stock_balances` يُحدَّث عبر Trigger/Event لأسباب الأداء) من مجموع الحركات — هذا يمنع تعارض الرصيد المباشر مع الحركات الفعلية (كان الحقل مباشراً وخطراً في V1).

**`invoices` / `invoice_lines`**
`invoices(id, company_id, branch_id, invoice_type[sale|purchase], invoice_number, partner_id, status, currency_code, exchange_rate, subtotal, discount_amount, tax_amount, total_amount, journal_entry_id nullable, version)`

**`partners`** (عملاء/موردون بكيان موحّد + نوع)
`id, company_id, partner_type[customer|supplier|both], name, tax_no, credit_limit, payment_terms...`

**`numbering_sequences`**
`id, company_id, branch_id nullable, document_type, prefix, next_number, padding_length, reset_policy[never|yearly|monthly]`

**`outbox_events`** (لدعم نمط Outbox المذكور في قسم Architecture)
`id, aggregate_type, aggregate_id, event_type, payload (JSONB), created_at, published_at nullable`

**`audit_logs`**
`id, company_id, user_id, entity_type, entity_id, action[create|update|delete|approve...], old_values (JSONB), new_values (JSONB), created_at`

### 8.4 العلاقات والقيود (Constraints) — مبادئ عامة
- **Foreign Keys إلزامية** مع `ON DELETE RESTRICT` للسجلات المالية (لا يُحذف حساب عليه قيود)، و`ON DELETE CASCADE` فقط للبيانات التبعية البحتة (سطور المستند مع رأسه).
- **Unique Constraints مركّبة مع `company_id`**: مثال `UNIQUE(company_id, code)` على `products` و`accounts` — لأن نفس الكود قد يتكرر بين شركتين مختلفتين شرعاً.
- **Check Constraints** لمنع القيم السالبة الخاطئة (مثال: `quantity` في `stock_movements` النوع `in` يجب أن تكون موجبة).

### 8.5 جداول منصة الذكاء الاصطناعي (تفصيل كامل للمحرك في القسم 7)

> ملاحظة تقنية: هذه الجداول تعيش منطقياً ضمن قاعدة بيانات `ai-platform` المنفصلة (اتساقاً مع القسم 6.12/7.10 — الخدمة منفصلة النشر)، لكنها تتبع **نفس مبادئ التصميم** الواردة في القسم 8.2 (UUID، `company_id`، Audit Fields، Soft Delete).

**`ocr_jobs`**
`id (UUID PK), company_id (FK), attachment_id (FK → documents), status[queued|processing|done|failed], engine_used, confidence_score, created_at, completed_at`

**`invoice_extraction_drafts`**
`id (UUID PK), company_id, ocr_job_id (FK), extracted_payload (JSONB — كل الحقول المستخرجة مع ثقة كل حقل), matched_supplier_id (FK nullable), status[pending_review|approved|rejected], reviewed_by, reviewed_at`

**`entity_match_suggestions`**
`id, extraction_draft_id (FK), entity_type[product|supplier], extracted_text, matched_entity_id nullable, confidence_score, is_confirmed, user_selected_alternative_id nullable`

**`ai_learning_feedback`** (جوهر حلقة التعلّم — القسم 7.6)
`id, company_id, extraction_draft_id (FK), field_name, ai_predicted_value, user_corrected_value, corrected_by, corrected_at`

**`entity_embeddings`** (يستخدم امتداد `pgvector` — القسم 7.12)
`id, company_id, entity_type[product|supplier], entity_id, embedding VECTOR(768), updated_at`
— فهرس تشابه تقريبي (`ivfflat`/`hnsw` حسب إصدار pgvector) على عمود `embedding` لكل (`company_id`, `entity_type`) لتسريع البحث عن أقرب المرشحين.

**`ai_conversations` / `ai_messages`** (لمرحلة AI Assistant لاحقاً)
`ai_conversations(id, company_id, user_id, started_at)`
`ai_messages(id, conversation_id, role[user|assistant], content, created_at)`

### 8.6 الفهرسة (Indexing Strategy)
- فهرس `(company_id, status, invoice_date)` على `invoices` لتسريع أكثر الاستعلامات شيوعاً (فواتير الشركة حسب الحالة والتاريخ).
- فهرس `(company_id, product_id, warehouse_id)` على `stock_movements`.
- Full-Text Search Index (PostgreSQL `tsvector`) على أسماء المنتجات/الشركاء لدعم Global Search.

## 9. تصميم الـ APIs

### 9.1 المبادئ العامة
- **REST + JSON** كأساس (OpenAPI 3 تُولَّد تلقائياً من FastAPI)، مع النظر لاحقاً في **GraphQL** كطبقة إضافية اختيارية (Future Modules) لاحتياجات التقارير المرنة، دون استبدال REST.
- **Versioning عبر المسار:** `/api/v1/...` — لا يُكسر أي Endpoint منشور؛ التغييرات الكاسرة تُصدر كنسخة `v2` جديدة بجانب `v1` لفترة انتقالية معلنة.
- كل استجابة تتبع صيغة موحّدة:
```json
{
  "success": true,
  "data": { ... },
  "meta": { "request_id": "...", "pagination": { "page": 1, "page_size": 20, "total": 134 } },
  "errors": []
}
```
- الأخطاء دائماً بصيغة موحّدة مع كود خطأ ثابت (وليس فقط نص) لتسهيل الترجمة في الواجهة:
```json
{ "success": false, "errors": [{ "code": "JOURNAL_ENTRY_UNBALANCED", "message": "...", "field": null }] }
```

### 9.2 نمط تسمية الـ Endpoints (Resource-Based)
```
GET    /api/v1/invoices                # قائمة + فلترة + بحث + ترقيم صفحات
POST   /api/v1/invoices                # إنشاء
GET    /api/v1/invoices/{id}           # تفاصيل
PATCH  /api/v1/invoices/{id}           # تعديل جزئي
DELETE /api/v1/invoices/{id}           # حذف ناعم (Soft Delete)
POST   /api/v1/invoices/{id}/post      # فعل عمل خاص (ترحيل) — Verb كمسار فرعي وليس PATCH عام
POST   /api/v1/invoices/{id}/cancel
GET    /api/v1/invoices/{id}/pdf       # مستند مُصدَّر
```
- الأفعال الخاصة بحالة المستند (ترحيل، إلغاء، اعتماد) تكون دائماً `POST /{resource}/{id}/{action}` — لا يُستخدم `PATCH status=posted` لأن الانتقال بين الحالات له منطق أعمال (Side Effects: قيد محاسبي، حركة مخزون) وليس مجرد تحديث حقل.

### 9.3 Filtering / Search / Pagination (موحّدة عبر كل الموارد)
```
GET /api/v1/products?search=قلم&category_id=...&is_active=true
    &sort=-created_at&page=1&page_size=20
```
- Pagination: Offset-based افتراضياً (`page`/`page_size`) للشاشات الإدارية، مع دعم Cursor-based (`after=`) للقوائم الضخمة (سجل الحركات، Audit Log) لتفادي مشاكل أداء `OFFSET` الكبير.
- `sort=-field` للترتيب التنازلي، `sort=field` للتصاعدي.

### 9.4 Import / Export / Upload
```
POST /api/v1/products/import          # يرفع ملف Excel/CSV، يُرجع job_id (معالجة غير متزامنة)
GET  /api/v1/imports/{job_id}/status  # نتيجة المعالجة: نجح/فشل/أخطاء لكل سطر
GET  /api/v1/products/export?format=xlsx
POST /api/v1/attachments              # رفع ملف عام (multipart/form-data) يُرجع attachment_id لاستخدامه في أي مستند
```

### 9.5 Authentication & Authorization
```
POST /api/v1/auth/login               # يُرجع access_token + refresh_token
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
GET  /api/v1/auth/me                  # المستخدم الحالي + شركاته + أدواره
```
- `company_id` **لا يُرسَل أبداً في الـ Body للعمليات الحساسة**، بل يُشتق من `X-Company-Id` Header يُتحقق من صلاحية المستخدم عليه، أو ضمن الـ Token نفسه إذا كان المستخدم مرتبطاً بشركة واحدة فقط.

### 9.6 Backup APIs (Platform/Admin فقط)
```
POST /api/v1/admin/companies/{id}/backup        # تشغيل نسخة احتياطية فورية
GET  /api/v1/admin/companies/{id}/backups       # قائمة النسخ المتاحة
POST /api/v1/admin/companies/{id}/restore       # استرجاع (محمي بصلاحية Platform Admin فقط + تأكيد مزدوج)
```

### 9.7 AI Platform Endpoints (تفصيل كامل للمحرك في القسم 7)
```
POST /api/v1/ai/documents/analyze        # رفع صورة/PDF فاتورة، يُرجع job_id فوراً (معالجة غير متزامنة عبر ai-platform)
GET  /api/v1/ai/documents/jobs/{job_id}  # حالة المعالجة (queued/processing/done/failed) + النتيجة عند الاكتمال
GET  /api/v1/ai/drafts/{draft_id}        # تفاصيل المسودة المستخرجة (كل حقل + درجة الثقة) لعرضها بشاشة المراجعة
POST /api/v1/ai/drafts/{draft_id}/confirm # اعتماد المسودة (بعد تعديل المستخدم إن لزم) → تتحول لفاتورة فعلية في وحدة sales/purchasing
POST /api/v1/ai/drafts/{draft_id}/reject
POST /api/v1/ai/drafts/{draft_id}/corrections  # تسجيل تصحيح حقل معيّن (يغذّي Learning Engine — القسم 7.6)
GET  /api/v1/ai/entities/{type}/suggestions?text=...   # اقتراحات مطابقة فورية (منتج/مورد) أثناء الكتابة اليدوية أيضاً، وليس فقط بعد OCR
POST /api/v1/ai/assistant/chat           # رسالة للمساعد الذكي (مرحلة لاحقة — القسم 7.8)
GET  /api/v1/ai/recommendations          # توصيات جاهزة (إعادة طلب، تسعير...) — مرحلة لاحقة
```
- كل هذه الـ Endpoints تعيش فعلياً خلف بوابة `core-api` (نفس Auth/RBAC/Rate Limiting الموحّد)، لكنها داخلياً تُوجَّه لخدمة `ai-platform` المنفصلة (القسم 7.10/6.12) — العميل (Web/Mobile/Desktop) لا يرى أو يهتم بهذا الفصل الداخلي إطلاقاً.

### 9.8 Webhooks (للتكاملات الخارجية)
```
POST /api/v1/webhooks/subscriptions   # الشركة تسجّل رابطاً ليستقبل أحداثاً (invoice.posted, payment.received...)
```
— تُبنى فوق نفس Event Bus الداخلي المذكور في قسم Architecture: أي حدث داخلي معلن "عام" يمكن نشره خارجياً بنفس الآلية.

## 10. Naming Convention (نظام تسمية موحّد)

| العنصر | القاعدة | مثال |
|---|---|---|
| مجلدات Backend (Modules) | `snake_case` مفرد بالمعنى الوظيفي | `invoicing/`, `inventory/`, `accounting/` |
| ملفات Python | `snake_case.py` | `invoice_service.py`, `journal_repository.py` |
| Classes (Python) | `PascalCase` | `InvoiceService`, `JournalEntryRepository` |
| Interfaces/Ports (Python — Protocol/ABC) | بادئة `I` أو لاحقة `Port` | `IInvoiceRepository` أو `InvoiceRepositoryPort` |
| Pydantic DTOs (Request/Response) | لاحقة واضحة للاتجاه | `InvoiceCreateRequest`, `InvoiceResponse`, `InvoiceListItem` |
| Enums | `PascalCase` للكلاس، `UPPER_SNAKE` للقيم | `class InvoiceStatus: DRAFT, POSTED, PAID` |
| Use Cases | فعل + كيان + `UseCase` | `PostInvoiceUseCase`, `CreateJournalEntryUseCase` |
| Controllers/Routers (FastAPI) | `{resource}_router.py` | `invoices_router.py` |
| متغيرات وثوابت | `snake_case` للمتغيرات، `UPPER_SNAKE_CASE` للثوابت | `max_retry_count`, `DEFAULT_PAGE_SIZE` |
| جداول قاعدة البيانات | `snake_case` جمع | `invoices`, `journal_entry_lines` |
| أعمدة قاعدة البيانات | `snake_case` | `created_at`, `company_id` |
| مفاتيح أجنبية | `{singular_table}_id` | `invoice_id`, `account_id` |
| API Routes | `kebab-case` جمع للموارد | `/purchase-orders`, `/journal-entries` |
| أكواد الأخطاء (Error Codes) | `UPPER_SNAKE_CASE` وصفي | `JOURNAL_ENTRY_UNBALANCED`, `INSUFFICIENT_STOCK` |
| مكوّنات Frontend (React) | `PascalCase.tsx` | `InvoiceListPage.tsx`, `AccountTreeSelect.tsx` |
| Hooks (React) | بادئة `use` | `useInvoices.ts`, `usePermissions.ts` |
| ملفات CSS/Style Modules | مطابقة اسم المكوّن | `InvoiceListPage.module.css` |
| فروع Git | انظر قسم "خطة الدمج" | `feature/invoicing-post-endpoint` |

**قاعدة صارمة:** أي اسم جديد (Module/Table/Endpoint) يجب أن يمرّ بمراجعة تسمية سريعة (Naming Review) ضمن الـ PR الأول الذي يُدخله، لتفادي التضارب المذكور في متطلبات المشروع.

## 11. Coding Standards

### 11.1 Folder Structure Rules
- كل Module جديد **يجب** أن يتبع القالب الأربع-طبقات المذكور في القسم 6.4 حرفياً (`domain/application/infrastructure/presentation`) — لا استثناءات.
- لا يوجد ملف يتجاوز ~300 سطر تقريباً كقاعدة إرشادية (ليست صارمة رياضياً)؛ إن تجاوزها يُقسَّم منطقياً.

### 11.2 Import Rules
- **ممنوع** أي `import` مباشر من `infrastructure` أو `presentation` داخل `domain`.
- **ممنوع** أي Module يستورد مباشرة من `infrastructure` لـ Module آخر (مثال: `invoicing` لا يستورد `inventory.infrastructure.repositories`) — التواصل بين الوحدات فقط عبر: (أ) الواجهة العامة المعلنة للـ Module (`application` layer المصدَّرة)، أو (ب) الأحداث (Events).
- تُفرض هذه القواعد آلياً عبر أداة تحليل استيراد ضمن CI (فشل الـ Build عند مخالفتها)، وليس فقط Code Review.

### 11.3 Error Handling
- كل Use Case يرفع استثناءات من التصنيف المعرَّف في القسم 6.8 فقط (`DomainError` وفروعه) — لا تُرفع استثناءات Python عامة (`Exception`) بمنطق أعمال.
- كل استثناء غير متوقع (تقني) يُسجَّل بالكامل (Stack Trace) في اللوق، ويُرجَع للعميل رسالة عامة آمنة فقط + `request_id` لتتبعه.

### 11.4 Logging
- لا `print()` إطلاقاً — فقط عبر Logger المركزي من `platform_core`.
- كل عملية تُغيّر بيانات مالية (ترحيل قيد، حذف، تعديل مبلغ) تُسجَّل حتماً في `audit_logs` بالإضافة لسجل التطبيق العادي.

### 11.5 Documentation
- كل Use Case وPublic Function تحمل Docstring بصيغة: الهدف، المدخلات، المخرجات، الاستثناءات المحتملة.
- كل Endpoint موثَّق تلقائياً عبر OpenAPI (وصف + أمثلة Request/Response) — يُعتبر جزءاً من "تعريف الإنجاز" (Definition of Done) لأي API جديد.

### 11.6 Testing
- **Domain layer:** اختبارات وحدة خالصة (بدون قاعدة بيانات) — الأسرع والأكثر تغطية مطلوبة (>90%).
- **Application layer (Use Cases):** اختبارات مع Repository وهمي (Fake/Mock تنفيذ لواجهة الـ Port).
- **Infrastructure layer:** اختبارات تكامل فعلية مع قاعدة بيانات اختبار (Test Container/SQLite للاختبار السريع أو Postgres حقيقي في CI).
- **API layer:** اختبارات End-to-End لأهم السيناريوهات (Happy Path + أهم حالات الفشل) لكل Endpoint.
- لا يُدمَج أي PR يخفّض نسبة تغطية الاختبارات الإجمالية.

### 11.7 Commit Convention
نمط [Conventional Commits](https://www.conventionalcommits.org):
```
feat(invoicing): add post invoice endpoint
fix(inventory): correct weighted-average cost calculation
refactor(accounting): extract journal balance validation to domain rule
test(invoicing): add unit tests for invoice cancellation
docs(api): document pagination query params
```

### 11.8 Branch Naming
```
feature/{module}-{short-description}    → feature/invoicing-post-endpoint
fix/{module}-{short-description}        → fix/inventory-negative-stock-bug
chore/{short-description}               → chore/update-ci-pipeline
```

### 11.9 Pull Request Rules
- عنوان الـ PR يطابق نمط Conventional Commit.
- وصف PR يشمل: ماذا تغيّر، لماذا، كيف اختُبر، لقطات شاشة إن وُجدت واجهة.
- **PR واحد = تغيير منطقي واحد** (لا PR ضخم يخلط عدة Modules).
- لا يُدمَج PR بدون: (1) مراجعة من عضو آخر على الأقل، (2) اجتياز كل اختبارات CI، (3) عدم مخالفة قواعد الاستيراد، (4) تحديث التوثيق إن لزم.

### 11.10 Code Review Rules
- المراجعة خلال 24 ساعة عمل كحد أقصى لتفادي تعطيل باقي الفريق (خاصة أن كل عضو يعمل بشكل مستقل — القسم 13).
- التركيز في المراجعة: صحة منطق الأعمال، الالتزام بحدود الـ Module، وجود اختبارات كافية، وليس فقط الأسلوب الشكلي (يُترك للأدوات الآلية: Linter/Formatter مثل `ruff`/`black` لـ Python وESLint/Prettier للواجهة).

## 12. هيكل المشروع (Folder Structure — Monorepo)

```
alqaim-v2/                              # Monorepo واحد لكل المنصات (يسهّل تقاسم الـ DTOs/Types)
│
├── apps/
│   ├── core-api/                       # ★ Backend الرئيسي (Python/FastAPI) — Modular Monolith
│   │   ├── platform_core/              # بنية تحتية مشتركة (DB, Auth Middleware, EventBus, Cache, Storage, Logging)
│   │   ├── shared_kernel/              # Money, Currency, TenantContext, AuditFields
│   │   ├── modules/
│   │   │   ├── identity/               # Auth, Users, Roles, Permissions
│   │   │   ├── tenancy/                # Companies, Branches, Warehouses, Settings, Numbering
│   │   │   ├── partners/               # Customers, Suppliers, Contacts
│   │   │   ├── catalog/                # Products, Categories, UoM, Price Lists
│   │   │   ├── inventory/              # Stock, Movements, Transfers, Valuation
│   │   │   ├── purchasing/             # Purchase Orders/Invoices
│   │   │   ├── sales/                  # Quotations, Sales Orders/Invoices, Credit Notes
│   │   │   ├── pos/                    # نقاط البيع
│   │   │   ├── accounting/             # Chart of Accounts, Journal Entries, Fiscal Periods
│   │   │   ├── taxation/               # Tax Engine, E-Invoicing
│   │   │   ├── payments/               # Payments, Receipts, Bank Reconciliation
│   │   │   ├── reporting/              # Financial Reports, Dashboards
│   │   │   ├── workflow/               # Approval Engine (يُستهلك من كل الوحدات)
│   │   │   ├── notifications/
│   │   │   ├── documents/              # File/Attachment Manager
│   │   │   ├── audit/                  # Audit Log
│   │   │   ├── integrations/           # Webhooks, Import/Export
│   │   │   └── platform_admin/         # إدارة الشركات/التراخيص (لوحة مزوّد الخدمة)
│   │   ├── migrations/                 # Alembic (قاعدة بيانات واحدة، كل Module يضيف migrations خاصة به بترتيب)
│   │   ├── tests/
│   │   └── main.py
│   │
│   ├── web/                            # Next.js — لوحة الإدارة + PWA
│   │   └── src/{pages or app, components, features/{module}, common, hooks, api-client}
│   │
│   ├── ai-platform/                     # ★ خدمة الذكاء الاصطناعي المستقلة (القسم 6.12/7.10) — Python
│   │   ├── services/
│   │   │   ├── preprocessing/           # Document Preprocessing (OpenCV)
│   │   │   ├── ocr/                     # OCR Engine (تنفيذ IOCREngine — PaddleOCR افتراضياً)
│   │   │   ├── invoice_parser/          # Invoice Understanding
│   │   │   ├── entity_matching/         # Product/Supplier Matching (pgvector)
│   │   │   ├── validation/              # AI Validation Engine
│   │   │   ├── learning/                # Learning Engine (تخزين التصحيحات)
│   │   │   ├── llm_gateway/             # ILLMProvider (سحابي/ذاتي حسب النشر)
│   │   │   └── recommendation/          # Recommendation & Predictive (مراحل لاحقة)
│   │   ├── workers/                     # Celery workers تلتقط المهام من core-api عبر Redis
│   │   └── main.py                      # REST داخلي + Worker entrypoint
│   │
│   ├── desktop/                        # غلاف Tauri فوق apps/web (Windows/Linux/macOS)
│   │
│   ├── mobile/                         # Flutter (Android/iOS) — تطبيق مستقل يستهلك نفس API
│   │   └── lib/{features/{module}, core, widgets}
│   │
│   └── pos-app/                        # Flutter (يُشغَّل كجهاز مستقل Offline-first) — قد يُدمَج لاحقاً مع mobile
│
├── packages/                           # كود مشترك بين التطبيقات (Frontend فقط هنا)
│   ├── api-types/                      # أنواع TypeScript مولَّدة تلقائياً من OpenAPI الخاص بـ core-api
│   ├── ui-kit/                         # مكوّنات تصميم مشتركة (Web/Desktop/PWA)
│   └── i18n/                           # قواميس الترجمة المشتركة
│
├── infra/
│   ├── docker/                         # Dockerfiles لكل تطبيق
│   ├── docker-compose.selfhosted.yml   # حزمة Self-Hosted كاملة (API+DB+Redis+MinIO+Web)
│   ├── k8s/                            # Manifests للنشر السحابي
│   └── ci/                             # إعدادات GitHub Actions
│
├── docs/
│   ├── architecture/                   # هذه الوثيقة ومشتقاتها
│   ├── api/                            # OpenAPI مُصدَّر + أدلة استخدام
│   └── modules/                        # ملف تفصيلي لكل Module عند تصميمه (domain rules, ERD جزئي)
│
├── scripts/                            # سكربتات إعداد/بيانات تجريبية/نسخ احتياطي
├── .github/workflows/                  # CI/CD
└── README.md
```

**ملاحظة معمارية مهمة:** `core-api` هو **مصدر الحقيقة الوحيد**؛ لا يوجد أي منطق أعمال مكرر في `web`/`desktop`/`mobile` — هذه التطبيقات الثلاثة عرض وتفاعل فقط (Presentation)، بخلاف V1 حيث كانت الواجهة تستورد المحرك مباشرة. `ai-platform` هو الاستثناء الوحيد المتعمَّد المنشور بشكل مستقل عن `core-api` (القسم 6.12).

## 13. تقسيم الفريق (Team Division) & 14. مصفوفة الملفات/الجداول/الـ APIs لكل عضو

### 13.1 آلية ضمان الاستقلالية الحقيقية (قبل توزيع الأعضاء)

المشكلة المعتادة في تقسيم فرق ERP: عضو ينتظر عضواً آخر لأن وحدته تعتمد على وحدته (مثال: Sales يحتاج Accounting لتوليد القيد). الحل المعتمد هنا:

1. **يوم 0 (Contract-First):** قبل أي كتابة كود فعلي، يجتمع الفريق ليعرّف **الواجهات (Ports/Interfaces) وDTOs وأسماء الأحداث (Event Names)** بين كل الوحدات (مثال: `IAccountingPort.record_invoice_posting(invoice_dto) -> JournalEntryRef`، وحدث `InvoicePosted{invoice_id, total, currency, partner_id}`). هذه العقود تُحفَظ في `shared_kernel`/توثيق مشترك ولا تتغيّر إلا بموافقة الفريق.
2. **كل عضو يبني وحدته ضد Mock/Fake لتنفيذ باقي الوحدات** (مثال: عضو Sales يستخدم `FakeAccountingPort` أثناء تطويره، لا ينتظر تنفيذ Accounting الحقيقي).
3. **الدمج (Integration) يحدث دورياً** (كل نهاية Sprint) عبر استبدال الـ Fakes بالتنفيذ الحقيقي — لا يحدث "دمج كبير" في آخر المشروع.
4. عضو **Platform & Foundation** يسلّم **نسخة أولية (Skeleton) من Auth/TenantContext خلال أول 2-3 أيام** حتى لو غير مكتملة، لأنها الاعتماد الوحيد الحقيقي الذي لا يمكن Mock بسهولة من اليوم الأول (باقي الفريق يحتاج شكل الـ Token وTenantContext ليبني عليه).

### 13.2 توزيع الأعضاء (8 أعضاء أساسيين لمرحلة MVP + إمكانية التوسع)

> **تحديث بعد مراجعة الخبير:** أُضيف عضو ثامن أساسي (وليس اختيارياً) لمنصة الذكاء الاصطناعي، لأنها أصبحت جزءاً من MVP نفسه (القسم 5/7) وليست Future Module. هذا العضو مستقل تماماً بحكم أن `ai-platform` خدمة منفصلة النشر أصلاً (القسم 6.12) — لا ينتظر ولا يُنتظَر من بقية الفريق إلا عبر العقد المتفق عليه (Job Queue + Event Bus).

---

#### 👤 العضو 1 — Platform & Foundation Lead
- **المسؤولية:** `platform_core` (DB/Auth Middleware/EventBus/Cache/Storage/Logging)، وحدات `identity` (Auth/Users/Roles/Permissions) و`tenancy` (Companies/Branches/Warehouses/Settings/Numbering)، هيكل Workflow Engine الأساسي (بدون تفعيل استخدامه من الوحدات بعد).
- **المجلدات:** `apps/core-api/platform_core/**`, `apps/core-api/modules/identity/**`, `apps/core-api/modules/tenancy/**`
- **الجداول:** `users, roles, permissions, role_permissions, user_company_roles, companies, branches, warehouses, system_settings, numbering_sequences, audit_logs (البنية فقط، كل module يكتب إليه)`
- **APIs:** `/auth/*`, `/users/*`, `/roles/*`, `/companies/*`, `/branches/*`, `/warehouses/*`, `/settings/*`
- **الواجهات التي يوفّرها للآخرين:** `AuthMiddleware`, `TenantContext`, `IPermissionChecker`, `INumberingService.next_number(document_type)`, `EventBus.publish/subscribe`
- **الاختبارات المطلوبة:** اختبارات وحدة لـ RBAC (مصفوفة صلاحيات كاملة)، اختبار تكامل لتوليد الأرقام المتزامن (لا تكرار عند الطلب المتزامن)، اختبار عزل `company_id` (محاولة وصول شركة لبيانات شركة أخرى يجب أن تُرفض دائماً).
- **معيار التسليم:** أي مستخدم يمكنه تسجيل الدخول، إنشاء شركة وفرع ومستودع، وتعيين دور لمستخدم آخر، مع رفض كامل لأي طلب عبر شركة أخرى.

---

#### 👤 العضو 2 — Master Data (Partners & Catalog)
- **المسؤولية:** وحدة `partners` (Customers/Suppliers/Contacts)، وحدة `catalog` (Products/Categories/UoM/Price Lists)، Import/Export لهذه الكيانات.
- **المجلدات:** `apps/core-api/modules/partners/**`, `apps/core-api/modules/catalog/**`
- **الجداول:** `partners, product_categories, units_of_measure, products, price_lists, price_list_items`
- **APIs:** `/partners/*`, `/products/*`, `/categories/*`, `/units-of-measure/*`, `/price-lists/*`, `/products/import`, `/products/export`
- **الواجهات التي يوفّرها:** `IPartnerLookup.get(partner_id)`, `IProductLookup.get(product_id)` (تُستخدم من Sales/Purchasing/Inventory دون أن تعتمد هذه الوحدات على تفاصيل تنفيذه)
- **الاختبارات:** تحقق قيود التفرد (`code` فريد لكل شركة)، اختبار استيراد Excel بملف يحتوي أخطاء جزئية (يُقبل الصحيح ويُرفض الخاطئ مع تقرير واضح).
- **معيار التسليم:** كتالوج منتجات وعملاء/موردين كامل قابل للإدخال يدوياً واستيراده بالجملة.

---

#### 👤 العضو 3 — Inventory
- **المسؤولية:** وحدة `inventory` بالكامل (أرصدة، حركات، تحويلات، تسويات، طرق تقييم، Barcode/QR).
- **المجلدات:** `apps/core-api/modules/inventory/**`
- **الجداول:** `stock_movements, stock_balances (materialized), stock_transfers, stock_adjustments`
- **APIs:** `/inventory/movements`, `/inventory/balances`, `/inventory/transfers`, `/inventory/adjustments`, `/inventory/barcode/{code}`
- **يعتمد على:** `IProductLookup` (من العضو 2) — عبر Mock بداية.
- **يوفّر للآخرين:** `IInventoryPort.reserve_stock(...)`, `IInventoryPort.deduct_stock(...)` — تُستخدم من Sales/POS عند ترحيل الفاتورة.
- **الاختبارات:** اختبار حساب المتوسط المرجّح رياضياً بدقة (Decimal بدون فروقات تقريب)، اختبار منع الرصيد السالب (أو السماح به بإعداد صريح "شراء على المكشوف" حسب سياسة الشركة).
- **معيار التسليم:** رصيد كل منتج بكل مستودع صحيح ومطابق لمجموع الحركات دائماً (Reconciliation Test تلقائي في CI).

---

#### 👤 العضو 4 — Sales & POS
- **المسؤولية:** وحدة `sales` (عروض أسعار/أوامر بيع/فواتير بيع/إشعارات دائن) ووحدة `pos`.
- **المجلدات:** `apps/core-api/modules/sales/**`, `apps/core-api/modules/pos/**`
- **الجداول:** `quotations, sales_orders, invoices (type=sale), invoice_lines, credit_notes, pos_sessions, pos_sync_queue`
- **APIs:** `/quotations/*`, `/sales-orders/*`, `/sales-invoices/*`, `/credit-notes/*`, `/pos/*`, `/pos/sync`
- **يعتمد على:** `IInventoryPort` (العضو 3), `IPartnerLookup/IProductLookup` (العضو 2), `IAccountingPort` (العضو 6) — كلها عبر Mocks بداية.
- **الاختبارات:** اختبار كامل لدورة حياة فاتورة البيع (مسودة→مرحّلة→مدفوعة) وأثرها على Mocks المخزون والمحاسبة، اختبار طابور مزامنة POS (تعارض بيع نفس الكمية من جهازين).
- **معيار التسليم:** إمكانية إنشاء عرض سعر → تحويله لأمر بيع → فاتورة → ترحيلها، وبيع فعلي عبر واجهة POS محاكاة (Simulated Offline).

---

#### 👤 العضو 5 — Purchasing & Payments
- **المسؤولية:** وحدة `purchasing` (طلبات/أوامر شراء/فواتير شراء)، وحدة `payments` (سندات قبض/صرف، حسابات بنكية، تسوية بنكية أساسية).
- **المجلدات:** `apps/core-api/modules/purchasing/**`, `apps/core-api/modules/payments/**`
- **الجداول:** `purchase_orders, invoices (type=purchase), payments, receipts, bank_accounts, bank_statements_lines`
- **APIs:** `/purchase-orders/*`, `/purchase-invoices/*`, `/payments/*`, `/receipts/*`, `/bank-accounts/*`
- **الاختبارات:** اختبار ربط الدفعة الجزئية بعدة فواتير، اختبار مطابقة كشف حساب بنكي مع السجلات الداخلية.
- **معيار التسليم:** دورة شراء كاملة من أمر شراء لفاتورة لسند صرف، مع تحديث حالة الفاتورة (مدفوعة جزئياً/كلياً) تلقائياً.

---

#### 👤 العضو 6 — Accounting & Taxation
- **المسؤولية:** وحدة `accounting` (شجرة حسابات/قيود/سنوات وفترات مالية/مراكز تكلفة)، وحدة `taxation` (ضرائب/فوترة إلكترونية)، `reporting` (التقارير المالية الأساسية).
- **المجلدات:** `apps/core-api/modules/accounting/**`, `apps/core-api/modules/taxation/**`, `apps/core-api/modules/reporting/**`
- **الجداول:** `accounts, journal_entries, journal_entry_lines, fiscal_years, fiscal_periods, cost_centers, tax_rates, e_invoice_submissions`
- **APIs:** `/accounts/*`, `/journal-entries/*`, `/fiscal-periods/*`, `/tax-rates/*`, `/reports/trial-balance`, `/reports/income-statement`, `/reports/balance-sheet`
- **يوفّر للآخرين:** `IAccountingPort.record_document_posting(document_dto) -> JournalEntryRef` — **هذه أهم واجهة في كامل النظام** ويجب تثبيتها (Freeze) مبكراً جداً في يوم العقود (12.1) لأن كل وحدة تشغيلية (Sales/Purchasing/Inventory/Payroll لاحقاً) تعتمد عليها.
- **الاختبارات:** اختبار توازن القيد إلزامياً (Constraint + منطق تطبيق)، اختبار إقفال الفترة المالية (منع أي قيد جديد على فترة مقفلة).
- **معيار التسليم:** أي حدث تشغيلي (فاتورة، دفعة) يولّد قيداً متوازناً صحيحاً تلقائياً، وميزان المراجعة يتوازن دائماً.

---

#### 👤 العضو 7 — Web Frontend Lead
- **المسؤولية:** `apps/web` بالكامل (لوحة الإدارة)، `packages/ui-kit`, `packages/i18n`، توليد `packages/api-types` من OpenAPI.
- **المجلدات:** `apps/web/**`, `packages/ui-kit/**`, `packages/i18n/**`
- **يعتمد على:** OpenAPI Spec المُصدَّرة تلقائياً من `core-api` (لا يحتاج انتظار تنفيذ الـ Backend الفعلي، فقط توقيع الـ Endpoint المتفق عليه من يوم العقود).
- **الاختبارات:** اختبارات مكوّنات (Component Tests)، اختبارات E2E لأهم رحلات المستخدم (تسجيل دخول → إنشاء فاتورة → عرضها في التقرير).
- **معيار التسليم:** واجهة RTL/LTR كاملة، متجاوبة (Responsive)، متصلة فعلياً بكل الـ APIs أعلاه.

---

---

#### 👤 العضو 8 — AI Platform Lead
- **المسؤولية:** خدمة `apps/ai-platform` المستقلة بالكامل (القسم 6.12/7.10): Preprocessing، OCR Engine، Invoice Understanding، Entity Matching، AI Validation، Learning Engine، LLM Gateway.
- **المجلدات:** `apps/ai-platform/**` بالكامل (لا يشاركه فيها أي عضو آخر).
- **الجداول:** `ocr_jobs, invoice_extraction_drafts, entity_match_suggestions, ai_learning_feedback, entity_embeddings, ai_conversations, ai_messages` (القسم 8.5).
- **APIs:** كل مسارات `/ai/*` المذكورة في القسم 9.7 (تُعرَّض عبر `core-api` كبوابة، لكن التنفيذ الفعلي هنا).
- **الواجهات (Ports) التي يلتزم بها:** `IOCREngine`, `IInvoiceParser`, `IEntityMatcher`, `IValidator`, `ILLMProvider`, `ILearningStore` — مثبَّتة يوم العقود (القسم 13.1) مع العضو 4 (Sales — أول مستهلك لمخرجات هذه الخدمة عبر حدث `InvoiceDraftReady`).
- **يعتمد على:** `IProductLookup`/`IPartnerLookup` (العضو 2) عبر Mock بداية، ونفس Event Bus/Job Queue من `platform_core` (العضو 1).
- **الاختبارات المطلوبة:** دقة استخراج على مجموعة فواتير اختبار عربية/إنجليزية حقيقية (Benchmark Set يُعتمَد كمعيار قبول)، اختبار أن لا فاتورة تُرحَّل تلقائياً بدون حالة `approved` صريحة، اختبار المطابقة الدلالية (كوكاكولا/Coca Cola/Coca-Cola تُطابَق فعلياً).
- **معيار التسليم (MVP):** رفع صورة فاتورة مورد حقيقية → استخراج الحقول الأساسية بثقة معقولة → عرضها بشاشة مراجعة → اعتمادها كفاتورة فعلية في وحدة Purchasing، مع تسجيل أي تصحيح يدوي في `ai_learning_feedback`.

---

### 13.3 التوسع لأكثر من 8 أعضاء (عند توفر فريق أكبر)
يمكن فصل الأدوار التالية كأعضاء إضافيين مستقلين دون تعارض حدود:
- **عضو 9 — Mobile & POS Client (Flutter):** يستهلك نفس API، مستقل تماماً عن فريق Backend.
- **عضو 10 — Desktop Client (Tauri wrapper + تحسينات Native):** مستقل، يعتمد فقط على `apps/web` المبني مسبقاً.
- **عضو 11 — DevOps/Platform Admin:** `infra/**`, CI/CD، `modules/platform_admin`, `modules/notifications`, `modules/documents`, `modules/audit` (وحدات أفقية عامة لا ترتبط بمنطق تجاري معقد فتُناسب عضواً مستقلاً منذ البداية).

## 15. خطة الدمج على GitHub

### 15.1 استراتيجية الفروع (Branching Strategy)
**Trunk-Based Development مع فروع قصيرة العمر** (وليس GitFlow الثقيل) — مناسب لفريق يعمل بالتوازي على وحدات معزولة:

```
main                     → دائماً قابل للنشر (Production-ready)
 ├─ feature/identity-rbac-permissions      (العضو 1)
 ├─ feature/catalog-products-crud          (العضو 2)
 ├─ feature/inventory-stock-movements      (العضو 3)
 ├─ feature/sales-invoice-lifecycle        (العضو 4)
 ├─ feature/purchasing-purchase-orders     (العضو 5)
 ├─ feature/accounting-journal-posting     (العضو 6)
 └─ feature/web-invoices-page              (العضو 7)
```
- كل فرع **يعيش أقل من 3-4 أيام عمل** قدر الإمكان (تقسيم المهمة لأجزاء أصغر إن طالت) لتفادي Conflicts الكبيرة.
- `main` محمي: لا Push مباشر، فقط عبر Pull Request معتمد + CI أخضر.

### 15.2 قواعد تفادي التعارض بين الأعضاء
بما أن كل عضو يملك مجلد `modules/{his_module}/**` الخاص به حصرياً (حسب القسم 13)، **تعارضات الدمج (Merge Conflicts) شبه مستحيلة على مستوى الكود التجاري نفسه**. النقاط المشتركة الوحيدة المحتملة:
- `migrations/` (Alembic): يُتفق على ترقيم/تسمية ملفات الهجرة بحيث لا تتصادم (بادئة باسم الـ Module + Timestamp)، وتُدمَج تسلسلياً.
- `shared_kernel/` و`platform_core/`: **العضو 1 فقط** يعدّل هذين المجلدين؛ أي حاجة لتعديل من عضو آخر تمر عبر طلب صريح له (لتفادي كسر الاعتماد المشترك).
- ملف تعريف الـ Router الرئيسي (`main.py` الذي يجمع كل الـ Routers): يُبقى بسيطاً جداً (سطر تسجيل واحد لكل Module) لتقليل احتمال التعارض عليه لأدنى حد.

### 15.3 دورة العمل القياسية (لكل عضو)
1. `git checkout -b feature/{module}-{task}` من `main` محدّث.
2. تطوير + اختبارات محلياً (Domain → Application → Infrastructure → Presentation، بنفس هذا الترتيب لضمان بناء صحيح من الداخل للخارج).
3. Push + فتح PR بالقالب المعرَّف في القسم 11.9.
4. مراجعة عضو آخر (يُفضَّل عضو ذو معرفة بالـ Module المجاور المعتمِد على هذا الـ Module، مثال: العضو 4 يراجع تغييرات العضو 6 في `IAccountingPort`).
5. CI: Lint → Import-Boundary Check → Unit Tests → Integration Tests → Build.
6. دمج عبر **Squash & Merge** (سجل `main` نظيف، تاريخ تفصيلي محفوظ داخل الـ PR نفسه).
7. حذف الفرع تلقائياً بعد الدمج.

### 15.4 نقاط الدمج الدورية (Integration Checkpoints)
- **نهاية كل أسبوع:** استبدال الـ Mocks بين الوحدات بالتنفيذ الحقيقي (مثال: Sales يستبدل `FakeAccountingPort` بالتنفيذ الفعلي من العضو 6) ضمن بيئة Staging، لاكتشاف أي انحراف عن العقود المتفق عليها مبكراً.
- **قبل كل إصدار (Release):** اختبار End-to-End كامل لسيناريو تجاري متكامل (بيع → مخزون → محاسبة → تقرير) يشارك فيه ممثل عن كل وحدة معنية.

### 15.5 CI/CD (GitHub Actions)
```
on: pull_request → lint + import-boundaries + unit-tests + build
on: merge to main → integration-tests (DB حقيقية) + deploy to Staging تلقائياً
on: tag (v*) → deploy to Production (يدوي الموافقة/Manual Approval Gate)
```

## 16. خطة التنفيذ خطوة بخطوة

### المرحلة 0 — التأسيس (أسبوع 1)
1. إعداد الـ Monorepo، Docker Compose المحلي (Postgres/Redis/MinIO)، هيكل المجلدات الكامل (فارغة لكن موجودة لكل Module).
2. **"يوم العقود"**: تعريف كل الـ Ports/DTOs/Events بين الوحدات (القسم 13.1) — مخرج هذه الخطوة وثيقة `docs/architecture/contracts.md` يوقّع عليها كل الأعضاء.
3. العضو 1 يبني Skeleton الخاص بـ Auth/TenantContext/EventBus/Migrations الأساسية.
4. إعداد CI الأساسي (Lint + Import Boundaries + Test Runner) قبل أي كود منطق أعمال حقيقي.

### المرحلة 1 — بناء MVP بالتوازي (أسابيع 2–8، تقديرياً حسب حجم الفريق)
- كل الأعضاء (2–8، بما فيهم العضو 8 — AI Platform) يبنون وحداتهم بالتوازي ضد الـ Mocks المتفق عليها.
- العضو 8 يبني خط أنابيب OCR/Invoice Understanding بالتوازي التام مع البقية بحكم أن `ai-platform` خدمة منفصلة النشر (القسم 6.12) — لا يوجد اعتماد تنفيذي مباشر عليه لبدء العمل، فقط اتفاق العقد (حدث `InvoiceDraftReady`) من يوم العقود.
- دمج أسبوعي (Integration Checkpoint) كما في القسم 15.4.
- نهاية هذه المرحلة: دورة تجارية كاملة تعمل فعلياً (تسجيل شركة → إعداد حسابات ومنتجات → بيع/شراء → محاسبة تلقائية → تقرير مالي أساسي)، **بالإضافة إلى** رفع فاتورة مورد كصورة/PDF ومراجعتها واعتمادها آلياً كفاتورة شراء (القسم 7.13 — المرحلة الأولى).

### المرحلة 2 — التصلّب والإطلاق التجريبي (أسبوعان)
1. اختبار حمل (Load Testing) أساسي على Endpoints الأكثر استخداماً (POS، الفواتير).
2. تدقيق أمني أولي (Auth، عزل الشركات، صلاحيات).
3. إعداد نسخة Self-Hosted (`docker-compose.selfhosted.yml`) واختبارها فعلياً على بيئة نظيفة.
4. إطلاق تجريبي (Beta) لعدد محدود من الشركات الحقيقية، مع قناة تغذية راجعة مباشرة.

### المرحلة 3 — المرحلة الثانية من الوحدات (بالتوازي مع تشغيل MVP فعلياً في السوق)
تنفيذ وحدات القسم 5 (المرحلة الثانية): Workflow Engine العام، POS Offline الكامل، HR الأساسي، CRM، BI أولي — بنفس منهجية التقسيم بالأعضاء والعقود.

### المرحلة 4 — التوسع للمنصات الأخرى
- Web مستقر أولاً → PWA (تكلفة إضافية شبه معدومة) → Desktop (Tauri) → Mobile (Flutter) → POS مستقل.
- كل منصة جديدة **لا تحتاج أي تغيير على `core-api`** إن التُزم بمبدأ API-first (القسم 6.2) بدقة — هذا هو المقياس الحقيقي لنجاح القرار المعماري.

### مؤشرات نجاح كل مرحلة (Definition of Done على مستوى المرحلة)
- **المرحلة 0:** أي عضو جديد يستطيع تشغيل بيئة التطوير كاملة محلياً خلال أقل من 30 دقيقة باتباع التوثيق فقط.
- **المرحلة 1:** سيناريو "بيع كامل ينتج عنه قيد محاسبي صحيح ويظهر في ميزان المراجعة" يعمل تلقائياً بدون تدخل يدوي.
- **المرحلة 2:** شركة حقيقية تستخدم النظام لمدة أسبوعين متواصلين دون فقدان بيانات أو خطأ محاسبي.

## 17. المخاطر المحتملة وكيفية تجنبها

| المخاطرة | الاحتمال/الأثر | إجراء التخفيف |
|---|---|---|
| **تسرّب منطق أعمال إلى الواجهات مرة أخرى** (تكرار خطأ V1) | عالٍ / عالٍ جداً | إنفاذ قاعدة "الواجهات Presentation-only" آلياً عبر مراجعة الكود + بنية المجلدات نفسها تجعل المخالفة صعبة فيزيائياً |
| **كسر حدود الوحدات (Module Boundaries) مع الوقت** | متوسط / عالٍ | فحص استيراد آلي (Import Linter) في كل PR يفشل الـ Build فوراً عند أي استيراد مخالف |
| **عدم توازن قيد محاسبي بسبب خطأ برمجي** | متوسط / حرج (خطأ مالي) | Constraint على مستوى قاعدة البيانات نفسها (وليس فقط كود التطبيق) + اختبار تلقائي يفحص توازن كل القيود يومياً كـ Job مستقل |
| **فروقات تقريب في المبالغ المالية** | متوسط / عالٍ | استخدام `Decimal/NUMERIC` إلزامياً، ممنوع `Float` تماماً في أي كود يلمس مبالغ — يُفحص عبر Linter مخصص |
| **تأخر عضو Platform (العضو 1) يعطّل الجميع** | عالٍ في البداية / عالٍ | تسليم Skeleton مبكر (يوم 2-3) + بقية الفريق يعمل ضد Mocks من يوم العقود مباشرة، لا ينتظرون التنفيذ الكامل |
| **تعارض بيانات POS Offline عند المزامنة** | متوسط / متوسط | سياسة تعارض واضحة موثّقة (آخر تحديث فائز + تنبيه يدوي عند تعارض الكميات)، واختبارات محاكاة انقطاع اتصال متكررة |
| **بطء الأداء مع نمو عدد الشركات (Shared Schema)** | متوسط (يزداد مع النمو) / عالٍ | فهرسة صارمة من البداية (القسم 8.6) + خطة انتقال جاهزة مسبقاً لعزل الشركات الكبيرة لقاعدة بيانات مستقلة دون تغيير كود |
| **الفوترة الإلكترونية الحكومية تتغيّر متطلباتها بمرور الوقت** (كل دولة تعدّل قواعدها) | عالٍ / متوسط | عزل هذا المنطق بالكامل في `taxation` Module كـ Plugin مستقل قابل للتحديث دون المساس ببقية النظام |
| **اعتماد مفرط على مطور/عضو واحد لمعرفة Module حرج (Bus Factor)** | متوسط / عالٍ | توثيق إلزامي لكل Module (القسم 11.5) + مراجعة PR متبادلة بين الأعضاء المتجاورين (القسم 15.3) بدل الاعتماد على شخص واحد فقط |
| **زحف النطاق (Scope Creep) في MVP** | عالٍ جداً في مشاريع ERP / عالٍ | الالتزام الصارم بقائمة أولويات القسم 5 — أي طلب ميزة جديدة أثناء MVP يُقيَّم مقابل "هل يمنع الإطلاق فعلاً؟" قبل قبوله |
| **مخاطر أمنية: تسرّب بيانات شركة لشركة أخرى** | منخفض الاحتمال لكن حرج جداً عند الحدوث | دفاع مزدوج: تحقق في كود التطبيق + Row Level Security على قاعدة البيانات نفسها كخط دفاع مستقل ثانٍ |
| **اعتماد مفرط على اقتراحات AI Accounting دون تدقيق بشري** | متوسط / حرج (خطأ مالي فعلي) | كل مخرجات الذكاء الاصطناعي (استخراج فاتورة، اقتراح حساب) تبقى "اقتراحاً قابلاً للرفض" فقط — **لا ترحيل تلقائي بلا موافقة بشرية صريحة طوال المراحل المبكرة** (القسم 7.5/7.7)، ويُعاد تقييم توسيع الأتمتة تدريجياً بناءً على مقاييس دقة فعلية مقاسة، لا افتراضاً |
| **إرسال بيانات مالية حسّاسة لخدمات OCR/LLM سحابية خارجية (تعارض مع عملاء Self-Hosted)** | متوسط / عالٍ لعملاء القطاع الحكومي/الحسّاس | تجريد المحركات خلف `IOCREngine`/`ILLMProvider` (القسم 7.10) يسمح بتشغيل بدائل ذاتية بالكامل (PaddleOCR + نموذج مفتوح الأوزان) لهذه الفئة من العملاء دون أي تغيير في بقية النظام |
| **تكلفة تراكمية مرتفعة لاستدعاءات LLM/OCR السحابية مع نمو عدد الشركات** | عالٍ مع النمو / متوسط (يؤثر على هامش الربح) | حلقة التعلّم (القسم 7.6) وتقليل نسبة الفواتير المحتاجة مراجعة كاملة كتخفيف طبيعي مع الوقت، إضافة لتقييم الانتقال التدريجي لنماذج مفتوحة مضبوطة ذاتياً بعد تراكم بيانات تدريب كافية |
| **انحياز/انجراف دقة نموذج المطابقة الدلالية (Embedding Drift) مع تنوع بيانات الشركات** | منخفض/متوسط | مراقبة دورية لمعدل قبول/رفض اقتراحات المطابقة لكل شركة، مع تنبيه فريق AI Platform عند انخفاض ملحوظ في نسبة القبول |

---

## 18. خطة التطوير المستقبلية

### مساران متوازيان بعد استقرار MVP والمرحلة الثانية
1. **العمق الوظيفي**: تنفيذ المرحلة الثالثة (Projects/Manufacturing/Subscriptions/Helpdesk) ثم باقي Future Modules غير المتعلقة بالذكاء الاصطناعي (Fleet Management، تطبيقات Native كاملة) وفق نفس منهجية "عقود أولاً ثم بناء متوازٍ". **مسار تطوير الذكاء الاصطناعي نفسه مفصَّل بالكامل في القسم 7.13** (خمس مراحل من OCR الأساسي وحتى Predictive Analytics المتقدمة) وليس بنداً عاماً هنا بعد الآن.
2. **العمق التقني/التوسعي**:
   - تفعيل GraphQL كطبقة اختيارية فوق نفس `core-api` عند نضوج حاجة المطورين الخارجيين للتقارير المرنة.
   - دراسة فصل الوحدات عالية الحمل (POS، Inventory في ذروة المخزون الكبير) إلى Microservices مستقلة **فقط عند ظهور حاجة أداء فعلية مقاسة**، مستفيدين من حدود الوحدات الجاهزة أصلاً منذ التصميم الأول (`ai-platform` مفصولة أصلاً منذ اليوم الأول لنفس السبب — القسم 6.12).
   - فتح **Marketplace عام للمطورين الخارجيين** (Plugin System من القسم 4) بعد ثبات الـ API ووجود قاعدة عملاء كافية لتبرير الاستثمار — يشمل مستقبلاً إتاحة `ILLMProvider`/`IOCREngine` كنقاط تمديد (Extension Points) للمطورين الخارجيين لتوصيل محركاتهم الخاصة.

### مبدأ ثابت لا يتغيّر عبر كل هذه المراحل
**أي توسع مستقبلي (منصة جديدة، وحدة جديدة، تقنية جديدة) يجب أن يحدث بإضافة Module جديد أو Endpoint جديد، وليس بتعديل جوهر `platform_core` أو كسر عقد قائم.** هذا هو المقياس الحقيقي لنجاح هذه الوثيقة — إن استمر تحقّقه، يكون التصميم قد نجح في تجنّب المصير الذي وصل إليه V1.

---

*نهاية الوثيقة المرجعية V1.0 — الخطوة التالية المقترحة: مراجعة جماعية للفريق على قسم "يوم العقود" (12.1) وتثبيت أول نسخة من `contracts.md` قبل فتح أي Sprint فعلي.*
