# تسليم — `TASK-AI-02`: تأكيد الربط بالمخزون/المحاسبة (VERIFIED + فجوة موثَّقة)

**التاريخ:** 2026-08-13، فوق نفس commit، بعد الأجزاء الثلاثة من `TASK-AI-01`
(133 passed, 4 skipped بعد هذا التسليم — كان 130 قبله).

## نص المهمة كما وردت حرفياً في الخطة

> **الهدف:** التأكد أن فاتورة الشراء من `TASK-AI-01` تمر بنفس مسار الترحيل
> العادي — لا بناء جديد، فقط تكامل واختبار.
> **Tests Required:** فاتورة AI → ترحيل → قيد متوازن + حركة مخزون صحيحة.
> **Definition of Done:** VERIFIED فقط (**فجوة مكتشَفة هنا تُفتَح كـTask
> منفصلة، لا تُحَل ضمنياً**).

الخطة نفسها **تتوقّع صراحة** أن هذه المهمة قد تكتشف فجوة بدل أن "تنجح"
بالكامل — وتُلزِم بعدم حلّها ضمنياً. هذا بالضبط ما حدث.

## ما وُجِد فعلياً عند الفحص (قبل كتابة أي اختبار)

- `PostPurchaseInvoiceUseCase` (الموجودة فعلاً) تُنشئ **قيداً محاسبياً
  فقط** — لا تفحص `purchase_order_id` إطلاقاً ولا تلمس المخزون بأي شكل.
- حركة المخزون في المسار العادي تحدث **في خطوة مختلفة تماماً**:
  `ReceivePurchaseOrderUseCase.increase_stock` — وهي خطوة **"استلام أمر
  شراء"**، لا "ترحيل فاتورة".
- فاتورة AI (`CreatePurchaseInvoiceFromAiDraftUseCase`, الجزء الثاني
  المُسلَّم سابقاً) **بلا أمر شراء إطلاقاً** (`purchase_order_id=None`
  دائماً) — **لا توجد أي خطوة "استلام" يمكن أن تُشغِّل زيادة المخزون على
  هذا المسار بتاتاً**، بصرف النظر عن نجاح الترحيل المحاسبي أو فشله.

## النتيجة: نصف المهمة VERIFIED، والنصف الآخر فجوة حقيقية

### ✅ يعمل فعلاً بلا أي تعديل (مُتحقَّق منه باختبار حقيقي، لا افتراض)

فاتورة AI → `PostPurchaseInvoiceUseCase` مع `SqlAccountingPort` **الحقيقي**
(لا Fake) → قيد محاسبي متوازن حقيقي، `journal_entry_ref` لا يبدأ بـ`FAKE`.
لأن `PostPurchaseInvoiceUseCase` لا تفرّق أصلاً بين فاتورة AI وفاتورة
عادية — **لا بناء جديد كان مطلوباً هنا فعلاً**، مطابقة تماماً لنص المهمة.

### 🔴 فجوة حقيقية (مُتحقَّق منها فعلياً، لا افتراض): حركة المخزون **لا تحدث**

بعد ترحيل فاتورة AI بنجاح كامل، رصيد المخزون لهذا المنتج **غير موجود
إطلاقاً** (ليس صفراً — لا صف أصلاً) لأنه لا آلية على الإطلاق تستدعي
`increase_stock` لفواتير بلا أمر شراء.

**لماذا لم تُحَل هنا (بالضبط كما تُلزِم الخطة):** حلّها الحقيقي يحتاج
قرارات تصميم غير محسومة في أي وثيقة مشروع:
- متى تُضاف حركة المخزون؟ عند ترحيل الفاتورة؟ عند إنشائها من المسودة؟
- أي `warehouse_id`؟ — مسودة AI الحالية (`extracted_payload`) **لا تحتوي
  أي حقل مستودع إطلاقاً**، ولا الـ`Request` الجديد لهذا الـEndpoint يطلبه
  (طلب فقط `branch_id`). هذا قرار بيانات/واجهة جديد، وليس تفصيلاً تقنياً.
- هل تُعامَل فاتورة AI كـ"استلام ضمني تلقائي"، أم يبقى الاستلام خطوة يدوية
  منفصلة حتى في مسار AI (مطابقةً للمسار العادي)؟ قرار منتجي، لا تقني.

فرض أي إجابة من طرف واحد هنا يخالف مبدأ "لا إنشاء جزئي/قرار أعمال بلا
صاحب قرار" المتَّبع حرفياً في كل التسليمات السابقة.

## ما نُفِّذ (اختبارات فقط — صفر كود إنتاجي جديد، مطابقةً لنص المهمة "لا بناء جديد")

`apps/core-api/tests/integration/test_ai_invoice_posting_and_inventory_gap.py`
— 3 اختبارات، تُعيد استخدام دالة الإعداد حرفياً من `test_purchasing_real_ports.py`
(لا تكرار منطق):

| # | السيناريو | النتيجة |
|---|---|---|
| 1 | فاتورة AI → ترحيل بـ`SqlAccountingPort` الحقيقي | ✅ قيد متوازن حقيقي |
| 2 | نفس الفاتورة بعد الترحيل → فحص رصيد المخزون | 🔴 **لا رصيد إطلاقاً — الفجوة مُثبَتة حرفياً** |
| 3 | idempotency عبر مرحلة الترحيل (نفس `draft_id` بعد الترحيل) | ✅ تُعيد نفس الفاتورة المُرحَّلة، لا فاتورة `draft` جديدة |

## التحقق الفعلي (لا افتراض)

```
python3 -m py_compile test_ai_invoice_posting_and_inventory_gap.py   → نجح

pytest tests/integration/test_ai_invoice_posting_and_inventory_gap.py -v
    → 3 passed

pytest tests/integration/ -q
    → 133 passed, 4 skipped, 0 failed
      (130 + 3 جديدة = 133 — لا صفر تراجع)

ruff check <الملف الجديد>
    → إصلاحان تلقائيان (imports غير مستخدَمة، لا علاقة بالمنطق)، صفر مخالفات بعدهما
```

## لم يُلمَس

صفر تعديل على أي كود إنتاجي — هذا التسليم اختبارات فقط، مطابقةً حرفياً
لنص `TASK-AI-02`: "لا بناء جديد، فقط تكامل واختبار."

## 🆕 مهمة جديدة تُفتَح صراحة (كما تُلزِم الخطة، لا حل ضمني)

**`TASK-AI-02b` (مقترَح) — حركة مخزون لفواتير AI بلا أمر شراء.** تحتاج
قراري منتج/تصميم أولاً (مذكوران أعلاه: نقطة الزمن، وحقل `warehouse_id`
المفقود من مسودة AI بالكامل) قبل أي تنفيذ تقني — وليست جزءاً من `TASK-AI-01`
ولا `TASK-AI-02` كما وردتا حرفياً في الخطة.

## المتبقي فعلياً في المسار الحرج

`TASK-AI-02` مكتملة (VERIFIED جزئياً + فجوة موثَّقة بدل حل ضمني، تماماً
كما تسمح الخطة). التالي: `TASK-AI-03` (اختبار E2E الكامل = `GATE-03`
نفسه) — يحتاج خادم `ai-platform` حقيقي يعمل فعلياً، بيئة غير متاحة محلياً
هنا (نفس قيد `test_ai_gateway_proxy.py` الموجودة أصلاً والمُخطَّاة تلقائياً
في `TEST_BASELINE.md`).
